# Sitzung 2026-08-01 — Entwicklungsumfeld & Inventur

**Ausgangs-Commit:** `719f3b7`
**Commits dieser Sitzung:** `cf7f890` (gstack-Doku)
**Branch:** `dev`, synchron mit `origin/dev`, 1 Commit ahead, **nicht gepusht**
**Modell:** Opus 5 (1M Kontext), Claude Code v2.1.220

---

## Ziel der Sitzung

Entwicklungsumfeld neu aufsetzen, Repo inventarisieren, SSOT-Struktur anlegen.

## Was tatsächlich passiert ist

### 1. Die Enforcement-Schicht existierte nicht

`[cmd]` `.claude/hooks/pre-tool.ps1` hat 4 Parse-Fehler und lief **nie**.
Ursache: UTF-8 ohne BOM plus Emoji (🚨, 🔒) ausserhalb der BMP — PowerShell 5.1
liest BOM-lose Dateien als ANSI, zerlegt die Surrogate-Pairs und das String-Quoting
zerbricht. Damit gab es weder HARD_STOP noch SCOPE_GUARD noch Audit-Log.

Drei weitere Fehler, unabhängig davon:
- `settings.json` rief das Skript ohne Argumente auf; Claude Code übergibt
  Hook-Daten als JSON über **stdin**, nicht als benannte Parameter
- Matcher stand auf `Bash`, der SCOPE_GUARD prüfte `write|create|edit` — nie wahr
- Blockiert wurde mit `exit 1`; Claude Code blockiert nur bei **exit 2**

`[cmd]` `settings.local.json` enthielt 47 gewachsene Allow-Regeln, darunter
`Bash(powershell -Command:*)` als Generalschlüssel sowie `git add *` und
`git commit -m ' *`. Ein `deny`-Block existierte nirgends.

**Ergebnis: null Absicherung, obwohl alles so aussah, als liefe es.**

### 2. Neue Permission-Schicht, verifiziert

`.claude/settings.json`: 14 Deny / 7 Ask / 13 Allow, Hooks ausgehängt.
`settings.local.json` auf lean-ctx-Hooks reduziert, Sicherung als `.bak-2026-08-01`.

Provokationstest in Claude Code:
- `[cmd]` `git push --dry-run` → **geblockt**
- `[cmd]` `pnpm typecheck` → lief ohne Nachfrage, 17/17 grün
- `rm -rf` → **ungetestet**, Claude prüfte erst statt auszuführen (offen: B-03)

`[cmd]` Leck sichtbar geworden: eine verkettete Bash-Zeile mit `&&` und `;`
landete bei „requires approval" statt bei Deny. Deny matcht Präfixe, nicht
Befehle innerhalb einer Kette. Dafür braucht es den Hook (B-04).

### 3. Die SSOT war an einer Stelle falsch — mit Folgen

`docs/ist-zustand/00-overview.md` behauptete, `services/` und `packages/` seien leer.
Die Datei nennt den Grund selbst: der Scan lief ohne funktionierende Shell
(„Timeout bei jedem Kommando") und konnte nicht bauen.

Dieser ungeprüfte Satz wanderte in die Projektanweisung und stand dort als Regel
(„Erfinde keine Pfade dort"). Ich habe darauf mehrfach aufgebaut, unter anderem
Service-Einträge fälschlich als Altlast eingeordnet.

`[cmd]` Tatsächlich: 17 Packages mit Code, 42 leere Gerüste.
Tom hat geklärt: die leeren Ordner sind der **spezifizierte Endausbau**, kein Zerfall.

**Konsequenz:** Herkunftsmarker `[cmd]` / `[read]` / `[annahme]` sind ab jetzt Pflicht
in `docs/ssot/`. Nur `[cmd]`-Aussagen dürfen zu Regeln werden.

### 4. Inventur abgeschlossen

`[cmd]` 59 Workspace-Verzeichnisse, 17 mit Code, 42 Gerüste.
`[cmd]` **`apps/web` hat null `@lumeos/*`-Abhängigkeiten und null `@lumeos/`-Importe.**
13 der 17 lebenden Packages bilden einen Governance-Cluster, verwurzelt in `wo-core`,
funktional vollständig vom Produkt getrennt.

`[read]` `packages/shared` und `packages/types` sind Produktcode (Supabase-Clients,
Nutrition-Typen), aber unverdrahtet. `packages/supabase-clients` ist Governance
(sagt im Quelltext „for Control Plane").

`[cmd]` `temp/` belegt 16,5 GB, untracked.

### 5. Specs gesichtet

`[read]` 13 Module, ~1,5 MB Markdown, konsistentes Schema
`SPEC_01 Contract → 10 Components`. Nutrition am tiefsten ausgearbeitet
(46 Dateien, eigene Ordner für Decisions/Patches/ADRs/Reviews).
Kernprodukt ist laut `00_MASTER_VISION.md` **Buddy**; `apps/web` ist eines von fünf
Frontends. Die Workspaces sind separate Anwendungen mit Auth-Handoff, kein Embed.

`[cmd]` Stack-Lücke: 10 von 13 in `WebPlatform/INDEX.md` deklarierten Bibliotheken
fehlen in `apps/web/package.json`.

### 6. Angelegt

- `docs/ssot/00-INDEX.md`, `docs/ssot/10-workspace.md`
- `docs/todo/TODO.md` (Bereiche A–D, fortgeführt aus 2026-07-30)
- `docs/sessions/2026-08-01.md` (diese Datei)
- Leere Ordner: `docs/_archive/`

---

## Offen, unmittelbar als Nächstes

1. **A-01.3 bis A-01.7** — restliche SSOT-Dateien. `11-zielarchitektur.md` und
   `40-spec-code-matrix.md` erfordern Lesearbeit über `docs/specs/`.
   **Empfehlung: eigene Sitzung mit Fable 5**, frischer Kontext.
2. **A-02** — `CLAUDE.md` ausdünnen auf Schreibregeln + Verweis auf die SSOT.
3. **A-03 / A-05** — Archivierung und Müllentfernung.
4. **B-03 / B-04** — `rm`-Deny provozieren, dann den PreToolUse-Hook bauen.

## Nicht committet

`.claude/settings.json` ist geändert, aber nicht committet.
Die neuen Dateien unter `docs/ssot/`, `docs/todo/`, `docs/sessions/` ebenfalls nicht.
`docs/` steht in `.gitignore` **nicht** — aber `.claude/` schon (Zeile 79),
weshalb `settings.json` seinerzeit erzwungen hinzugefügt wurde.

## Hinweis für die nächste Sitzung

Fable 5 zieht laut Startup-Hinweis bis zu 50 % des Wochenlimits und verbraucht
schneller als Opus 4.8. Für gezielte Analyseläufe einsetzen, danach zurück auf Opus 5.

Bei Fable 5 möglich: automatischer Modellwechsel, wenn der Cybersecurity- oder
Biologie-Klassifikator anschlägt. Kann bereits bei der ersten Anfrage auslösen,
weil Workspace-Kontext (CLAUDE.md, Git-Status) mitgeht. Dieses Repo enthält
Security-Skills und Medical-Module — beide Domänen. Test bei Verdacht:
`claude --safe-mode`.
