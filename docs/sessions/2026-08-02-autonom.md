# Sitzung 2026-08-02 — autonomer Lauf (3 Jobs)

**Ankerhash bei Start:** 76c8080 (verifiziert, HEAD)
**Auftrag:** Job 1 Migrations-Rückbau (D-12) · Job 2 Spec-Audit (D-05) ·
Job 3 Encoding-Schäden (B-09). Keine Entscheidungen, keine Commits,
Schreibrechte nur `supabase/migrations-draft/` und `docs/ssot/`
(plus dieser Bericht, laut Auftrag).

---

## Job 1 — Migrations-Rückbau: **vollständig erledigt**

Container `supabase_db_LumeOS-Claude-V1` war erreichbar; alle Struktur-
aussagen wurden read-only direkt verifiziert (pg_extension, pg_proc,
pg_trigger, pg_indexes, pg_policies, ACLs, Zeilenzahlen).

Geliefert:

- **6 Draft-Migrationen** in `supabase/migrations-draft/` (neu angelegt),
  die den verifizierten Container-Ist-Zustand reproduzieren, je mit
  kommentiertem DOWN. Die 4 verwaisten Tabellen haben eigene Dateien
  (000300 Preferences inkl. der 2 RLS-Policies, 000400 Curation).
  RLS wurde als Container-Zustand übernommen (Vorgabe), nicht bewertet.
- **Bericht** `docs/ssot/31-migrations-rueckbau.md`: zeilenweiser Abgleich
  Dump ↔ alle 7 Bestandsdateien (tabellarisch), rekonstruierte
  Entstehungskette, Anwendungsreihenfolge, offene Fragen O-1 bis O-8.

Neue Befunde über den Auftrag hinaus (alle `[cmd]`):

1. `20240522_001/_002` liefen **nie**: pg_trgm fehlt, keine Grants/ACL,
   keine Makro-Spalten auf `foods`, keine `auto_tag_food()`-Funktion,
   kein Trigger, keine der 7 SELECT-Policies.
2. `20260424_002` (Classifier-Felder) ist angewendet, steht aber — wie die
   Slices — **nicht** im Migrations-Register. Auch `public` ist also teilweise
   an der Pipeline vorbei entstanden.
3. Herkunft der bisher unerklärten `foods`-Zusatzspalten und des Index
   `idx_food_aliases_food` gefunden:
   `docs/project/p1-005/P1-005-local-food-human-layer.sql`.
4. Thai-Spalten (`name_th`/`group_th`) sind strukturell da, aber in 0 von
   138 `nutrient_defs` gefüllt.
5. Duplikat-Index auf `foods(sort_weight DESC)` (2 identische Indizes).
6. Die Altdateien `20240522_001` + `_002` teilen sich den CLI-Versionspräfix
   `20240522` — Namensschema-Verstoss.

**Nicht getan (verboten):** `supabase db reset`-Testlauf der Drafts —
die Drafts sind gegen Dump + Live-Introspektion abgeglichen, aber ungetestet.

## Job 2 — Spec-Audit: **erledigt im beauftragten Umfang**

Geliefert: `docs/ssot/70-spec-audit/` mit `00-INDEX.md` + 13 Modulakten
(Admin, BuddyandAICoach, Core, Dashboard, Goals, HumanCoach, Marketplace,
Medical, Nutrition, Recovery, Supplements, Training, WebPlatform).
Vorbefunde aus `40-spec-code-matrix.md` wurden neu erhoben, nicht übernommen.

Kernbefunde (Auswahl, alle in den Akten belegt):

- **Medical:** 10 deklarierte Entities, aber nur 8 `CREATE TABLE` —
  `UserMedicalInsight` und `UserHealthReport` haben kein Schema; der eigene
  INDEX behauptet fälschlich „10 Tabellen". Härtester interner Bruch.
- **Buddy-INDEX:** 9 tote Übersichtsdateien + falsches `spec/`-Präfix auf
  allen 10 Spec-Links; SPEC_11 unindiziert.
- **Neuer Portfehler:** Nutrition-Workorder-Plan ruft `supplements:5200`
  (2 Stellen) — 5200 ist Training; sonst ist das 5x00-Schema konsistent.
  Admin-4100 als bekannter Ausreisser bestätigt.
- `packages/scoring`: 28+ Fundstellen in 10 Modulen, kein Gerüst;
  `apps/app` in 14 Dateien referenziert, existiert nicht; `apps/marketplace`
  als Bestand deklariert, fehlt; `apps/mobile`/`apps/staff` haben Gerüste,
  aber (fast) keine Spec-Erwähnung.
- 117-vs-138-Nährstoffe: Quelle der falschen App-Shell-Zahl (C-10) ist die
  WebPlatform-Spec selbst.
- Zählprüfungen Entities↔Tabellen für alle Standardmodule; ausser Medical
  überall konsistent.

**Bewusst nicht geleistet** (als `[annahme]`/Folgearbeit markiert):
Feld-für-Feld-Abgleich Entities↔DB↔API je Modul; Einzel-Review der 13
Nutrition-Patches und 12 ADRs (Nutrition wie beauftragt zuletzt und begrenzt).

## Job 3 — Encoding-Schäden: **vollständig erledigt**

Geliefert: `docs/ssot/32-encoding-schaeden.md`. Nichts repariert.

- **Genau 1 Datei mit echtem Schaden:**
  `supabase/migrations/20240522_002…sql` Z. 396 (`k?se` → `käse`) und
  Z. 399 (`n?sse` → `nüsse`), per Hex-Dump als literales `?` (0x3F) belegt.
  Zusatzbrisanz: in der Regex macht `k?se` das „k" optional — die
  lactose_free-Heuristik wäre grob falsch, wenn die (im Container nicht
  vorhandene) Funktion je liefe.
- **Der bekannte Fall `packages/shared/src/supabase/client.ts` ist heute
  sauber** (vollständig gelesen, `file` bestätigt UTF-8). Vermutlich war der
  B-09-Ursprungsbefund ein Konsolen-Anzeigefehler `[annahme]`.
- Alle übrigen Treffer der 4 Suchdurchgänge: Erkennungs-Regexe,
  Box-Drawing-Diagramme, URL-Query-Strings — kein Schaden.
- Nebenbefund: Live-DB-Daten sind sauber (0 `?`-Treffer in foods/aliases/defs).

---

## Angelegte Dateien (23 neu, 0 geändert)

```
supabase/migrations-draft/20260802000100_nutrition_schema_foundation.sql
supabase/migrations-draft/20260802000200_nutrition_eav_core_tables.sql
supabase/migrations-draft/20260802000300_nutrition_food_preferences.sql
supabase/migrations-draft/20260802000400_nutrition_food_curation.sql
supabase/migrations-draft/20260802000500_control_plane_tables.sql
supabase/migrations-draft/20260802000600_wo_classifier_fields.sql
docs/ssot/31-migrations-rueckbau.md
docs/ssot/32-encoding-schaeden.md
docs/ssot/70-spec-audit/                     (14 Dateien: 00-INDEX + 13 Akten)
docs/sessions/2026-08-02-autonom.md          (dieser Bericht)
```

Summe: 6 + 2 + 14 + 1 = 23.
Nicht angefasst: `docs/todo/TODO.md`, `supabase/migrations/`, `CLAUDE.md`,
`AGENTS.md`, jeder Code. Keine Commits, keine Schreibvorgänge gegen die DB.
Hinweis: `git status` zeigt `AGENTS.md`/`CLAUDE.md` als modified — das war
schon bei Sitzungsbeginn so (ijfw-Memory-Block, vgl. B-10), nicht aus
dieser Sitzung.

---

## Offene Fragen für Tom (gesammelt)

Aus Job 1 (Detail in `31-migrations-rueckbau.md`):

1. **O-1** pg_trgm wieder einführen oder Suche ohne Trigram lassen?
2. **O-2** Grants für authenticated/service_role definieren? (Derzeit: keine.)
3. **O-3** RLS-Zielbild: 2 `auth.uid()`-Policies + 9 offene Tabellen (Ist)
   vs. SELECT-RLS auf allen EAV-Tabellen (Altdatei)? Hängt an ADR-002/003, D-08, D-14.
4. **O-4** Duplikat-Index auf `foods(sort_weight)` behalten oder droppen?
5. **O-5** Seed-Strategie: Kataloge als Migration, `seed.sql` oder nur Dump?
6. **O-6** Schicksal der 7 Altdateien in `supabase/migrations/` bei Übernahme
   der Drafts (Archiv? Register-Neuaufbau via `db reset`?).
7. **O-7** Control-Plane-Tabellen reproduzieren oder mit Governance-Altlast fallen lassen?
8. **O-8** Makro-Spalten + Auto-Tagging aus `20240522_002`: wiederbeleben oder tot?

Aus Job 2 (Detail in `70-spec-audit/00-INDEX.md`):

9. Modulkanon festlegen: 10, 11 oder 13 Module?
10. Next.js 14 vs. 15 — eine Zahl, repo-weit.
11. Medical: 2 fehlende Tabellen nachziehen oder 2 Entities streichen?
12. 7 tote CONSOLIDATED_KNOWLEDGE-Verweise: Dateien rekonstruieren oder Links streichen?
13. Admin: welches der 3 Alt-Dokumente gilt; Port 4100 vs. 5x00?
14. Dashboard: in WebPlatform/SPEC_03 aufgehen lassen oder eigenes Modul?
15. Nutrition-Statuskopf-Muster (Gate, ADRs, Reviews) auf alle Module übertragen?

Aus Job 3:

16. `k?se`/`n?sse` in `20240522_002` fixen — nur sinnvoll, wenn die Datei
    O-6 überlebt.

## Auswirkungen auf TODO-Punkte (nur benannt, TODO.md unverändert)

- **D-12:** Entwurfsstand erreicht — Drafts + Bericht liegen vor; es fehlt
  Toms Entscheidung O-1…O-8 und der Reset-Test. Status könnte auf `[~]`.
- **D-13:** verschärft — nicht nur die Slices, auch `20260424_002` lief am
  Register vorbei; `20240522_*` dagegen nachweislich nie.
- **D-14:** Faktenbasis erweitert (keine Grants, keine ACL — RLS ist nicht
  die einzige fehlende Schutzschicht).
- **D-05:** erste vollständige Audit-Runde liegt vor (`70-spec-audit/`);
  Feldabgleich als Folgearbeit offen. Status könnte auf `[~]`.
- **B-09:** von „suchen" auf „2 Stellen entscheiden/fixen" verengt;
  `client.ts`-Verdacht entkräftet.
- **C-10:** Ursache gefunden — die 117/10.840-Zahlen stammen aus der
  WebPlatform-Spec, nicht nur aus dem Shell-Code.
- **D-08:** neues Argument — ohne Grants kann supabase-js mit
  `authenticated` derzeit gar nicht auf `nutrition` zugreifen.
- **E-05/E-04 (Sektion E):** Training-Spec behauptet Cloudflare R2 als
  Medienort; der E-Befund sagt Supabase Storage — bei der
  Legacy-Entscheidung mit klären.
