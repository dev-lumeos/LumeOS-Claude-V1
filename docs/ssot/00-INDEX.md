# LumeOS — SSOT Index

**Stand:** 2026-08-01
**Gilt für:** `D:\GitHub\LumeOS-Claude-V1`

---

## Für neue Sessions: lies das zuerst

Dieser Ordner ist die einzige verbindliche Beschreibung des Ist-Zustands.
Wenn eine andere Datei im Repo etwas anderes behauptet, gilt diese hier —
ausser der Code selbst widerspricht.

**Rangfolge bei Widerspruch:**

1. Code (verifiziert per Befehl)
2. `docs/ssot/`
3. `docs/specs/` (beschreibt das Ziel, nicht den Ist-Zustand)
4. alles andere

---

## Was LumeOS ist

Ein Health & Performance Operating System. Kernprodukt ist **Buddy**,
ein AI-Companion; die Fachmodule liefern das Wissen, aus dem Buddy schöpft.

Vollständige Produktvision: `docs/specs/00_MASTER_VISION.md`.
Diese SSOT beschreibt **nicht** das Produkt, sondern was davon gebaut ist.

---

## Herkunftsmarker — Pflicht bei jeder Aussage

Jede Tatsachenbehauptung in diesem Ordner trägt einen Marker.
Ohne Marker ist eine Aussage ungültig.

| Marker | Bedeutung | Beispiel |
|---|---|---|
| `[cmd]` | Durch ausgeführten Befehl belegt, mit Datum | `[cmd]` pnpm typecheck 2026-08-01, 17/17 grün |
| `[read]` | Datei gelesen, Inhalt zitierbar | `[read]` apps/web/package.json |
| `[annahme]` | Ungeprüft, Vermutung, Übernahme aus älterem Dokument | `[annahme]` Curation-Tabellen stammen aus manuellem SQL |

**Regel:** Nur `[cmd]`-Aussagen dürfen in `CLAUDE.md` oder in Agent-Anweisungen
als Regel übernommen werden. `[annahme]` gehört auf die Todo-Liste, nicht in eine Regel.

**Warum diese Regel existiert:** Am 2026-07-30 lief eine Bestandsaufnahme ohne
funktionierende Shell. Sie schloss daraus, `services/` und `packages/` seien leer.
Der Satz wanderte ungeprüft in die Projektanweisung und galt dort als Regel.
Tatsächlich enthalten beide Ordner 17 kompilierende Packages.
Ein `[annahme]`-Marker hätte das verhindert.

---

## Inhalt dieses Ordners

| Datei | Beantwortet |
|---|---|
| `00-INDEX.md` | Diese Datei. Einstieg, Regeln, Rangfolge |
| `10-workspace.md` | Was liegt im Monorepo? Was lebt, was ist Gerüst, was ist Altlast? |
| `11-zielarchitektur.md` | Wie soll es am Ende aussehen? Welche Apps, welche Services? |
| `20-apps-web-ist.md` | Was kann `apps/web` heute wirklich? |
| `30-datenbank.md` | Welches Schema ist live? Welche Entwürfe liegen daneben? |
| `35-naehrwert-bezugsgroesse.md` | Worauf beziehen sich die Nährwerte, und wie rechnet man auf eine Menge um? (Grundlage jeder Diary-Rechnung) |
| `36-testbasis.md` | Was wird womit geprüft — und was ausdrücklich **nicht**? (Gate, Rechteprüfung, E2E-Entscheidung) |
| `37-testkonten.md` | Testkonten der lokalen Instanz — wer darf, wer nicht, und wozu die zweite Session gebraucht wird |
| `40-spec-code-matrix.md` | Je Modul: Spec vorhanden? Code vorhanden? Delta? |
| `41-lebensmittelsuche-wortschatz.md` | Wie weit der BLS-Wortschatz von dem entfernt ist, was Menschen tippen |
| `42-dekompositum-messung.md` | Das deutsche Zerlegewörterbuch am BLS-Bestand gemessen |
| `43-wortschatz-luecke.md` | Die volle Lücke gegen OpenThesaurus, in vier Klassen |
| `44-bls-codestruktur.md` | Was der BLS-Code über Warengruppe und Zubereitung bereits sagt |
| `45-bls-dokumentation.md` | Was die amtliche Dokumentation hergibt — und was nicht |
| `46-bls-importluecke.md` | 30 Fettsäuren fehlten; Bestand gegen die Quelle geprüft |
| `48-artengruppierung-messung.md` | Erster Versuch, Arten aus dem BLS-Code abzuleiten — **gemessen gefallen**, 39 von 100 |
| `49-zubereitungsschluessel.md` | Die Reparatur dazu — **ebenfalls gefallen**, 47 von 100; der Schlüssel je Warengruppe, 204 Zellen |
| `51-sortweight-formel.md` | Wie `sort_weight` gerechnet wird: Spec-Formel, zwei Korrekturen, Umsetzung in die Kette |
| `52-anzeigenamen-batch.md` | Wie die 5.775 Anzeigenamen entstanden — und wie 704 Umlaute verloren gingen |
| `53-kettenluecke.md` | Warum fünf Kettenschritte nie liefen, und die Prüfung, die es künftig meldet |
| `54-nutrition-schema-abgleich.md` | Nutrition-Schema: was `SPEC_06` fordert, was gebaut ist, wo es auseinandergeht |
| `31-migrations-rueckbau.md` | Warum migrations/ nur drei Schema-Slices führt und die Kette die Wahrheit ist |
| `32-encoding-schaeden.md` | **Vier Encoding-Varianten** — umschriebene Umlaute, 704 Fragezeichen, doppelte Kodierung, UTF-16. Und warum es dreimal wiederkam |
| `33-pipeline-verifikation.md` | Verifikationslauf der Aufbaukette |
| `34-design-bestandsaufnahme.md` | Design — was vor Theme V1 vorhanden war |
| `38-cookie-bereich.md` | Cookie-Bereich über Apps hinweg, getrennte Sitzungen |
| `39-rueckleitadressen.md` | Rückleitadressen und site_url |
| `55-anzeigenamen-einspielen.md` | Wie die 7.140 Anzeigenamen in die Datenbank kamen — Kettenschritte 025 und 026 |
| `56-suchabdeckung.md` | 152 Lebensmittel mit Sollwerten. **Die Gegenprobe zeigt: C-38 hat die breite Abdeckung nicht verbessert** |
| `57-reis-kuration.md` | Reissorten als Suchbegriffe, griechischer Joghurt nach Recherche |
| `58-phonetische-varianten.md` | c/k, f/ph — gemessen, vorbereitet, **nicht gebaut**; wartet auf echte Fehlsuchen |
| `59-suchprotokoll.md` | nutrition.search_events — jede Anfrage, ohne Nutzerkennung |
| `60-nutrition-specs-auswertung.md` | Vier Specs gegen den Ist-Zustand. **docs/specs/Goals/ meldet vollständige Umsetzung für ein Modul, das hier nicht existiert** |
| `61-referenzwerte.md` | 165 Referenzwerte über alle 138 Codes. Erst abgebrochen, dann aus EFSA und US DRI gebaut |
| `62-lebensmittel-tags.md` | NOVA statt processed_food, Allergene umgekehrt markiert — **enthält Nüsse ist belegbar, enthält keine nicht** |
| `63-suchlaufzeit.md` | 494 auf 234 ms. **Der Index, um den es zwei Tage ging, existierte gar nicht** |
| `64-training-wortschatz.md` | Training hat dasselbe Wortschatzproblem — **3 von 34 Sollwerten**, diesmal vor dem Bau gemessen |
| `65-tags-und-aliase-einspielen.md` | 17.967 Tags und die Reis-Aliase live |
| `66-eigene-lebensmittel-entwurf.md` | Schema-Entwurf für foods_custom |
| `67-fettsaeuren-verlust.md` | **171.409 Nährwerte einen Tag lang verloren** — und die Prüfung schrieb den Schaden als Sollwert fest |
| `68-theme-faehigkeit.md` | 538 harte Farbwerte in 7 Dateien. **Keine davon benutzt einen Token** — zwei Welten, kein Mischzustand |
| `69-kette-ausfuehrbar.md` | **Es gab keine Kettensteuerung** — nur eine Prosa-Tabelle. Jetzt kette.json und ein Ausführer |
| `70-tagesbilanz-mikros.md` | 24 Mikros mit Fehlzählern. Flach statt JSONB, weil **23 von 24 Codes über 94 % belegt** sind |
| `71-eigene-lebensmittel.md` | foods_custom gebaut, Plausibilitätsprüfung meldet statt zu blockieren |
| `72-profil-und-bewertung.md` | Profil und daily_reference_assessment — **dieselbe Mahlzeit, 72,7 % gegen 50,0 % je nach Geschlecht** |
| `73-parallelstruktur.md` | /v2 neben der alten Oberfläche. **Der Bundle-Hash blieb gleich, als v2.css sich änderte** |
| `74-shell.md` | Sidebar, Topbar, Kontextspalte. **Der Entwurf hat null @media-Regeln** |
| `75-nutrition-oberflaeche.md` | Suche und Tagebuch. **Keine gefüllten Ringe ohne Ziel** — ein zu 68 % gefüllter Ring wäre eine Falschaussage |
| `77-profilpflege.md` | /v2/settings — die sechs Felder, ohne die keine Formel rechnet |
| `78-zielwerte.md` | TDEE und Makros. **Der Vorgänger hat GO-02 viermal verschieden beantwortet** |
| `79-portionen.md` | 23.402 Portionen über 7.048 Lebensmittel — **kategoriebasiert übernommen**, nicht kuriert |
| `80-vorgaengerrepo-fundus.md` | **Wegweiser durch referenz/lumeos-2026/** — 75 Migrationen, 52 Seed-Dateien, Formeln nach Thema. Vor jeder Einstufung als fehlend hier nachsehen |
| `81-tagebuch-schreiben.md` | Der Schreibpfad. **Einfrieren belegt: Lebensmittel auf 999 kcal geändert, Position bleibt bei 348** |
| `82-testdaten.md` | 3 Nutzer, 512 Mahlzeiten, 43 Tage — und **11 Szenariotage mit Register** |
| `83-dashboard.md` | Dashboard nach der Vorlage, 12 Kacheln mit Attrappenmarken |
| `84-hydration.md` | Wasserziel und Tagesdaten. **Getrunkenes und Wasser aus Lebensmitteln getrennt** |
| `85-mikro-uebersicht.md` | Netzdiagramm und Schwellenliste — **16 von 17 am Mangel-Szenariotag** |
| `91-training-mockup.md` | Training nach der Vorlage, 38 Kacheln, **alles Attrappe**. Klärt, wie die vier Vorlagendateien eines Moduls zusammenhängen — und welche Kachel zuerst echte Daten bekommen kann |
| `92-recovery-mockup.md` | Recovery nach der Vorlage, 36 Kacheln, **alles Attrappe** (kein Schema). **Zwei konkurrierende Rahmen — `app.jsx:122` entscheidet, welcher gilt.** Damit drei Muster für Vorlagendateien, und die Prüfung, die sie unterscheidet |
| `94-goals-mockup.md` | Goals nach der Vorlage, 39 Kacheln, alle markiert — **obwohl fünf davon auf `goals.zielwerte_am` passen und sofort echt werden könnten.** Dreistufige Vorlagenkette; `Math.random()` der Vorlage ersetzt, weil es die Hydration bricht |
| `95-supplements-mockup.md` | Supplements nach der Vorlage — der Rahmen und seine **sieben eigenen Tabs**, vier weitere als benannte Luecke. **227 KB Vorlage, viertes Muster: arbeitsteilig statt konkurrierend.** Mit dem, was im Vorgaengerrepo danebenliegt (acht Tabellen, 36 KB Seed, Halbwertszeit) |
| `99-medical-mockup.md` | Medical nach der Vorlage, 21 Kacheln, **alles Attrappe** (kein Schema). **Erste gezählte Vollständigkeit: 56 von 56**, mit wiederholbarem Skript. Fünftes Muster: der Rahmen ruft seine Zulieferer als **blosse Globale** — ein `window.`-Grep findet sie nicht. Plus 457 Laborwert-Synonyme im Vorgängerrepo |
| `102-coach-mockup.md` | Coach nach der Vorlage — **zwei Unterbereiche unter einem Menuepunkt.** `Human Coaches` gebaut und **gezaehlt vollstaendig (60 von 60)**, `AI Coach` offen (41 Posten) und in der Oberflaeche namentlich benannt. **Der Auftrag nannte 11 und 12 Tabs; gezaehlt sind es 10 und 20.** `-gaps.jsx` heisst „gap-closers" und gehoert zu zehn Elfteln dem Portal. Im Vorgaengerrepo liegt **ein fertiges Berechtigungsmodell** — aber mit zwei Stufen statt drei, **ohne Widerrufshistorie**, und mit einem `edit_auto_accept`, das dem Vorschlagsmodell der Vorlage widerspricht |
| `106-ai-coach-mockup.md` | AI Coach (Buddy) nach der Vorlage — **20 Tabs, 42 von 42**, damit sind beide Unterbereiche des Menuepunkts fertig. **Die vermuteten „elf fehlenden Zulieferer" fehlen nicht** — alle fuenfzehn liegen vor, einer davon in einer *Coach*-Datei; die Falle war die `Object.assign`-Zeile, die **nur Daten fuehrt, keine Komponenten**. `uploads/` ist byteweise identisch mit `docs/specs/` — **der Hash unterscheidet Module, nicht Fassungen**. Im Vorgaengerrepo liegen 6 Migrationen und `buddy.ts` mit 1.647 Zeilen, aber **BSS und Voice gibt es dort nicht** — die zwei Tabs sind neu zu bauen, nicht zu uebersetzen |
| `109-medical-anbindung.md` | **Medical zeigt eine Liste statt drei** (G-60) — die Form der Attrappe traegt echte Daten: 37 Marker aus 140 Werten und 5 Befunden, Panelfilter, Verlauf (Glucose 88→102, +15,9 %), Bereichsbalken, Popup. Die zwei Listen aus G-46 sind weg, 20 Marken bleiben. **Die elf Panels der Attrappe gibt es in keiner Quelle** — `loinc_class` gibt vier Gruppen, `panel_type` ist bei 11.421 von 11.676 leer, `panels` haengt Kreatinin in 33 Elternpanels, und das Vorgaengerrepo fuehrt `category` als Probenmaterial. Gebaut ist, was da ist; die Zuordnung ist Toms Entscheidung. **Der Bereichsbestand ist duenner als seine Zeilenzahl:** von 464 Zeilen tragen 54 Zahlen — und genau die schliesst die Lesefunktion aus. Teil I (G-46) haelt Lesepfad, Bereichsvorrang und den Befund fest, dass `tom.seed@example.com` kein Passwort hat |
| `113-ui-reste.md` | **Zwölf gemeldete Punkte abgearbeitet** (G-56) — `shield`/`history`/`file`, `Pill dot`, `Empty`, `v2-g-cols-5`, `InEntwicklungKnopf disabled`, JetBrains Mono. **Die 375-px-Ursache war eine andere als gemeldet:** die Seitenleiste rollt längst, breit war die Kopfzeile (606 px, `overflow: visible`). **Und die früheren 375-px-Messungen (G-40/G-42/G-46) waren nicht belastbar gewonnen** — `goto` setzt das Fenster zurück, eine abgelaufene Sitzung misst die Anmeldemaske. `btn-accent` (G-10) trifft nicht mehr zu, gemessen |
| `101-vollstaendigkeit.md` | **Alle sechs Module gegen die Vorlage gezählt** (G-38), mit verallgemeinertem Skript und belegter Umbenennungstabelle. Fünf von sechs vollständig; offen bleibt Supplements (G-31 — **plus eine neu gefundene Lücke in `SuppCost`**). Sechstes Muster: eine „feste" Formel kann engine-abhängig sein — `Math.sin` brach die Hydration trotz grünem Test. **Enthält eine Korrektur:** die Recovery-Zählung 67/71 war falsch |
| `104-muskelkarte.md` | **Die anatomische Muskelkarte** aus dem Mockup in `packages/ui` (G-26) — 17 Muskelgruppen statt 18 Blobs, drei Module können sie nutzen. 19 Hex-Werte auf Tokens gelegt, **ohne neue zu erfinden**; die vierte Aktivierungsstufe per `color-mix`. Siebtes Muster: **eine fehlende Zuordnung sieht aus wie ein fehlendes Bauteil** — `upper-back` mit Bindestrich wäre beinahe dauerhaft grau geblieben. **Nachtrag G-44:** alle 39 IDs eingeordnet (17 Gruppen · 16 Injektionsorte · 6 Nicht-Muskeln), Test macht eine unzugeordnete zum Fehler. Zwei gemeldete Gestaltungsfragen: **die grauen Teile heben sich um 0,012 Helligkeit ab** — praktisch unsichtbar — und **der Umriss der Vorlage ist kaputt** (118 von 200 Pfadbefehlen fehlerhaft, Kontur endet auf halber Höhe) **Nachtrag G-55:** die drei Ebenen stehen — alle **96 Muskelgruppen aus C-73** fallen auf 17 Flaechen, **gemittelt statt maximiert**, und der Klick trennt wieder auf (Flaeche -> Gruppe -> Muskeln). Sichtbarkeit im Dunkelmodus geloest: `--surface-2` lag 0,030 ueber dem Kartengrund, `--border-strong` liegt 0,155 — kein neuer Token. Achtes Muster: **der Umriss der Vorlage ist toter Code** (`outlinePath` wird nie angehaengt), die volle Figur kommt allein aus `MUSCLES`. Die Check-ins sind angebunden, greifen aber erst, wenn `recovery` in `supabase/config.toml:16` freigegeben ist |
| `47-lebensmittelsuche-stand.md` | **Der erreichte Stand** — fasst 41 bis 46 zusammen |
| `60-legacy-cloud.md` | Was liegt wirklich in der Legacy-Cloud-Instanz? (echte Zeilenzahlen, Medienverweise, Schemakonflikt — ersetzt alle Schätzungen) |
| `50-governance-rest.md` | Was ist vom alten Governance-System übrig und warum? |
| `86-erfassung.md` | Erfassung im Tagebuch — Suche in der Mahlzeitenkarte, Portionswahl, `Same as yesterday`. **Ringe springen ohne Neuladen.** |
| `87-preferences.md` | Lebensmittel-Vorlieben — `food_preferences_read/write`, Zeilenschutz beidseitig. **`hard` ist kein Sicherheitsversprechen** (C-62). |
| `88-i18n.md` | Mehrsprachigkeit mit next-intl, **ohne Sprachpraefix**. 95 Schluessel de/en, Thai vorgesehen. Gate-Pruefung erzwingt Vollstaendigkeit. |
| `89-mahlzeitzeit.md` | `meal_time` in `meals`, UNIQUE-Index entfernt — **mehrere Mahlzeiten je Typ moeglich.** 686 bestehende nachgetragen. |
| `90-datum-und-mahlzeiten.md` | Datumsnavigation `‹ Heute ›` und eine Karte je Mahlzeit. **Drei Stellen rechneten ueber Mitternacht** und waeren an Zeitumstellungstagen gekippt. |
| `93-trainingssitzungen.md` | `workout_sessions`, `workout_exercises`, `workout_sets` — **9 Sitzungen, 60 Saetze live.** Der Uebungsname wird eingefroren. |
| `96-recovery-checkins.md` | `recovery.checkins` — **36 Eintraege, davon 27 ohne HRV.** Der `manual`-Modus ist der Normalfall, nicht die Ausnahme. |
| `97-ziele-und-phasen.md` | `user_goals` und `goal_phases`. **Vier Zielvokabulare aufeinander abgebildet** — als Datendatei, ohne offene Begriffe still zu fuellen. |
| `98-supplements-schema.md` | Fuenf Tabellen, eine Sicht — **44 Katalogeintraege live.** Keine Wechselwirkungsbewertung: das waere eine medizinische Aussage. |
| `100-koerpermessungen.md` | `body_measurements` und `body_circumferences`, **43 Messungen.** Dazu Koerperfett nach Navy **mit Spanne** — 10,31 % (6,81–13,81). |
| `103-biomarker-katalog.md` | **11.676 LOINC-Codes** in acht Dateien, mit Einheit, Klasse und deutschem Namen. Normbereiche liefert LOINC nicht — sie docken spaeter an. |
| `105-medical-schema.md` | `biomarker_catalog`, `lab_reports`, `lab_result_values`. **Der Referenzbereich sitzt am Befund**, der Katalogbereich ist der Rueckfall. |
| `107-laborimport.md` | Zuordnung von Markernamen auf LOINC — **292 Aliase.** Drei Faelle belegt: eindeutig, mehrdeutig, **unbekannt mit Rohtext.** |
| `108-muskelgruppen.md` | Kuration der Muskelgruppen: **107 → 96**, 89 Eltern-Beziehungen — **`exercise_muscles` unveraendert bei 6.624.** |
| `110-datenherkunft.md` | Herkunftsspalten in **sieben Messtabellen**, bevor die Geraete kommen. `meal_items` trennt `food_source` von `measurement_source`. |
| `111-meilensteine-und-tdee.md` | Meilensteine und **adaptive TDEE** — 3.143,2 gegen Formel 3.527,0. **`alpha 0.3` macht den Wert zu 70 % zur Formel** (GO-15). |
| `112-dev-konto.md` | Der volle Demo-Bestand auf `dev@lumeos.app` — **43 Messungen, 36 Check-ins, 9 Sitzungen, 2 Befunde.** `tom.seed` kopiert, nicht verschoben; `test-user` bleibt Pruefkonto. |
| `114-supplements-angebunden.md` | Vier Tabs lesen echt, **14 Kacheln verlieren die Marke** — `Cost` entgegen der Auftragsannahme dabei, weil alle 44 Katalogeintraege Preise fuehren. **`Compliance` braucht 30 Tage und einen Auslasser**, es liegt einer. `refillUrgent` sollte eine Schwelle sein, keine Handmarkierung. |
| `115-training-exercises-tab.md` | Der Exercises-Tab liest echt — **1.416 Uebungen, 8 von 8 Filtern gegen SQL geprueft**, Suche 142–172 ms. `Type` faellt weg (keine Quelle), `Discipline` tritt an seine Stelle. **Die Rollenfrage entschieden:** primaer als Vorgabe, denn bei Hamstrings stehen 100 gegen 384. Zuerst zwei Sperren dokumentiert (PGRST106, RLS ohne Policy) — **die Kette fuehrte die Policy, die Datenbank hatte sie verloren.** Zwei stille Fehler gefunden: PostgREST deckelt bei 1.000 Zeilen, `.in()` kippt ueber 200 IDs **ohne Fehlermeldung**. |
| `116-food-db-tab.md` | Der Food-DB-Tab bekommt die Form des Entwurfs — **7.140 Lebensmittel, 9 von 9 Pillen gegen SQL geprueft**, Suche 199–304 ms. **Drei Pillen des Entwurfs tragen nicht:** `Favorites` (C-94), `Recent` (Verlauf, keine Kategorie), `Supplements` (der BLS fuehrt keine). **2.237 ohne Kategorie bleiben ueber Suche und `All` erreichbar** — nichts erfunden; `food_groups` hat keinen Fremdschluessel zu `food_categories`, nur Prosa im `bls_hint`. `huhn` zeigt 136 statt 31: **die Synonymaufloesung, kein Fehler.** |
| `117-ausschluss-presets.md` | **11 Ausschluss-Presets, 33 Regeln, 3.767 Zuordnungen** — „Keine Innereien" trifft 131 mit allen fuenf Lebern. `halal`/`kosher` hatten seit C-98 null Zuordnungen, jetzt 6.379/6.451 — **aber das Tag sagt „enthaelt keine verbotene Zutat", nicht „ist halal":** die Schlachtung steht nicht in den Daten, der Vorbehalt haengt als `caveat_de` am Preset. Das Schwein-Muster brauchte drei gemessene Gegenmuster (32 Fehltreffer → 0). **231 Wurstwaren ohne Tierart im Namen bleiben unerfassbar** — gemeldet statt geraten. Laktose gehoert bewusst zu `intolerances[]`, nicht hierher. |
| `120-daumen-bewertung.md` | Daumen hoch/runter in Trefferliste **und** Detailansicht, Zyklus neutral→like→dislike→neutral. **Abfrage nur abwaerts**, mit dem Namen des Lebensmittels; danach verschwindet die Zeile. Schreibt `source='search_thumb'` und eine Stufe **unter** dem Assistenten (`like`/`soft_dislike` statt `boost`/`hard_exclude`) — **der Aufwand sagt etwas ueber die Absicht.** Bei Widerspruch gewinnt das Spaetere, erzwungen durch `uq_food_pref_items_user_food`. **Befund an der Naht zu G-65:** dessen RPC loescht alle Items der Nutzerin und schreibt `source` fest auf `settings` — Daumen-Bewertungen verlieren dabei ihre Herkunft. |
| `116-goals-anbindung.md` | **Fuenf von zehn Tabs lesen echt** (GO-16), 38 Marken werden 23 — vier Tabs zeigen keine mehr. **Drei Zahlen des Auftrags gelten an einem anderen Tag:** KFA 10,31 % und FFMI 21,97 stammen vom 2026-09-13, weil **26 der 43 Messungen in der Zukunft liegen**; heute sind es 11,38 % und 21,49. **Der adaptive TDEE ist leer und bleibt es**, bis 14 von 14 Zufuhrtagen vollstaendig sind (es sind 13) — die Kachel zeigt die Bedingung statt einer Ersatzzahl, daneben die echte Formel 3.527 kcal. `alpha 0.3` steht jetzt ausgeschrieben da (GO-15 offen). Das feste „Heute" der Vorlage ist raus, `lib/datum.ts` rechnet. **16 Eingabefelder bleiben markiert stehen** — Goals erfasst nicht. |
| `117-zusammenhaengende-seeds.md` | **180 Tage korrespondierende Seeds** (C-78) — zwei 90-Tage-Fenster, 2.157 Mahlzeiten, 180 Messungen, 170 Check-ins, 30 Trainingssitzungen. `dev@lumeos.app` bekommt denselben Bestand wie `tom.seed`; `test-user` bleibt klein. Adaptive TDEE ist `complete`, die Bilanz geht auf: 0,170 kg Gewicht plus entspricht 100,7 kcal/Tag. |
| `118-training-sitzungen.md` | **Fuenf Tabs lesen echte Sitzungen** (G-69), 40 Marken werden **24** — History, Progression, Standards, Calendar und die Serie. **Die zwei Sperren aus G-64 sind weg** (`training` in `config.toml`, Policy auf `exercises` wieder da). **`workout_sessions.status` trennt geplant und absolviert NICHT:** alle 30 stehen auf `completed`, auch die 15 nach heute — massgeblich ist das Datum, der Status kommt zum Vergleich mit. Volumen, Serie und e1RM zaehlen nur Absolviertes; Kalender zeigt alle, mit Pille und Strichform statt blasserer Farbe. **Volume landmarks bleibt Attrappe** — MEV/MAV/MRV stehen nur in Vorlagendateien, und dieselbe Regel streicht das Zielband der Volumenkachel und die Einstufung `Beginner…Elite`. **Der offene Punkt aus G-64 loest sich:** Progression zeigt ohnehin nur die 6 Uebungen mit Verlauf. Ungenutzt gefuellt: `rpe` 200/200, `logged_via` 200/200, `set_type` 200/200. |
| `119-vorlieben-tab.md` | **Der Preferences-Tab liest und schreibt echt** (G-65) — alle **6 Kacheln verlieren die Marke, eine siebte kommt dazu** (generelle Ausschluesse). Die Datenseite war fertig: `food_preferences_read`/`_write` seit C-87, benutzt statt nachgebaut. **Zwei Abweichungen vom Mockup, mit Tom besprochen:** 20 Allergene in drei Stufen statt 14 an/aus (sonst waere `intolerances[]` nicht bedienbar und die Laktose-Testdaten unsichtbar), und Kategorien aus dem Katalog statt fester Achterliste. **Der gefaehrlichste Fund:** eine gesetzte Unterkategorie („Kekse & Plätzchen", Enkel von SÜSSES & SNACKS) waere unsichtbar gewesen — und weil `food_preferences_write` die Items **ersetzt**, haette die naechste Aenderung sie still geloescht. **C-93 wurde mitten im Auftrag fertig:** statt Leerzustand bietet die Kachel jetzt 11 Presets, und der Schlachtungs-Vorbehalt kommt aus `caveat_de` statt aus dem Code. **Die Rangfolge ist hinterlegt und wirkungslos** — `food_search` hat keinen Nutzerparameter (C-94). Acht Spalten sind gespeichert ohne Wirkung und ohne Kachel: Kochlevel, Zeit, Budget, Mahlzeiten, Kuechen. |
| `121-supplements-bestandsaufnahme.md` | **Bestandsaufnahme aus drei Quellen** (F-02) — das Gebaute traegt; die Luecken sind drei Datenbestaende mit je eigener Sorgfaltsstufe. **`SPEC_08` ist keine Importpipeline, sondern Seed-SQL** (18 Praeparate, „kuratiert, keine externe Quelle"); die echte Kandidatenliste ist die Mini-PC-CSV mit **702 Zeilen (nicht 1302, wie das Importskript behauptet)** — davon 200 Enhanced/PED, 0 mit Quellenangabe. Interactions: 28 CSV-Zeilen + 12 Spec-Zeilen, **6 der Spec-Zeilen sind Supplement×Medikament** und brauchen ein Medikationsmodell, das es nicht gibt; die Grenze „nennen statt bewerten" liegt fertig im Vorgaenger (`intelligence.ts`: Mengenschnitt mit dem Stack). Injections: der 17-KB-Change-Request deckt sich mit dem Mockup, drei Objekte reichen — **aber keine der 16×3 Grenzwerte (max_ml, rest_days, Nadel) ist im Repo belegt.** Compliance ist seit C-82 anbindbar (90 d, 8 skipped, 93,3 %), offen nur: tragen die skipped-Zeilen notes? Sechs Auftragsentwuerfe, sortiert nach Freigabebedarf. |
| `122-vorlieben-suche.md` | Die Suche wendet die Vorlieben an — `p_user_id` als 15. Argument. **`hard_exclude` entfernt, `soft_dislike` wertet ab.** Union der drei Presets: nur 808 von 7.140. |
| `123-rechtemodell.md` | Permissions (Nutzer setzt Sicht und Bestaetigungspflicht) und Autonomy (Coach setzt den Athletenlevel) als **zwei getrennte Achsen**, mit Widerrufshistorie. Dazu `alpha = 1,0`. |
| `124-nutrition-feinschliff.md` | Kopfzeile, Warnhinweis, Filter, Blaettern. **Die BLS-Nennung darf weg** — Kapitel 9.2/9.3 der Dokumentation: Zitierweise **empfohlen** (fuer Publikationen), Bestand „kostenfrei und ohne Lizenzbarrieren"; die `SOURCE`-Spalte bleibt. Zentrierung erst nach drei Anlaeufen (71 px daneben → abgeschnitten → Raster, 0 px): **`v2.css:1064` setzt `position: relative` auf jedes Kopf-Kind und schlaegt eine einzelne Klasse.** **7.140 sind jetzt erreichbar** (143 Seiten), 4 von 4 Filtern gegen SQL geprueft. **Drei Sperren in der Suchfunktion gemeldet statt umgangen:** nur EIN Tag (kein ODER/UND), **kein Ausschluss-Parameter** (Allergene wirken nur auf der Seite), kein `processing_level`. Laufzeit 330–492 ms — **gemessen in der RPC selbst, nicht durch diesen Umbau** (bei limit 25 und 50 gleich lang). |
| `125-supplements-inventory.md` | **Inventory, Compliance und die Rueckfallfassungen** (G-74). **Der Markenzaehler kann jetzt unterscheiden:** `attrappe={RUECKFALL}` trennt „abgeloest, aber aufgehoben" von „nie angebunden" — `tabs.tsx` zeigt **1 echte Attrappe gegen 16 Rueckfallfassungen** statt pauschal 17. Toms dritter Weg: nicht loeschen wie bei Goals, aber erkennbar machen. **Die Reichweite haengt an der Einheit** — die alte Formel meldete fuer Kreatin 30 statt 6 Tage, weil sie `stock_unit` (`g`) ignorierte; die Auftragszahl war ebenso falsch. Drei Nachfuellstufen aus der Reichweite (7/14/30 Tage), `low_stock_threshold` als Rueckfall. **Compliance 93,1 %**, nicht 100 — und **alle 32 `skipped`-Zeilen tragen `notes`, aber der Text ist ein Erzeugungsvermerk**, kein Nutzergrund: Datum gezeigt, Fehlen benannt. Kostenverlauf **4 ehrliche Monatspunkte**, angeschnittene gestrichelt. **Ein HTTP 500 im Browser gefunden, den der Typecheck nicht sah:** ein Wert-Import aus einer Datei mit `next/headers` zog das Server-I/O ins Browserbuendel. |
| `126-tdee-abweichung.md` | **Die Messung war korrekt, der Seed-Faktor nicht.** `very_active` bei 0,58 Trainings je Woche — Abstand von −1.029,6 auf **−55,0 kcal**. |
| `127-recovery-checkins.md` | Der Erholungswert rechnet echt — **Manual-Gewichte 30/15/15/10/15/10/5, ohne HRV** (127 von 170 Check-ins haben keins). Beispieltag ohne HRV: **82**, unabhaengig nachgerechnet. **Zahl ja, Urteil nein:** „Score 88 · Good" wurde zu „Score 82" — `Good` ist eine Readiness-Stufe der `SPEC_09` und damit Urteilssprache. **Fehlende Anteile zaehlen nicht mit**, weder als null noch als voll: gerechnet werden 75 von 100 Gewichtspunkten, die zwei offenen stehen mit Grund da. **Soreness-Mittelung entschieden (E2, Tom muss bestaetigen):** nur gemeldete Muskeln — das JSONB fuehrt hoechstens **fuenf**, nie 18 und nie leer. **Drei fehlerhafte Quellformeln nicht nachgebaut** (u. a. ACWR-Faktor 1.1 bei 1.4). **Modalitaeten brauchen eine eigene Tabelle** — `checkins` hat keine Spalte dafuer; `recovery` hat genau eine Tabelle. |
| `128-recovery-scores.md` | `recovery.scores` und `modality_log` mit Manual-Modus. **170 Scores, 89 Modalitaeten** je Konto. |
| `129-goals-prioritaeten.md` | Prioritaeten setzen, Ziele bearbeiten, Zeitachse. **Beide Spalten lagen vor** (`priority`, `status`) — es fehlte nur der Schreibweg. **Die eigentliche Regel steht in der Datenbank, nicht in einer Spec:** `user_goals_check1` (aktiv = 1–3) UND `uq_user_goals_active_slot` (eindeutig je Nutzer) ergeben **genau drei aktive Plaetze, jeden einmal** — der zweite Teil fiel erst beim Ruecksetzen der Testdaten auf. **Zwei stille Fehler:** ein Wert-Import aus einer `'use client'`-Datei zog `next/headers` ins Buendel (**HTTP 500 auf JEDER Seite**, Typecheck gruen — wie G-74), und PostgREST meldet `ok` bei einem `update`, das der Zeilenschutz leergefiltert hat (behoben mit `.select()`). **Die Historie ist leer, und das ist der Befund:** kein Ziel traegt einen abgeschlossenen Status, alle drei Meilensteine stehen auf `open` — zwei davon ueberfaellig. Toms Seed fehlt. TDEE-Satz berichtigt: `alpha = 1,0`, keine Glaettung. |
| `130-medical-felder-erfahrungsgrad.md` | **Vier ungenutzte Medical-Felder und der Erfahrungsgrad** (G-80). **Die Herkunftsspalte ist zurueck:** `reference_source` zeigt den Katalog-Rueckfall wieder — 1 von 37 Zeilen (Calcium, der Fall aus G-46), als Unterzeile statt eigener Spalte, weil 138 von 140 dasselbe sagten. `lab_name` und `report_time` stehen im Popup je Messung (zwei Labore, fuenf Befunde). **`fasting_status` ist auf allen 140 Zeilen `unknown`** — die Anzeige ist gebaut und zeigt heute nichts. **Er kann nie umschalten, nur anzeigen:** `biomarker_reference_ranges` hat keine Nuechtern-Spalte, `population` steht 560x auf `general`. **Die Unterscheidung steckt bereits im LOINC** — `1558-6` ist *Fasting* glucose gegen `2345-7`; ob `fasting_status` daneben gebraucht wird, ist eine Schemafrage (Code sagt die Absicht, Feld die Tatsache). **Der Erfahrungsgrad hat keine Spalte** — `public.profiles` fuehrt 14, `activity_level` ist etwas anderes (ein Anfaenger kann `very_active` sein). Kachel sichtbar, Auswahl gesperrt, ohne Attrappenmarke — das Muster aus G-65. **Nutzer-Onboarding existiert nicht**, nur der Klienten-Assistent im Coach-Modul. Erfahrungsgrad ist **Selbstauskunft**, Autonomy (C-71) **Fremdeinschaetzung** — nicht zusammenlegen. |
| `131-medikamente.md` | Medikamente: **56 Wirkstoffe, 119 Formulierungen, 124 Produkte**, dazu `user_medications` und `user_conditions`. |
| `132-recovery-anzeige.md` | **Die Score-Anzeige liest aus der Tabelle** (G-82). **Die zwei Rechnungen stimmten NICHT ueberein** — fuenf gemessene Tage, Abweichung −7,0 bis +2,6. **C-125s Referenztag stimmt (2026-06-05 = 43,0), G-76s nicht:** die Tabelle sagt fuer den 2026-11-06 **79,4, nicht 82**. Der Grund sind nicht die Gewichte (Kern identisch), sondern **wieviele Anteile zaehlen**: `score.ts` rechnet fuenf von sieben und normiert auf Basis 75, die Tabelle rechnet alle sieben und fuellt Trainingslast und Ernaehrung mit Rueckfallwerten (Basis 100). **Die Tabelle ist die bessere Zahl** — sie benutzt den gemessenen ACWR auf **118 von 170 Tagen**, die `score.ts` verwirft. Die Anzeige liest jetzt `recovery.scores` (neu: `lib/recovery/scores-read.ts`), zeigt alle sieben Anteile mit Herkunft im Klartext, Rueckfaelle blass, plus Kurve ueber 170 Tage. **`score.ts` bleibt — aber die Live-Vorschau benutzte sie nie:** `tab-checkin.tsx` rechnete mit der Entwurfsformel (eigene Gewichte, HRV aus der Attrappe, `0.88 * 10` als erfundene Ernaehrung, `readinessFor` als Urteilssprache). Jetzt rechnet sie mit `score.ts` und nennt ihre Basis; **sie ist bewusst nicht dieselbe Zahl wie die gespeicherte**, und das steht an der Kachel. `modality_log` angebunden: 89 Zeilen, vier Arten, **alle Bonus 0 mit `pending_c124_e5`** — gezeigt, nicht geaendert. **Zwei Messfehler der eigenen Arbeit korrigiert:** die Kurve zeichnete ein Dutzend Zacken, obwohl **genau ein** Tag unter 60 liegt und es **keine Datumsluecke** gibt (Streckung im 100x100-Feld); und `immediate_effect` ist **1–10, nicht 1–5** (`121_…sql:216`). Zeilenschutz gegengeprueft: `dev` 170/89/170, `test-user` 0/0/0 bei erfolgreicher Anmeldung. Kacheln mit Marke auf `Today` 4 → 3. |
| `133-substanzbruecke.md` | Aliasbruecke zwischen drei Substanzbestaenden (1.132 Zeilen) und `missing_input` fuer Regeln. **42 von 64 Regeln auswertbar** statt 1. |
| `134-medical-score.md` | **Der Medical-Kopf zaehlt jetzt, statt zu bewerten** (G-84). **`Health score 87` und `6 alerts` waren beide erfunden** — aus dem Entwurfskatalog gerechnet, ohne Bezug zu den Befunden; dass die 6 zufaellig stimmte, haette sich bei keinem neuen Befund bewegt. **Zwei Zahlen statt einer, weil sie verschiedene Autoritaet haben:** **2 ausserhalb des Laborbereichs** (Befund des Labors) und **4 nur ausserhalb des Optimalbands** (Empfehlung aus der Literatur) — zusammen die **6**, die der Filter „Non-optimal only" zeigt, mit der Gleichung als Unterzeile ausgeschrieben. **Das Wort „Alert" ist weg:** es sagt „etwas stimmt nicht", das waere ein Urteil neben einer Zeile, die „no diagnosis" schreibt. **Der `Health score` ist ersatzlos aus dem Kopf entfernt, nicht blass gestellt** — **nur 23 von 37 Markern sind gruppierbar, 47 von 140 Werten fielen heraus**. Die 14 zerfallen in **fuenf blosse LOINC-Abweichungen** (Glucose `1558-6`↔`2345-7`, LDL `13457-7`↔`2089-1`, Hematocrit `4544-3`↔`20570-8`, Vitamin D `14635-7`↔`1989-3`, Magnesium `2601-3`↔`2614-6` — bei Glucose und Vitamin D messen die Paare aber **nicht dasselbe**) und **neun echte Luecken** (ApoB, PSA, Zink, FSH, IGF-1, Prolaktin, Calcium + 2 Rohmarker ohne Code). **ApoB fehlt und LDL haengt an einer Codeabweichung** — die zwei Marker des kardiovaskulaeren Risikos. Dazu ist die **Gewichtung ungetroffen**: `.25/.25/.20/.15/.15` steht nur in `daten.ts`, und die **8 Kategorien der Datenbank passen nicht auf die 5 Systeme** des Entwurfs. **Datenbefund: Glukose steht zweimal in der Liste** — ein Rohmarker **ohne LOINC** faellt auf Namensschluesselung zurueck, also zaehlt eine Messung von 102 mg/dL doppelt. Nicht zusammengelegt, das ist eine Identitaetsentscheidung. **`Tracking`/`Insights` bleiben Attrappe:** fuer Symptome gibt es **keine Tabelle im ganzen Schema**, fuer Korrelationen und Supplement-Wirksamkeit keine Bruecke, fuer den Bevoelkerungsvergleich keine Verteilung (`population` 560× `general`). **Aber `medical.user_medications` existiert (2 Zeilen, 124 Produkte)** — die Sperrbegruendung am Knopf „Add medication" nennt eine Tabelle, die es unter anderem Namen laengst gibt; nur die Ueberwachungsspalten (`next_due`) fehlen, aus denen vier der sechs Alert-Eintraege stammen. **Messfehler der eigenen Arbeit korrigiert:** die erste SQL-Zaehlung ergab 1+5 statt 2+4, weil sie nur numerische Spalten verglich — `befund.ts` zerlegt auch **Textbereiche** (`<5.7`, `>40`), und die meisten der 560 Zeilen tragen nur Text. Die Anzeige war richtig, die Pruefung nicht; **gezeigt hat es erst die gerenderte Seite.** 393 Tests gruen (vorher 388), 20 Attrappenmarken unveraendert, `test-user` sieht 0 auch bei ausdruecklicher Abfrage der fremden UUID. |
| `136-training-restliche-tabs.md` | **Plan, Volume, Standards und was davon traegt** (G-86). **Zwei Kacheln wurden echt:** „Training readiness" aus `recovery.scores` — der Auftrag hatte recht, *das ist Recovery*, eine Lesefunktion genuegt; **alle fuenf Zeilen der Vorlage sind auf 170 Tagen belegt**, wobei `nutrition_score` auf **allen** 170 gleich 70 ist (Rueckfall `fallback_c123_e9`, blass gezeigt und benannt). Und „Diese Woche" aus `workout_sessions`. **Der Muskelname kommt aus `checkins.soreness`**, nicht aus dem hart gesetzten „chest" des Mockups. **Kein Urteil** — „Good to go" und der Ratschlag bleiben draussen (E3), wie in G-76/G-82. **`Plan` ist NICHT baubar, eindeutig:** von elf Feldern der zwei Kacheln fuehrt `workout_sessions` **keines** (Block, Woche, Phase, Last, Deload, Routine, Vorlage, Herkunft); **`name` traegt 30 Sitzungen mit 30 verschiedenen Namen, jeder genau einmal** — eine Routine ist etwas Wiederverwendbares, hier wiederholt sich nichts. Der Kalender zeigt datierte Sitzungen, `Plan` zeigt die **Struktur darueber** — das ist ein fehlendes Schema, kein Anzeigeproblem. **MEV/MAV/MRV bestaetigt fehlend:** `volume_landmarks`, `strength_standards`, `standards`, `landmarks` melden alle **PGRST205** (existieren nicht, sind nicht gesperrt); die Regel greift dreifach — Tab, Zielband, Einstufung. **HR zones:** `workout_sets` und `workout_sessions` fuehren **keine einzige** HR-Spalte, `resting_hr` und `spo2_pct` sind je **0 von 170**. **Offline sync ist eine Betriebsart, kein Datenbestand** — keine Warteschlangentabelle, und `logged_via` steht auf **101 von 101** Zeilen auf `manual`; **erst die Faehigkeit, dann die Anzeige.** **`status` traegt jetzt, was er zu G-69 nicht trug:** 15 `completed`, 14 `planned`, 1 `cancelled` statt 30x `completed` — die Kommentare in `sitzungen-read.ts` und `tab-verlauf.tsx` sind korrigiert, `absolviert` bleibt bewusst am Datum. Sichtbare Marken je Tab gezaehlt: Today **1**, History/Progression/Standards/Calendar **0**, gesamt **22**. **Die 38 `attrappe`-Marken im Text sind als Mass untauglich** (RUECKFALL-Muster seit G-74). Zeilenschutz: `test-user` sieht 0 Sitzungen, **aber 1.416 Katalogzeilen**. **Drei eigene Messfehler korrigiert**, alle drei keine Befunde: die 1.000-Zeilen-Grenze bei `exercise_muscles` (6.588), `muscle_groups` fuehrt `name` statt `name_de`, und „geplante Sitzungen haben keine Uebungen" war eine abgeschnittene ID-Liste — **alle 14 haben welche**. |
| `137-goals-phase-physique.md` | **Die letzten beiden Goals-Tabs, soweit belegt** (G-87). **Der Leseweg lag schon da:** `goals.phase_am()` waehlt die geltende Phase, `ladePhase` reicht sie seit GO-16 bis in die Ansicht — **der Tab hat die Requisite nur nie angenommen.** Phase engine zeigt jetzt `lean_bulk`/`moderate` aus `goal_phases` (14 Spalten, 5 Zeilen, davon **2 fuer `dev`**), Physique ratios die Quotienten aus `body_circumferences` (**27 Saetze fuer `dev`, alle 13 Stellen belegt**, 14 bis zum Stichtag). Gegen SQL geprueft am 2026-08-19: **Schulter:Taille 1,492 · Taille:Huefte 0,860 · Arm:Bein 0,649**, dazu beide Symmetrien mit Vorzeichen. **Die Einstufungen bleiben Attrappe:** `1.618`, V-Taper und Steve Reeves stehen **ausschliesslich in `theme-v1/uploads/`** — Begleitdateien des Entwurfs, keine Spezifikation; **der Reeves-Wert ist gar keine Rechnung, `daten.ts:438` gibt fest `88` zurueck.** Auch die **Farbe faellt weg**: die Vorlage faerbt eine wachsende Taille rot und einen wachsenden Arm gruen — welche Richtung erwuenscht ist, haengt vom Ziel ab; das Vorzeichen sagt dasselbe ohne Urteil. **Ein Kandidat mit echter Quelle, bewusst nicht gebaut:** Taille:Huefte hat mit WHO 2008 (`<0,90` M / `<0,80` F, im Vorgaengerrepo hinterlegt) als Einzige eine belegbare Schwelle — ein Grenzwert, der Geschlecht voraussetzt, ist eine Produktentscheidung. **Zwei Funde am Bestand:** `phase_am()` liefert **8 der 14 Spalten** — `transitioned_from`, `recommended_next`, `transition_reason` fehlen in der Signatur und werden jetzt nachgelesen (Muster aus G-80), **die Funktion blieb unangetastet, das Schema gehoert Codex**; und die Sperrbegruendung am Knopf „Switch to …" nennt `goals.goal_phases` als fehlend, **die Tabelle gibt es seit GO-07**. **Woche und Restdauer sind gerechnet, nicht gespeichert** (C-119) — `goal_phases` fuehrt weder Woche noch Adhaerenz noch Trend, und das steht als Satz an der Kachel. **Anzeigefehler der eigenen Arbeit, erst im Browser gesehen:** „Woche 12 von 7" — beide Zahlen richtig, die Fuegung sinnlos; jetzt „Woche 12 · 33 Tage ueber das geplante Ende" und „Tage ueberzogen 33". Gerenderte Marken **23 → 14** (Phase engine −5, Physique ratios −4). 411 Tests gruen (vorher 393), Zeilenschutz: `test-user` sieht 0 Phasen, 0 Umfaenge, 0 Zeilen — auch bei ausdruecklicher Abfrage der fremden UUID. **`pnpm gate` steht aus:** der `build`-Schritt bricht in `apps/web/src/app/v2/training/` ab (untracked, paralleler Agent) — `goals/` ist in jedem Lauf fehlerfrei, `next build` baut `/v2/goals` durch. |
| `139-coach-rechte.md` | **Permissions und Autonomy im Coach-Modul** (G-90). **Die zwei Achsen stehen in der Datenbank, nicht in der Oberflaeche:** `client_permissions` nimmt Schreibzugriffe nur von `client_id`, `client_autonomy` nur von `coach_id` — **der Coach kann seine Sichtrechte nicht selbst setzen, der Klient nicht seinen Reifegrad.** Genau die Umkehr, die F-04 verlangt (*„Der Coach setzt seine eigene Autonomiestufe — halte ich fuer einen Irrtum"*). **Der scheinbare Widerspruch zwischen F-04 und Toms Vorgabe ist keiner:** *wie autonom der Coach handeln darf* ist `*_auto_apply` und setzt der Nutzer; *wie reif der Athlet ist* ist `client_autonomy` und schaetzt der Coach — im Vorgaengerrepo war beides dieselbe Tabelle. **BLOCKER: `coach` ist nicht fuer PostgREST freigegeben** — `Invalid schema: coach`, dieselbe Meldung wie ein Schema, das es nicht gibt, waehrend `recovery` (170), `training` (1.416) und `goals` (181) im selben angemeldeten Lauf lesen. **Die Tabellen existieren** (C-119, `schema-sollstand.json`); es fehlt die Konfiguration, und die ist kein SQL — **dieselbe Sperre wie bei `training` in G-64, und C-119 hat sie nicht erwaehnt.** Deshalb Leerzustand nach G-65 statt Attrappe, mit der Ursache im Klartext. **Marken im Browser gezaehlt** (A-24): Permissions 7 → **0**, Autonomy 13 → **0**, Proposals → **0**; im Text bleiben sie stehen, weil die Entwuerfe in eigene Rueckfall-Funktionen gewandert sind. **Mockup und Schema fuehren beide sieben Module und nicht dieselben:** `buddy` gegen `body_metrics` — gebaut ist das Schema, `body_metrics` haette keine Spalte. **Der Bestaetigungspfad ist Zustandswechsel, keine Ausfuehrung:** `payload` beschreibt, was geschehen soll, **niemand wendet es an**; `action_log` mit `undo_data` bleibt ungenutzt, und Eintraege koennen ohnehin nur aus dem Coach-Portal kommen. **Nicht erbracht:** Setzen/neu laden und der Zeilenschutz-Nachweis — **beide Konten sehen dasselbe `Invalid schema`.** **Eigener Fehler gemeldet:** `.next` geloescht (alte Auftragsfassung), was den Dev-Server auf 3200 lahmlegte; das Ausweichen auf `LUMEOS_DIST_DIR=.next-g90` loeste dann die Kodierungspruefung des Gates aus — **`.next` und `.next-gate` sind die einzigen zwei vorgesehenen Namen.** |
| `140-supplements-restliche-tabs.md` | **Ein Tab von fuenf war baubar** (G-91). **Zuerst die Namen:** ein Tab `Timing` und ein Tab `Insights` **existieren nicht** — `Timing` ist seit G-37 die Gruppierung in `Today` (`stack_items.timing`, 8/8 gefuellt), `Insights` heisst `Intelligence`. **Gebaut ist `Catalog`:** 44 Eintraege aus `supplement_catalog`, Stufenfilter mit gezaehlter Verteilung (**S 4 · A 13 · B 16 · C 7 · D 4**), Serving, Timing, Preis, „In stack". Marken **22 → 20**, im Browser gezaehlt (A-24) und am Quelltext gegengeprueft. **Drei Behauptungen der Vorlage weggelassen:** das **Gewicht je Evidenzstufe** (S 1.00 … F 0.00) steht nur in `spec-daten.ts` und speist einen Score, den es nicht gibt; die Spalte **„Mode"** hat kein Feld; die Spalte **„Dose"** zeigt `serving_size`, weil `typical_dose_min/max` auf **allen 44 leer** sind — *eine Dosis zeigen ist etwas anderes, als eine zu raten.* Dazu sagt der Tab, dass **`evidence_summary` 0/44** und `evidence_sources` ueberall leer ist: der Buchstabe liegt vor, seine Begruendung nicht. **Der Hauptbefund steckt in `Intelligence`:** der Mikro-Lesepfad **existiert inzwischen** (`nutrition.micronutrient_snapshot`, 8 echte Zeilen) und **7 der 8 Codes** kommen auch in `nutrients_provided` vor — **die Gap Analysis scheitert an einer Einheit.** Vitamin D steht im Supplement in **IU**, im Mikro-Pfad in **µg**; naiv addiert ergaebe das **33.430 %** statt rund 930 %, ein **Faktor 40**. **Ein Umrechnungsfaktor liegt nirgends im Repo** (auch nicht im Vorgaengerrepo) — betroffen ist genau **eine** von 11 Zeilen. Dazu unveraendert: `nutrients_provided` **11 von 44**. **`Stacks` bleibt Attrappe:** 1 von 4 Kacheln haette Daten (`user_stacks`, fuer `dev` **1 Zeile**), `stack_templates` existiert nicht, `cycling` ist auf allen 8 Positionen leer — **und die Kachel behauptet einen „DB EXCLUDE constraint", den `pg_constraint` nicht kennt**, waehrend zwei Zeilen auf `is_active` stehen. **`Extended` bleibt Attrappe:** `enhanced_substances` **existiert nicht**, und **`experience_level` ist auf allen 5 Profilen NULL** — der Tab zeigt trotzdem etwas, weil sein Gate ein blosses `useState` ist und **nichts schuetzt**. `pnpm gate` 8/8, **418 Tests gruen** (vorher 411), Zeilenschutz: `test-user` sieht **0 Stacks, 0 Positionen, 0 Einnahmen — aber 44 Katalogzeilen**, weil der Katalog kuratierte Referenz ist. **Eigener Fehler gemeldet:** zweimal `next build` ohne `LUMEOS_DIST_DIR` (also in `.next`) neben dem laufenden Dev-Server plus dreimal `.next` geloescht — genau der Schaden, vor dem B-18 warnt; die Trennung ist nachgeprueft intakt, der Gate-Lauf ist mit `pnpm gate` gemacht. **Nebenbefund:** `tools/encoding-pruefen.mjs` prueft 3.846 Dateien und **nimmt `.next*` nicht aus** — fuenf `U+FFFD` in erzeugten Chunks unter `apps/web/.next-g90/` (fremdes Build-Verzeichnis) faerben das Gate rot, obwohl keine Quelldatei betroffen ist; seit B-18 jeder Agent in ein eigenes `.next-<auftrag>` baut, trifft das kuenftig jeden. Bildschirmfotos fehlen (geteilte Browsersitzung dauerhaft blockiert). |

**Was als Nächstes zu tun ist:** `docs/todo/TODO.md`
**Getroffene Entscheidungen:** `docs/spezifikation/90-entscheidungen/`
(D-06, 2026-08-06: `docs/decisions/` war nie befüllt — `[cmd]` einzige
getrackte Datei dort ist `.gitkeep`. Der Ort ist aufgelöst.)
**Sitzungsübergaben:** `docs/sessions/`

---

## Was hier NICHT steht

- **Produktspezifikation** → `docs/specs/` (13 Module, ~1,5 MB, Zielbild)
- **Design-System** → `docs/design-system/`
- **Historische Brainstorms** → `docs/BrainstormDocs/` (archiviert, keine Referenz)
- **Altlast Governance-Dokumentation** → `docs/_archive/`

---

## Pflege

Diese Dateien werden bei jeder Sitzung aktualisiert, in der sich etwas ändert.
Wer eine Aussage ändert, ändert auch ihren Marker und das Datum im Kopf der Datei.
Eine SSOT ohne Datum ist wertlos.
