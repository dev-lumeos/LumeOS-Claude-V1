# LumeOS — SSOT Index

**Stand:** 2026-09-08
**Gilt für:** `D:\GitHub\LumeOS-Claude-V1`

---

## Für neue Sessions: lies das zuerst

Dieser Ordner ist die einzige verbindliche Beschreibung des Ist-Zustands.
Wenn eine andere Datei im Repo etwas anderes behauptet, gilt diese hier —
ausser der Code selbst widerspricht.

**Rangfolge bei Widerspruch:**

1. Code (verifiziert per Befehl)
2. `docs/ssot/`
3. `docs/specs/` (beschreibt das Ziel, nicht den Ist-Zustand)
4. alles andere

---

## Erzeugte Dateien ? nicht von Hand aendern

`[cmd]` **Drei Dateien werden aus der Datenbank und den
Punktdateien erzeugt:**

| Datei | Inhalt | Erzeuger |
|---|---|---|
| `00-MODULTABELLEN.md` | Tabellen, Spalten, Zeilen, Datum | `tools/ssot-modultabellen.mjs` |
| `00-SCHEMA.md` | Funktionen, Policies, CHECKs, Sichten | `tools/ssot-schema.mjs` |
| `00-ABGENOMMEN.md` | was wann abgenommen wurde | `tools/ssot-nachtragen.mjs` |
| `00-SPEC-ABGLEICH.md` | was die Spec nennt und das Schema nicht hat, und umgekehrt | `tools/spec-abgleich.mjs` |

`[read]` **Erst dort nachsehen, dann messen** ? **heisst die
Tabelle so? Gibt es den Schreibweg? Was erlaubt der CHECK?**

`[cmd]` **Nach jeder Abnahme mit Schema- oder Datenwirkung neu
erzeugen** (A-74).

`[read]` **Was sie NICHT sagen: wozu etwas da ist, und was bewusst
nicht gebaut wurde.** `[cmd]` **Das ist Handarbeit** ? **und genau
dort entstanden am 08.09. fuenf Falschaussagen** (G-382 bis
G-386).

---

## Was LumeOS ist

Ein Health & Performance Operating System. Kernprodukt ist **Buddy**,
ein AI-Companion; die Fachmodule liefern das Wissen, aus dem Buddy schöpft.

Vollständige Produktvision: `docs/specs/00_MASTER_VISION.md`.
Diese SSOT beschreibt **nicht** das Produkt, sondern was davon gebaut ist.

---

## Herkunftsmarker — Pflicht bei jeder Aussage

Jede Tatsachenbehauptung in diesem Ordner trägt einen Marker.
Ohne Marker ist eine Aussage ungültig.

| Marker | Bedeutung | Beispiel |
|---|---|---|
| `[cmd]` | Durch ausgeführten Befehl belegt, mit Datum | `[cmd]` pnpm typecheck 2026-08-01, 17/17 grün |
| `[read]` | Datei gelesen, Inhalt zitierbar | `[read]` apps/web/package.json |
| `[annahme]` | Ungeprüft, Vermutung, Übernahme aus älterem Dokument | `[annahme]` Curation-Tabellen stammen aus manuellem SQL |

**Regel:** Nur `[cmd]`-Aussagen dürfen in `CLAUDE.md` oder in Agent-Anweisungen
als Regel übernommen werden. `[annahme]` gehört auf die Todo-Liste, nicht in eine Regel.

**Warum diese Regel existiert:** Am 2026-07-30 lief eine Bestandsaufnahme ohne
funktionierende Shell. Sie schloss daraus, `services/` und `packages/` seien leer.
Der Satz wanderte ungeprüft in die Projektanweisung und galt dort als Regel.
Tatsächlich enthalten beide Ordner 17 kompilierende Packages.
Ein `[annahme]`-Marker hätte das verhindert.

---

## Inhalt dieses Ordners

| Datei | Beantwortet |
|---|---|
| `00-INDEX.md` | Diese Datei. Einstieg, Regeln, Rangfolge |
| `10-workspace.md` | Was liegt im Monorepo? Was lebt, was ist Gerüst, was ist Altlast? |
| `11-zielarchitektur.md` | Wie soll es am Ende aussehen? Welche Apps, welche Services? |
| `20-apps-web-ist.md` | Was kann `apps/web` heute wirklich? |
| `30-datenbank.md` | Welches Schema ist live? Welche Entwürfe liegen daneben? |
| `35-naehrwert-bezugsgroesse.md` | Worauf beziehen sich die Nährwerte, und wie rechnet man auf eine Menge um? (Grundlage jeder Diary-Rechnung) |
| `36-testbasis.md` | Was wird womit geprüft — und was ausdrücklich **nicht**? (Gate, Rechteprüfung, E2E-Entscheidung) |
| `37-testkonten.md` | Testkonten der lokalen Instanz — wer darf, wer nicht, und wozu die zweite Session gebraucht wird |
| `40-spec-code-matrix.md` | Je Modul: Spec vorhanden? Code vorhanden? Delta? |
| `41-lebensmittelsuche-wortschatz.md` | Wie weit der BLS-Wortschatz von dem entfernt ist, was Menschen tippen |
| `42-dekompositum-messung.md` | Das deutsche Zerlegewörterbuch am BLS-Bestand gemessen |
| `43-wortschatz-luecke.md` | Die volle Lücke gegen OpenThesaurus, in vier Klassen |
| `44-bls-codestruktur.md` | Was der BLS-Code über Warengruppe und Zubereitung bereits sagt |
| `45-bls-dokumentation.md` | Was die amtliche Dokumentation hergibt — und was nicht |
| `46-bls-importluecke.md` | 30 Fettsäuren fehlten; Bestand gegen die Quelle geprüft |
| `48-artengruppierung-messung.md` | Erster Versuch, Arten aus dem BLS-Code abzuleiten — **gemessen gefallen**, 39 von 100 |
| `49-zubereitungsschluessel.md` | Die Reparatur dazu — **ebenfalls gefallen**, 47 von 100; der Schlüssel je Warengruppe, 204 Zellen |
| `51-sortweight-formel.md` | Wie `sort_weight` gerechnet wird: Spec-Formel, zwei Korrekturen, Umsetzung in die Kette |
| `52-anzeigenamen-batch.md` | Wie die 5.775 Anzeigenamen entstanden — und wie 704 Umlaute verloren gingen |
| `53-kettenluecke.md` | Warum fünf Kettenschritte nie liefen, und die Prüfung, die es künftig meldet |
| `54-nutrition-schema-abgleich.md` | Nutrition-Schema: was `SPEC_06` fordert, was gebaut ist, wo es auseinandergeht |
| `31-migrations-rueckbau.md` | Warum migrations/ nur drei Schema-Slices führt und die Kette die Wahrheit ist |
| `32-encoding-schaeden.md` | **Vier Encoding-Varianten** — umschriebene Umlaute, 704 Fragezeichen, doppelte Kodierung, UTF-16. Und warum es dreimal wiederkam. **Fortschreibung 2026-08-23: die vierte Wiederkehr, und der Weg war neu** — nicht der Dateizeiger, sondern die Standardeingabe. `[cmd]` 78 Befunde in `TODO.md`/`ERLEDIGT.md`/`00-UEBERSICHT.md`, vom Pre-Commit-Hook abgefangen; `LAUFEND.md` blieb sauber, weil es über das Datei-Werkzeug geschrieben wurde. Zwei Reparaturversuche schlugen fehl (zeilenweise scheitert an gemischten Zeilen; Latin-1 scheitert an CP1252) — **beides stand als Antwort schon im Bericht vom 2026-08-16 und wurde nicht gelesen.** Behoben sequenzweise über CP1252, in beide Richtungen belegt: 78 → 0, Gate grün. Neue Regel in `CLAUDE.md` |
| `33-pipeline-verifikation.md` | Verifikationslauf der Aufbaukette |
| `34-design-bestandsaufnahme.md` | Design — was vor Theme V1 vorhanden war |
| `38-cookie-bereich.md` | Cookie-Bereich über Apps hinweg, getrennte Sitzungen |
| `39-rueckleitadressen.md` | Rückleitadressen und site_url |
| `55-anzeigenamen-einspielen.md` | Wie die 7.140 Anzeigenamen in die Datenbank kamen — Kettenschritte 025 und 026 |
| `56-suchabdeckung.md` | 152 Lebensmittel mit Sollwerten. **Die Gegenprobe zeigt: C-38 hat die breite Abdeckung nicht verbessert** |
| `57-reis-kuration.md` | Reissorten als Suchbegriffe, griechischer Joghurt nach Recherche |
| `58-phonetische-varianten.md` | c/k, f/ph — gemessen, vorbereitet, **nicht gebaut**; wartet auf echte Fehlsuchen |
| `59-suchprotokoll.md` | nutrition.search_events — jede Anfrage, ohne Nutzerkennung |
| `60-nutrition-specs-auswertung.md` | Vier Specs gegen den Ist-Zustand. **docs/specs/Goals/ meldet vollständige Umsetzung für ein Modul, das hier nicht existiert** |
| `61-referenzwerte.md` | 165 Referenzwerte über alle 138 Codes. Erst abgebrochen, dann aus EFSA und US DRI gebaut |
| `62-lebensmittel-tags.md` | NOVA statt processed_food, Allergene umgekehrt markiert — **enthält Nüsse ist belegbar, enthält keine nicht** |
| `63-suchlaufzeit.md` | 494 auf 234 ms. **Der Index, um den es zwei Tage ging, existierte gar nicht** |
| `64-training-wortschatz.md` | Training hat dasselbe Wortschatzproblem — **3 von 34 Sollwerten**, diesmal vor dem Bau gemessen |
| `65-tags-und-aliase-einspielen.md` | 17.967 Tags und die Reis-Aliase live |
| `66-eigene-lebensmittel-entwurf.md` | Schema-Entwurf für foods_custom |
| `67-fettsaeuren-verlust.md` | **171.409 Nährwerte einen Tag lang verloren** — und die Prüfung schrieb den Schaden als Sollwert fest |
| `68-theme-faehigkeit.md` | 538 harte Farbwerte in 7 Dateien. **Keine davon benutzt einen Token** — zwei Welten, kein Mischzustand |
| `69-kette-ausfuehrbar.md` | **Es gab keine Kettensteuerung** — nur eine Prosa-Tabelle. Jetzt kette.json und ein Ausführer |
| `70-tagesbilanz-mikros.md` | 24 Mikros mit Fehlzählern. Flach statt JSONB, weil **23 von 24 Codes über 94 % belegt** sind |
| `71-eigene-lebensmittel.md` | foods_custom gebaut, Plausibilitätsprüfung meldet statt zu blockieren |
| `72-profil-und-bewertung.md` | Profil und daily_reference_assessment — **dieselbe Mahlzeit, 72,7 % gegen 50,0 % je nach Geschlecht** |
| `73-parallelstruktur.md` | /v2 neben der alten Oberfläche. **Der Bundle-Hash blieb gleich, als v2.css sich änderte** |
| `74-shell.md` | Sidebar, Topbar, Kontextspalte. **Der Entwurf hat null @media-Regeln** |
| `75-nutrition-oberflaeche.md` | Suche und Tagebuch. **Keine gefüllten Ringe ohne Ziel** — ein zu 68 % gefüllter Ring wäre eine Falschaussage |
| `77-profilpflege.md` | /v2/settings — die sechs Felder, ohne die keine Formel rechnet |
| `78-zielwerte.md` | TDEE und Makros. **Der Vorgänger hat GO-02 viermal verschieden beantwortet** |
| `79-portionen.md` | 23.402 Portionen über 7.048 Lebensmittel — **kategoriebasiert übernommen**, nicht kuriert |
| `80-vorgaengerrepo-fundus.md` | **Wegweiser durch referenz/lumeos-2026/** — 75 Migrationen, 52 Seed-Dateien, Formeln nach Thema. Vor jeder Einstufung als fehlend hier nachsehen |
| `81-tagebuch-schreiben.md` | Der Schreibpfad. **Einfrieren belegt: Lebensmittel auf 999 kcal geändert, Position bleibt bei 348** |
| `82-testdaten.md` | 3 Nutzer, 512 Mahlzeiten, 43 Tage — und **11 Szenariotage mit Register** |
| `83-dashboard.md` | Dashboard nach der Vorlage, 12 Kacheln mit Attrappenmarken |
| `84-hydration.md` | Wasserziel und Tagesdaten. **Getrunkenes und Wasser aus Lebensmitteln getrennt** |
| `85-mikro-uebersicht.md` | Netzdiagramm und Schwellenliste — **16 von 17 am Mangel-Szenariotag** |
| `91-training-mockup.md` | Training nach der Vorlage, 38 Kacheln, **alles Attrappe**. Klärt, wie die vier Vorlagendateien eines Moduls zusammenhängen — und welche Kachel zuerst echte Daten bekommen kann |
| `92-recovery-mockup.md` | Recovery nach der Vorlage, 36 Kacheln, **alles Attrappe** (kein Schema). **Zwei konkurrierende Rahmen — `app.jsx:122` entscheidet, welcher gilt.** Damit drei Muster für Vorlagendateien, und die Prüfung, die sie unterscheidet |
| `94-goals-mockup.md` | Goals nach der Vorlage, 39 Kacheln, alle markiert — **obwohl fünf davon auf `goals.zielwerte_am` passen und sofort echt werden könnten.** Dreistufige Vorlagenkette; `Math.random()` der Vorlage ersetzt, weil es die Hydration bricht |
| `95-supplements-mockup.md` | Supplements nach der Vorlage — der Rahmen und seine **sieben eigenen Tabs**, vier weitere als benannte Luecke. **227 KB Vorlage, viertes Muster: arbeitsteilig statt konkurrierend.** Mit dem, was im Vorgaengerrepo danebenliegt (acht Tabellen, 36 KB Seed, Halbwertszeit) |
| `99-medical-mockup.md` | Medical nach der Vorlage, 21 Kacheln, **alles Attrappe** (kein Schema). **Erste gezählte Vollständigkeit: 56 von 56**, mit wiederholbarem Skript. Fünftes Muster: der Rahmen ruft seine Zulieferer als **blosse Globale** — ein `window.`-Grep findet sie nicht. Plus 457 Laborwert-Synonyme im Vorgängerrepo |
| `102-coach-mockup.md` | Coach nach der Vorlage — **zwei Unterbereiche unter einem Menuepunkt.** `Human Coaches` gebaut und **gezaehlt vollstaendig (60 von 60)**, `AI Coach` offen (41 Posten) und in der Oberflaeche namentlich benannt. **Der Auftrag nannte 11 und 12 Tabs; gezaehlt sind es 10 und 20.** `-gaps.jsx` heisst „gap-closers" und gehoert zu zehn Elfteln dem Portal. Im Vorgaengerrepo liegt **ein fertiges Berechtigungsmodell** — aber mit zwei Stufen statt drei, **ohne Widerrufshistorie**, und mit einem `edit_auto_accept`, das dem Vorschlagsmodell der Vorlage widerspricht |
| `106-ai-coach-mockup.md` | AI Coach (Buddy) nach der Vorlage — **20 Tabs, 42 von 42**, damit sind beide Unterbereiche des Menuepunkts fertig. **Die vermuteten „elf fehlenden Zulieferer" fehlen nicht** — alle fuenfzehn liegen vor, einer davon in einer *Coach*-Datei; die Falle war die `Object.assign`-Zeile, die **nur Daten fuehrt, keine Komponenten**. `uploads/` ist byteweise identisch mit `docs/specs/` — **der Hash unterscheidet Module, nicht Fassungen**. Im Vorgaengerrepo liegen 6 Migrationen und `buddy.ts` mit 1.647 Zeilen, aber **BSS und Voice gibt es dort nicht** — die zwei Tabs sind neu zu bauen, nicht zu uebersetzen |
| `109-medical-anbindung.md` | **Medical zeigt eine Liste statt drei** (G-60) — die Form der Attrappe traegt echte Daten: 37 Marker aus 140 Werten und 5 Befunden, Panelfilter, Verlauf (Glucose 88→102, +15,9 %), Bereichsbalken, Popup. Die zwei Listen aus G-46 sind weg, 20 Marken bleiben. **Die elf Panels der Attrappe gibt es in keiner Quelle** — `loinc_class` gibt vier Gruppen, `panel_type` ist bei 11.421 von 11.676 leer, `panels` haengt Kreatinin in 33 Elternpanels, und das Vorgaengerrepo fuehrt `category` als Probenmaterial. Gebaut ist, was da ist; die Zuordnung ist Toms Entscheidung. **Der Bereichsbestand ist duenner als seine Zeilenzahl:** von 464 Zeilen tragen 54 Zahlen — und genau die schliesst die Lesefunktion aus. Teil I (G-46) haelt Lesepfad, Bereichsvorrang und den Befund fest, dass `tom.seed@example.com` kein Passwort hat |
| `113-ui-reste.md` | **Zwölf gemeldete Punkte abgearbeitet** (G-56) — `shield`/`history`/`file`, `Pill dot`, `Empty`, `v2-g-cols-5`, `InEntwicklungKnopf disabled`, JetBrains Mono. **Die 375-px-Ursache war eine andere als gemeldet:** die Seitenleiste rollt längst, breit war die Kopfzeile (606 px, `overflow: visible`). **Und die früheren 375-px-Messungen (G-40/G-42/G-46) waren nicht belastbar gewonnen** — `goto` setzt das Fenster zurück, eine abgelaufene Sitzung misst die Anmeldemaske. `btn-accent` (G-10) trifft nicht mehr zu, gemessen |
| `101-vollstaendigkeit.md` | **Alle sechs Module gegen die Vorlage gezählt** (G-38), mit verallgemeinertem Skript und belegter Umbenennungstabelle. Fünf von sechs vollständig; offen bleibt Supplements (G-31 — **plus eine neu gefundene Lücke in `SuppCost`**). Sechstes Muster: eine „feste" Formel kann engine-abhängig sein — `Math.sin` brach die Hydration trotz grünem Test. **Enthält eine Korrektur:** die Recovery-Zählung 67/71 war falsch |
| `104-muskelkarte.md` | **Die anatomische Muskelkarte** aus dem Mockup in `packages/ui` (G-26) — 17 Muskelgruppen statt 18 Blobs, drei Module können sie nutzen. 19 Hex-Werte auf Tokens gelegt, **ohne neue zu erfinden**; die vierte Aktivierungsstufe per `color-mix`. Siebtes Muster: **eine fehlende Zuordnung sieht aus wie ein fehlendes Bauteil** — `upper-back` mit Bindestrich wäre beinahe dauerhaft grau geblieben. **Nachtrag G-44:** alle 39 IDs eingeordnet (17 Gruppen · 16 Injektionsorte · 6 Nicht-Muskeln), Test macht eine unzugeordnete zum Fehler. Zwei gemeldete Gestaltungsfragen: **die grauen Teile heben sich um 0,012 Helligkeit ab** — praktisch unsichtbar — und **der Umriss der Vorlage ist kaputt** (118 von 200 Pfadbefehlen fehlerhaft, Kontur endet auf halber Höhe) **Nachtrag G-55:** die drei Ebenen stehen — alle **96 Muskelgruppen aus C-73** fallen auf 17 Flaechen, **gemittelt statt maximiert**, und der Klick trennt wieder auf (Flaeche -> Gruppe -> Muskeln). Sichtbarkeit im Dunkelmodus geloest: `--surface-2` lag 0,030 ueber dem Kartengrund, `--border-strong` liegt 0,155 — kein neuer Token. Achtes Muster: **der Umriss der Vorlage ist toter Code** (`outlinePath` wird nie angehaengt), die volle Figur kommt allein aus `MUSCLES`. Die Check-ins sind angebunden, greifen aber erst, wenn `recovery` in `supabase/config.toml:16` freigegeben ist |
| `47-lebensmittelsuche-stand.md` | **Der erreichte Stand** — fasst 41 bis 46 zusammen |
| `60-legacy-cloud.md` | Was liegt wirklich in der Legacy-Cloud-Instanz? (echte Zeilenzahlen, Medienverweise, Schemakonflikt — ersetzt alle Schätzungen) |
| `50-governance-rest.md` | Was ist vom alten Governance-System übrig und warum? |
| `86-erfassung.md` | Erfassung im Tagebuch — Suche in der Mahlzeitenkarte, Portionswahl, `Same as yesterday`. **Ringe springen ohne Neuladen.** |
| `87-preferences.md` | Lebensmittel-Vorlieben — `food_preferences_read/write`, Zeilenschutz beidseitig. **`hard` ist kein Sicherheitsversprechen** (C-62). |
| `88-i18n.md` | Mehrsprachigkeit mit next-intl, **ohne Sprachpraefix**. 95 Schluessel de/en, Thai vorgesehen. Gate-Pruefung erzwingt Vollstaendigkeit. |
| `89-mahlzeitzeit.md` | `meal_time` in `meals`, UNIQUE-Index entfernt — **mehrere Mahlzeiten je Typ moeglich.** 686 bestehende nachgetragen. |
| `90-datum-und-mahlzeiten.md` | Datumsnavigation `‹ Heute ›` und eine Karte je Mahlzeit. **Drei Stellen rechneten ueber Mitternacht** und waeren an Zeitumstellungstagen gekippt. |
| `93-trainingssitzungen.md` | `workout_sessions`, `workout_exercises`, `workout_sets` — **9 Sitzungen, 60 Saetze live.** Der Uebungsname wird eingefroren. |
| `95-kimi-bestand.md` | **Was in Kimis Rechercheverzeichnis tatsaechlich drinsteht** — `[cmd]` 189,4 MB, 38 Crawls, 442 Berichte, 76 Dateien allein unter `data/evidence/`. Der Orchestrator hatte `RESEARCH_STATUS.md` dreimal zitiert und **nie geoeffnet**; darauf gestuetzt entstand ein ganzer Nachtlauf mit Schablonentexten fuer Felder, deren Inhalt seit Wochen vorlag. Kimi hat die Uebergabe selbst sortiert: `DATA_READY` (66 Biomarker-Erklaerungen auf Deutsch, 8 Enrichment-Layer, 290 Dosisanreicherungen), `SCHEMA_CHANGE_REQUIRED` (Symptomtabelle, 102 Kanten), `REPO_DEPENDENCY` (LOINC-Mapping auf HOLD, weil Kimi unser Repo nicht kennt). **Die Arbeit ist Import und Zuordnung, nicht Recherche.** |
| `96-kimi-inhalt.md` | **Was in Kimis Bestand tatsaechlich drinsteht** — jede Datei geoeffnet, nicht gezaehlt. Tom, 2026-08-25: *„das ist wahres gold."* Die Regel-Engine ist fertig spezifiziert (64 Regeln mit ausfuehrbaren Bedingungen plus `module_field_spec` als Schnittstelle). Der WADA-Geltungsbereich ist zweifach beantwortet und genauer als vermutet — Natural-Ligen fuehren **eigene, weitergehende** Sperrlisten. `effect_class` trennt *„der Wert aendert sich"* von *„der Wert wird falsch gemessen"*. 56 aufgeloeste Wissensluecken sagen, **warum** ein Feld leer ist. 265 Trainings-Konstanten sagen, was **nicht** gebaut werden soll (ACWR `implement: no`). Die Produktebene traegt **unsere** `sub_*`-IDs samt Preis je Portion. **Stand 2026-08-26: vier Auftraege daraus durch** — C-272, C-273, C-275, C-288. Die Regel-Engine feuert trotzdem nicht, weil `user_medications` im Klartext speichert (C-285). |
| `96-recovery-checkins.md` | `recovery.checkins` — **36 Eintraege, davon 27 ohne HRV.** Der `manual`-Modus ist der Normalfall, nicht die Ausnahme. |
| `97-ziele-und-phasen.md` | `user_goals` und `goal_phases`. **Vier Zielvokabulare aufeinander abgebildet** — als Datendatei, ohne offene Begriffe still zu fuellen. |
| `98-supplements-schema.md` | Fuenf Tabellen, eine Sicht — **44 Katalogeintraege live.** Keine Wechselwirkungsbewertung: das waere eine medizinische Aussage. |
| `100-koerpermessungen.md` | `body_measurements` und `body_circumferences`, **43 Messungen.** Dazu Koerperfett nach Navy **mit Spanne** — 10,31 % (6,81–13,81). |
| `103-biomarker-katalog.md` | **11.676 LOINC-Codes** in acht Dateien, mit Einheit, Klasse und deutschem Namen. Normbereiche liefert LOINC nicht — sie docken spaeter an. |
| `105-medical-schema.md` | `biomarker_catalog`, `lab_reports`, `lab_result_values`. **Der Referenzbereich sitzt am Befund**, der Katalogbereich ist der Rueckfall. |
| `107-laborimport.md` | Zuordnung von Markernamen auf LOINC — **292 Aliase.** Drei Faelle belegt: eindeutig, mehrdeutig, **unbekannt mit Rohtext.** |
| `108-muskelgruppen.md` | Kuration der Muskelgruppen: **107 → 96**, 89 Eltern-Beziehungen — **`exercise_muscles` unveraendert bei 6.624.** |
| `110-datenherkunft.md` | Herkunftsspalten in **sieben Messtabellen**, bevor die Geraete kommen. `meal_items` trennt `food_source` von `measurement_source`. |
| `111-meilensteine-und-tdee.md` | Meilensteine und **adaptive TDEE** — 3.143,2 gegen Formel 3.527,0. **`alpha 0.3` macht den Wert zu 70 % zur Formel** (GO-15). |
| `112-dev-konto.md` | Der volle Demo-Bestand auf `dev@lumeos.app` — **43 Messungen, 36 Check-ins, 9 Sitzungen, 2 Befunde.** `tom.seed` kopiert, nicht verschoben; `test-user` bleibt Pruefkonto. |
| `114-supplements-angebunden.md` | Vier Tabs lesen echt, **14 Kacheln verlieren die Marke** — `Cost` entgegen der Auftragsannahme dabei, weil alle 44 Katalogeintraege Preise fuehren. **`Compliance` braucht 30 Tage und einen Auslasser**, es liegt einer. `refillUrgent` sollte eine Schwelle sein, keine Handmarkierung. |
| `115-training-exercises-tab.md` | Der Exercises-Tab liest echt — **1.416 Uebungen, 8 von 8 Filtern gegen SQL geprueft**, Suche 142–172 ms. `Type` faellt weg (keine Quelle), `Discipline` tritt an seine Stelle. **Die Rollenfrage entschieden:** primaer als Vorgabe, denn bei Hamstrings stehen 100 gegen 384. Zuerst zwei Sperren dokumentiert (PGRST106, RLS ohne Policy) — **die Kette fuehrte die Policy, die Datenbank hatte sie verloren.** Zwei stille Fehler gefunden: PostgREST deckelt bei 1.000 Zeilen, `.in()` kippt ueber 200 IDs **ohne Fehlermeldung**. |
| `116-food-db-tab.md` | Der Food-DB-Tab bekommt die Form des Entwurfs — **7.140 Lebensmittel, 9 von 9 Pillen gegen SQL geprueft**, Suche 199–304 ms. **Drei Pillen des Entwurfs tragen nicht:** `Favorites` (C-94), `Recent` (Verlauf, keine Kategorie), `Supplements` (der BLS fuehrt keine). **2.237 ohne Kategorie bleiben ueber Suche und `All` erreichbar** — nichts erfunden; `food_groups` hat keinen Fremdschluessel zu `food_categories`, nur Prosa im `bls_hint`. `huhn` zeigt 136 statt 31: **die Synonymaufloesung, kein Fehler.** |
| `117-ausschluss-presets.md` | **11 Ausschluss-Presets, 33 Regeln, 3.767 Zuordnungen** — „Keine Innereien" trifft 131 mit allen fuenf Lebern. `halal`/`kosher` hatten seit C-98 null Zuordnungen, jetzt 6.379/6.451 — **aber das Tag sagt „enthaelt keine verbotene Zutat", nicht „ist halal":** die Schlachtung steht nicht in den Daten, der Vorbehalt haengt als `caveat_de` am Preset. Das Schwein-Muster brauchte drei gemessene Gegenmuster (32 Fehltreffer → 0). **231 Wurstwaren ohne Tierart im Namen bleiben unerfassbar** — gemeldet statt geraten. Laktose gehoert bewusst zu `intolerances[]`, nicht hierher. |
| `120-daumen-bewertung.md` | Daumen hoch/runter in Trefferliste **und** Detailansicht, Zyklus neutral→like→dislike→neutral. **Abfrage nur abwaerts**, mit dem Namen des Lebensmittels; danach verschwindet die Zeile. Schreibt `source='search_thumb'` und eine Stufe **unter** dem Assistenten (`like`/`soft_dislike` statt `boost`/`hard_exclude`) — **der Aufwand sagt etwas ueber die Absicht.** Bei Widerspruch gewinnt das Spaetere, erzwungen durch `uq_food_pref_items_user_food`. **Befund an der Naht zu G-65:** dessen RPC loescht alle Items der Nutzerin und schreibt `source` fest auf `settings` — Daumen-Bewertungen verlieren dabei ihre Herkunft. |
| `116-goals-anbindung.md` | **Fuenf von zehn Tabs lesen echt** (GO-16), 38 Marken werden 23 — vier Tabs zeigen keine mehr. **Drei Zahlen des Auftrags gelten an einem anderen Tag:** KFA 10,31 % und FFMI 21,97 stammen vom 2026-09-13, weil **26 der 43 Messungen in der Zukunft liegen**; heute sind es 11,38 % und 21,49. **Der adaptive TDEE ist leer und bleibt es**, bis 14 von 14 Zufuhrtagen vollstaendig sind (es sind 13) — die Kachel zeigt die Bedingung statt einer Ersatzzahl, daneben die echte Formel 3.527 kcal. `alpha 0.3` steht jetzt ausgeschrieben da (GO-15 offen). Das feste „Heute" der Vorlage ist raus, `lib/datum.ts` rechnet. **16 Eingabefelder bleiben markiert stehen** — Goals erfasst nicht. |
| `117-zusammenhaengende-seeds.md` | **180 Tage korrespondierende Seeds** (C-78) — zwei 90-Tage-Fenster, 2.157 Mahlzeiten, 180 Messungen, 170 Check-ins, 30 Trainingssitzungen. `dev@lumeos.app` bekommt denselben Bestand wie `tom.seed`; `test-user` bleibt klein. Adaptive TDEE ist `complete`, die Bilanz geht auf: 0,170 kg Gewicht plus entspricht 100,7 kcal/Tag. |
| `118-training-sitzungen.md` | **Fuenf Tabs lesen echte Sitzungen** (G-69), 40 Marken werden **24** — History, Progression, Standards, Calendar und die Serie. **Die zwei Sperren aus G-64 sind weg** (`training` in `config.toml`, Policy auf `exercises` wieder da). **`workout_sessions.status` trennt geplant und absolviert NICHT:** alle 30 stehen auf `completed`, auch die 15 nach heute — massgeblich ist das Datum, der Status kommt zum Vergleich mit. Volumen, Serie und e1RM zaehlen nur Absolviertes; Kalender zeigt alle, mit Pille und Strichform statt blasserer Farbe. **Volume landmarks bleibt Attrappe** — MEV/MAV/MRV stehen nur in Vorlagendateien, und dieselbe Regel streicht das Zielband der Volumenkachel und die Einstufung `Beginner…Elite`. **Der offene Punkt aus G-64 loest sich:** Progression zeigt ohnehin nur die 6 Uebungen mit Verlauf. Ungenutzt gefuellt: `rpe` 200/200, `logged_via` 200/200, `set_type` 200/200. |
| `119-vorlieben-tab.md` | **Der Preferences-Tab liest und schreibt echt** (G-65) — alle **6 Kacheln verlieren die Marke, eine siebte kommt dazu** (generelle Ausschluesse). Die Datenseite war fertig: `food_preferences_read`/`_write` seit C-87, benutzt statt nachgebaut. **Zwei Abweichungen vom Mockup, mit Tom besprochen:** 20 Allergene in drei Stufen statt 14 an/aus (sonst waere `intolerances[]` nicht bedienbar und die Laktose-Testdaten unsichtbar), und Kategorien aus dem Katalog statt fester Achterliste. **Der gefaehrlichste Fund:** eine gesetzte Unterkategorie („Kekse & Plätzchen", Enkel von SÜSSES & SNACKS) waere unsichtbar gewesen — und weil `food_preferences_write` die Items **ersetzt**, haette die naechste Aenderung sie still geloescht. **C-93 wurde mitten im Auftrag fertig:** statt Leerzustand bietet die Kachel jetzt 11 Presets, und der Schlachtungs-Vorbehalt kommt aus `caveat_de` statt aus dem Code. **Die Rangfolge ist hinterlegt und wirkungslos** — `food_search` hat keinen Nutzerparameter (C-94). Acht Spalten sind gespeichert ohne Wirkung und ohne Kachel: Kochlevel, Zeit, Budget, Mahlzeiten, Kuechen. |
| `121-supplements-bestandsaufnahme.md` | **Bestandsaufnahme aus drei Quellen** (F-02) — das Gebaute traegt; die Luecken sind drei Datenbestaende mit je eigener Sorgfaltsstufe. **`SPEC_08` ist keine Importpipeline, sondern Seed-SQL** (18 Praeparate, „kuratiert, keine externe Quelle"); die echte Kandidatenliste ist die Mini-PC-CSV mit **702 Zeilen (nicht 1302, wie das Importskript behauptet)** — davon 200 Enhanced/PED, 0 mit Quellenangabe. Interactions: 28 CSV-Zeilen + 12 Spec-Zeilen, **6 der Spec-Zeilen sind Supplement×Medikament** und brauchen ein Medikationsmodell, das es nicht gibt; die Grenze „nennen statt bewerten" liegt fertig im Vorgaenger (`intelligence.ts`: Mengenschnitt mit dem Stack). Injections: der 17-KB-Change-Request deckt sich mit dem Mockup, drei Objekte reichen — **aber keine der 16×3 Grenzwerte (max_ml, rest_days, Nadel) ist im Repo belegt.** Compliance ist seit C-82 anbindbar (90 d, 8 skipped, 93,3 %), offen nur: tragen die skipped-Zeilen notes? Sechs Auftragsentwuerfe, sortiert nach Freigabebedarf. |
| `122-vorlieben-suche.md` | Die Suche wendet die Vorlieben an — `p_user_id` als 15. Argument. **`hard_exclude` entfernt, `soft_dislike` wertet ab.** Union der drei Presets: nur 808 von 7.140. |
| `123-rechtemodell.md` | Permissions (Nutzer setzt Sicht und Bestaetigungspflicht) und Autonomy (Coach setzt den Athletenlevel) als **zwei getrennte Achsen**, mit Widerrufshistorie. Dazu `alpha = 1,0`. |
| `124-nutrition-feinschliff.md` | Kopfzeile, Warnhinweis, Filter, Blaettern. **Die BLS-Nennung darf weg** — Kapitel 9.2/9.3 der Dokumentation: Zitierweise **empfohlen** (fuer Publikationen), Bestand „kostenfrei und ohne Lizenzbarrieren"; die `SOURCE`-Spalte bleibt. Zentrierung erst nach drei Anlaeufen (71 px daneben → abgeschnitten → Raster, 0 px): **`v2.css:1064` setzt `position: relative` auf jedes Kopf-Kind und schlaegt eine einzelne Klasse.** **7.140 sind jetzt erreichbar** (143 Seiten), 4 von 4 Filtern gegen SQL geprueft. **Drei Sperren in der Suchfunktion gemeldet statt umgangen:** nur EIN Tag (kein ODER/UND), **kein Ausschluss-Parameter** (Allergene wirken nur auf der Seite), kein `processing_level`. Laufzeit 330–492 ms — **gemessen in der RPC selbst, nicht durch diesen Umbau** (bei limit 25 und 50 gleich lang). |
| `125-supplements-inventory.md` | **Inventory, Compliance und die Rueckfallfassungen** (G-74). **Der Markenzaehler kann jetzt unterscheiden:** `attrappe={RUECKFALL}` trennt „abgeloest, aber aufgehoben" von „nie angebunden" — `tabs.tsx` zeigt **1 echte Attrappe gegen 16 Rueckfallfassungen** statt pauschal 17. Toms dritter Weg: nicht loeschen wie bei Goals, aber erkennbar machen. **Die Reichweite haengt an der Einheit** — die alte Formel meldete fuer Kreatin 30 statt 6 Tage, weil sie `stock_unit` (`g`) ignorierte; die Auftragszahl war ebenso falsch. Drei Nachfuellstufen aus der Reichweite (7/14/30 Tage), `low_stock_threshold` als Rueckfall. **Compliance 93,1 %**, nicht 100 — und **alle 32 `skipped`-Zeilen tragen `notes`, aber der Text ist ein Erzeugungsvermerk**, kein Nutzergrund: Datum gezeigt, Fehlen benannt. Kostenverlauf **4 ehrliche Monatspunkte**, angeschnittene gestrichelt. **Ein HTTP 500 im Browser gefunden, den der Typecheck nicht sah:** ein Wert-Import aus einer Datei mit `next/headers` zog das Server-I/O ins Browserbuendel. |
| `126-tdee-abweichung.md` | **Die Messung war korrekt, der Seed-Faktor nicht.** `very_active` bei 0,58 Trainings je Woche — Abstand von −1.029,6 auf **−55,0 kcal**. |
| `127-recovery-checkins.md` | Der Erholungswert rechnet echt — **Manual-Gewichte 30/15/15/10/15/10/5, ohne HRV** (127 von 170 Check-ins haben keins). Beispieltag ohne HRV: **82**, unabhaengig nachgerechnet. **Zahl ja, Urteil nein:** „Score 88 · Good" wurde zu „Score 82" — `Good` ist eine Readiness-Stufe der `SPEC_09` und damit Urteilssprache. **Fehlende Anteile zaehlen nicht mit**, weder als null noch als voll: gerechnet werden 75 von 100 Gewichtspunkten, die zwei offenen stehen mit Grund da. **Soreness-Mittelung entschieden (E2, Tom muss bestaetigen):** nur gemeldete Muskeln — das JSONB fuehrt hoechstens **fuenf**, nie 18 und nie leer. **Drei fehlerhafte Quellformeln nicht nachgebaut** (u. a. ACWR-Faktor 1.1 bei 1.4). **Modalitaeten brauchen eine eigene Tabelle** — `checkins` hat keine Spalte dafuer; `recovery` hat genau eine Tabelle. |
| `128-recovery-scores.md` | `recovery.scores` und `modality_log` mit Manual-Modus. **170 Scores, 89 Modalitaeten** je Konto. |
| `129-goals-prioritaeten.md` | Prioritaeten setzen, Ziele bearbeiten, Zeitachse. **Beide Spalten lagen vor** (`priority`, `status`) — es fehlte nur der Schreibweg. **Die eigentliche Regel steht in der Datenbank, nicht in einer Spec:** `user_goals_check1` (aktiv = 1–3) UND `uq_user_goals_active_slot` (eindeutig je Nutzer) ergeben **genau drei aktive Plaetze, jeden einmal** — der zweite Teil fiel erst beim Ruecksetzen der Testdaten auf. **Zwei stille Fehler:** ein Wert-Import aus einer `'use client'`-Datei zog `next/headers` ins Buendel (**HTTP 500 auf JEDER Seite**, Typecheck gruen — wie G-74), und PostgREST meldet `ok` bei einem `update`, das der Zeilenschutz leergefiltert hat (behoben mit `.select()`). **Die Historie ist leer, und das ist der Befund:** kein Ziel traegt einen abgeschlossenen Status, alle drei Meilensteine stehen auf `open` — zwei davon ueberfaellig. Toms Seed fehlt. TDEE-Satz berichtigt: `alpha = 1,0`, keine Glaettung. |
| `130-medical-felder-erfahrungsgrad.md` | **Vier ungenutzte Medical-Felder und der Erfahrungsgrad** (G-80). **Die Herkunftsspalte ist zurueck:** `reference_source` zeigt den Katalog-Rueckfall wieder — 1 von 37 Zeilen (Calcium, der Fall aus G-46), als Unterzeile statt eigener Spalte, weil 138 von 140 dasselbe sagten. `lab_name` und `report_time` stehen im Popup je Messung (zwei Labore, fuenf Befunde). **`fasting_status` ist auf allen 140 Zeilen `unknown`** — die Anzeige ist gebaut und zeigt heute nichts. **Er kann nie umschalten, nur anzeigen:** `biomarker_reference_ranges` hat keine Nuechtern-Spalte, `population` steht 560x auf `general`. **Die Unterscheidung steckt bereits im LOINC** — `1558-6` ist *Fasting* glucose gegen `2345-7`; ob `fasting_status` daneben gebraucht wird, ist eine Schemafrage (Code sagt die Absicht, Feld die Tatsache). **Der Erfahrungsgrad hat keine Spalte** — `public.profiles` fuehrt 14, `activity_level` ist etwas anderes (ein Anfaenger kann `very_active` sein). Kachel sichtbar, Auswahl gesperrt, ohne Attrappenmarke — das Muster aus G-65. **Nutzer-Onboarding existiert nicht**, nur der Klienten-Assistent im Coach-Modul. Erfahrungsgrad ist **Selbstauskunft**, Autonomy (C-71) **Fremdeinschaetzung** — nicht zusammenlegen. |
| `131-medikamente.md` | Medikamente: **56 Wirkstoffe, 119 Formulierungen, 124 Produkte**, dazu `user_medications` und `user_conditions`. |
| `132-recovery-anzeige.md` | **Die Score-Anzeige liest aus der Tabelle** (G-82). **Die zwei Rechnungen stimmten NICHT ueberein** — fuenf gemessene Tage, Abweichung −7,0 bis +2,6. **C-125s Referenztag stimmt (2026-06-05 = 43,0), G-76s nicht:** die Tabelle sagt fuer den 2026-11-06 **79,4, nicht 82**. Der Grund sind nicht die Gewichte (Kern identisch), sondern **wieviele Anteile zaehlen**: `score.ts` rechnet fuenf von sieben und normiert auf Basis 75, die Tabelle rechnet alle sieben und fuellt Trainingslast und Ernaehrung mit Rueckfallwerten (Basis 100). **Die Tabelle ist die bessere Zahl** — sie benutzt den gemessenen ACWR auf **118 von 170 Tagen**, die `score.ts` verwirft. Die Anzeige liest jetzt `recovery.scores` (neu: `lib/recovery/scores-read.ts`), zeigt alle sieben Anteile mit Herkunft im Klartext, Rueckfaelle blass, plus Kurve ueber 170 Tage. **`score.ts` bleibt — aber die Live-Vorschau benutzte sie nie:** `tab-checkin.tsx` rechnete mit der Entwurfsformel (eigene Gewichte, HRV aus der Attrappe, `0.88 * 10` als erfundene Ernaehrung, `readinessFor` als Urteilssprache). Jetzt rechnet sie mit `score.ts` und nennt ihre Basis; **sie ist bewusst nicht dieselbe Zahl wie die gespeicherte**, und das steht an der Kachel. `modality_log` angebunden: 89 Zeilen, vier Arten, **alle Bonus 0 mit `pending_c124_e5`** — gezeigt, nicht geaendert. **Zwei Messfehler der eigenen Arbeit korrigiert:** die Kurve zeichnete ein Dutzend Zacken, obwohl **genau ein** Tag unter 60 liegt und es **keine Datumsluecke** gibt (Streckung im 100x100-Feld); und `immediate_effect` ist **1–10, nicht 1–5** (`121_…sql:216`). Zeilenschutz gegengeprueft: `dev` 170/89/170, `test-user` 0/0/0 bei erfolgreicher Anmeldung. Kacheln mit Marke auf `Today` 4 → 3. |
| `133-substanzbruecke.md` | Aliasbruecke zwischen drei Substanzbestaenden (1.132 Zeilen) und `missing_input` fuer Regeln. **42 von 64 Regeln auswertbar** statt 1. |
| `134-medical-score.md` | **Der Medical-Kopf zaehlt jetzt, statt zu bewerten** (G-84). **`Health score 87` und `6 alerts` waren beide erfunden** — aus dem Entwurfskatalog gerechnet, ohne Bezug zu den Befunden; dass die 6 zufaellig stimmte, haette sich bei keinem neuen Befund bewegt. **Zwei Zahlen statt einer, weil sie verschiedene Autoritaet haben:** **2 ausserhalb des Laborbereichs** (Befund des Labors) und **4 nur ausserhalb des Optimalbands** (Empfehlung aus der Literatur) — zusammen die **6**, die der Filter „Non-optimal only" zeigt, mit der Gleichung als Unterzeile ausgeschrieben. **Das Wort „Alert" ist weg:** es sagt „etwas stimmt nicht", das waere ein Urteil neben einer Zeile, die „no diagnosis" schreibt. **Der `Health score` ist ersatzlos aus dem Kopf entfernt, nicht blass gestellt** — **nur 23 von 37 Markern sind gruppierbar, 47 von 140 Werten fielen heraus**. Die 14 zerfallen in **fuenf blosse LOINC-Abweichungen** (Glucose `1558-6`↔`2345-7`, LDL `13457-7`↔`2089-1`, Hematocrit `4544-3`↔`20570-8`, Vitamin D `14635-7`↔`1989-3`, Magnesium `2601-3`↔`2614-6` — bei Glucose und Vitamin D messen die Paare aber **nicht dasselbe**) und **neun echte Luecken** (ApoB, PSA, Zink, FSH, IGF-1, Prolaktin, Calcium + 2 Rohmarker ohne Code). **ApoB fehlt und LDL haengt an einer Codeabweichung** — die zwei Marker des kardiovaskulaeren Risikos. Dazu ist die **Gewichtung ungetroffen**: `.25/.25/.20/.15/.15` steht nur in `daten.ts`, und die **8 Kategorien der Datenbank passen nicht auf die 5 Systeme** des Entwurfs. **Datenbefund: Glukose steht zweimal in der Liste** — ein Rohmarker **ohne LOINC** faellt auf Namensschluesselung zurueck, also zaehlt eine Messung von 102 mg/dL doppelt. Nicht zusammengelegt, das ist eine Identitaetsentscheidung. **`Tracking`/`Insights` bleiben Attrappe:** fuer Symptome gibt es **keine Tabelle im ganzen Schema**, fuer Korrelationen und Supplement-Wirksamkeit keine Bruecke, fuer den Bevoelkerungsvergleich keine Verteilung (`population` 560× `general`). **Aber `medical.user_medications` existiert (2 Zeilen, 124 Produkte)** — die Sperrbegruendung am Knopf „Add medication" nennt eine Tabelle, die es unter anderem Namen laengst gibt; nur die Ueberwachungsspalten (`next_due`) fehlen, aus denen vier der sechs Alert-Eintraege stammen. **Messfehler der eigenen Arbeit korrigiert:** die erste SQL-Zaehlung ergab 1+5 statt 2+4, weil sie nur numerische Spalten verglich — `befund.ts` zerlegt auch **Textbereiche** (`<5.7`, `>40`), und die meisten der 560 Zeilen tragen nur Text. Die Anzeige war richtig, die Pruefung nicht; **gezeigt hat es erst die gerenderte Seite.** 393 Tests gruen (vorher 388), 20 Attrappenmarken unveraendert, `test-user` sieht 0 auch bei ausdruecklicher Abfrage der fremden UUID. |
| `135-erfahrungsgrad-und-luecken.md` | **C-140, Codex:** `public.profiles.experience_level` (vier Werte, nullable, Selbstauskunft — nicht `activity_level`, nicht Coach-Autonomy), LOINC-Luecken, Zielhistorie und feste Erwartungen. |
| `136-training-restliche-tabs.md` | **Plan, Volume, Standards und was davon traegt** (G-86). **Zwei Kacheln wurden echt:** „Training readiness" aus `recovery.scores` — der Auftrag hatte recht, *das ist Recovery*, eine Lesefunktion genuegt; **alle fuenf Zeilen der Vorlage sind auf 170 Tagen belegt**, wobei `nutrition_score` auf **allen** 170 gleich 70 ist (Rueckfall `fallback_c123_e9`, blass gezeigt und benannt). Und „Diese Woche" aus `workout_sessions`. **Der Muskelname kommt aus `checkins.soreness`**, nicht aus dem hart gesetzten „chest" des Mockups. **Kein Urteil** — „Good to go" und der Ratschlag bleiben draussen (E3), wie in G-76/G-82. **`Plan` ist NICHT baubar, eindeutig:** von elf Feldern der zwei Kacheln fuehrt `workout_sessions` **keines** (Block, Woche, Phase, Last, Deload, Routine, Vorlage, Herkunft); **`name` traegt 30 Sitzungen mit 30 verschiedenen Namen, jeder genau einmal** — eine Routine ist etwas Wiederverwendbares, hier wiederholt sich nichts. Der Kalender zeigt datierte Sitzungen, `Plan` zeigt die **Struktur darueber** — das ist ein fehlendes Schema, kein Anzeigeproblem. **MEV/MAV/MRV bestaetigt fehlend:** `volume_landmarks`, `strength_standards`, `standards`, `landmarks` melden alle **PGRST205** (existieren nicht, sind nicht gesperrt); die Regel greift dreifach — Tab, Zielband, Einstufung. **HR zones:** `workout_sets` und `workout_sessions` fuehren **keine einzige** HR-Spalte, `resting_hr` und `spo2_pct` sind je **0 von 170**. **Offline sync ist eine Betriebsart, kein Datenbestand** — keine Warteschlangentabelle, und `logged_via` steht auf **101 von 101** Zeilen auf `manual`; **erst die Faehigkeit, dann die Anzeige.** **`status` traegt jetzt, was er zu G-69 nicht trug:** 15 `completed`, 14 `planned`, 1 `cancelled` statt 30x `completed` — die Kommentare in `sitzungen-read.ts` und `tab-verlauf.tsx` sind korrigiert, `absolviert` bleibt bewusst am Datum. Sichtbare Marken je Tab gezaehlt: Today **1**, History/Progression/Standards/Calendar **0**, gesamt **22**. **Die 38 `attrappe`-Marken im Text sind als Mass untauglich** (RUECKFALL-Muster seit G-74). Zeilenschutz: `test-user` sieht 0 Sitzungen, **aber 1.416 Katalogzeilen**. **Drei eigene Messfehler korrigiert**, alle drei keine Befunde: die 1.000-Zeilen-Grenze bei `exercise_muscles` (6.588), `muscle_groups` fuehrt `name` statt `name_de`, und „geplante Sitzungen haben keine Uebungen" war eine abgeschnittene ID-Liste — **alle 14 haben welche**. |
| `137-goals-phase-physique.md` | **Die letzten beiden Goals-Tabs, soweit belegt** (G-87). **Der Leseweg lag schon da:** `goals.phase_am()` waehlt die geltende Phase, `ladePhase` reicht sie seit GO-16 bis in die Ansicht — **der Tab hat die Requisite nur nie angenommen.** Phase engine zeigt jetzt `lean_bulk`/`moderate` aus `goal_phases` (14 Spalten, 5 Zeilen, davon **2 fuer `dev`**), Physique ratios die Quotienten aus `body_circumferences` (**27 Saetze fuer `dev`, alle 13 Stellen belegt**, 14 bis zum Stichtag). Gegen SQL geprueft am 2026-08-19: **Schulter:Taille 1,492 · Taille:Huefte 0,860 · Arm:Bein 0,649**, dazu beide Symmetrien mit Vorzeichen. **Die Einstufungen bleiben Attrappe:** `1.618`, V-Taper und Steve Reeves stehen **ausschliesslich in `theme-v1/uploads/`** — Begleitdateien des Entwurfs, keine Spezifikation; **der Reeves-Wert ist gar keine Rechnung, `daten.ts:438` gibt fest `88` zurueck.** Auch die **Farbe faellt weg**: die Vorlage faerbt eine wachsende Taille rot und einen wachsenden Arm gruen — welche Richtung erwuenscht ist, haengt vom Ziel ab; das Vorzeichen sagt dasselbe ohne Urteil. **Ein Kandidat mit echter Quelle, bewusst nicht gebaut:** Taille:Huefte hat mit WHO 2008 (`<0,90` M / `<0,80` F, im Vorgaengerrepo hinterlegt) als Einzige eine belegbare Schwelle — ein Grenzwert, der Geschlecht voraussetzt, ist eine Produktentscheidung. **Zwei Funde am Bestand:** `phase_am()` liefert **8 der 14 Spalten** — `transitioned_from`, `recommended_next`, `transition_reason` fehlen in der Signatur und werden jetzt nachgelesen (Muster aus G-80), **die Funktion blieb unangetastet, das Schema gehoert Codex**; und die Sperrbegruendung am Knopf „Switch to …" nennt `goals.goal_phases` als fehlend, **die Tabelle gibt es seit GO-07**. **Woche und Restdauer sind gerechnet, nicht gespeichert** (C-119) — `goal_phases` fuehrt weder Woche noch Adhaerenz noch Trend, und das steht als Satz an der Kachel. **Anzeigefehler der eigenen Arbeit, erst im Browser gesehen:** „Woche 12 von 7" — beide Zahlen richtig, die Fuegung sinnlos; jetzt „Woche 12 · 33 Tage ueber das geplante Ende" und „Tage ueberzogen 33". Gerenderte Marken **23 → 14** (Phase engine −5, Physique ratios −4). 411 Tests gruen (vorher 393), Zeilenschutz: `test-user` sieht 0 Phasen, 0 Umfaenge, 0 Zeilen — auch bei ausdruecklicher Abfrage der fremden UUID. **`pnpm gate` steht aus:** der `build`-Schritt bricht in `apps/web/src/app/v2/training/` ab (untracked, paralleler Agent) — `goals/` ist in jedem Lauf fehlerfrei, `next build` baut `/v2/goals` durch. |
| `138-coach-seeds.md` | **C-147, Codex:** Die sechs C-119-Coach-Tabellen waren leer — der Testdaten-Erzeuger legt jetzt `coach.seed` an und verknuepft ihn mit `tom.seed`; `eigenes-konto-fuellen.sql` baut dieselbe Beziehung fuer `dev@lumeos.app`. `test-user` bleibt bewusst ohne Coach-Beziehung. Dazu Phasenspalten und Trainingssatz-Metadaten. |
| `139-coach-rechte.md` | **Permissions und Autonomy im Coach-Modul** (G-90). **Die zwei Achsen stehen in der Datenbank, nicht in der Oberflaeche:** `client_permissions` nimmt Schreibzugriffe nur von `client_id`, `client_autonomy` nur von `coach_id` — **der Coach kann seine Sichtrechte nicht selbst setzen, der Klient nicht seinen Reifegrad.** Genau die Umkehr, die F-04 verlangt (*„Der Coach setzt seine eigene Autonomiestufe — halte ich fuer einen Irrtum"*). **Der scheinbare Widerspruch zwischen F-04 und Toms Vorgabe ist keiner:** *wie autonom der Coach handeln darf* ist `*_auto_apply` und setzt der Nutzer; *wie reif der Athlet ist* ist `client_autonomy` und schaetzt der Coach — im Vorgaengerrepo war beides dieselbe Tabelle. **BLOCKER: `coach` ist nicht fuer PostgREST freigegeben** — `Invalid schema: coach`, dieselbe Meldung wie ein Schema, das es nicht gibt, waehrend `recovery` (170), `training` (1.416) und `goals` (181) im selben angemeldeten Lauf lesen. **Die Tabellen existieren** (C-119, `schema-sollstand.json`); es fehlt die Konfiguration, und die ist kein SQL — **dieselbe Sperre wie bei `training` in G-64, und C-119 hat sie nicht erwaehnt.** Deshalb Leerzustand nach G-65 statt Attrappe, mit der Ursache im Klartext. **Marken im Browser gezaehlt** (A-24): Permissions 7 → **0**, Autonomy 13 → **0**, Proposals → **0**; im Text bleiben sie stehen, weil die Entwuerfe in eigene Rueckfall-Funktionen gewandert sind. **Mockup und Schema fuehren beide sieben Module und nicht dieselben:** `buddy` gegen `body_metrics` — gebaut ist das Schema, `body_metrics` haette keine Spalte. **Der Bestaetigungspfad ist Zustandswechsel, keine Ausfuehrung:** `payload` beschreibt, was geschehen soll, **niemand wendet es an**; `action_log` mit `undo_data` bleibt ungenutzt, und Eintraege koennen ohnehin nur aus dem Coach-Portal kommen. **Nicht erbracht:** Setzen/neu laden und der Zeilenschutz-Nachweis — **beide Konten sehen dasselbe `Invalid schema`.** **Eigener Fehler gemeldet:** `.next` geloescht (alte Auftragsfassung), was den Dev-Server auf 3200 lahmlegte; das Ausweichen auf `LUMEOS_DIST_DIR=.next-g90` loeste dann die Kodierungspruefung des Gates aus — **`.next` und `.next-gate` sind die einzigen zwei vorgesehenen Namen.** |
| `140-supplements-restliche-tabs.md` | **Ein Tab von fuenf war baubar** (G-91). **Zuerst die Namen:** ein Tab `Timing` und ein Tab `Insights` **existieren nicht** — `Timing` ist seit G-37 die Gruppierung in `Today` (`stack_items.timing`, 8/8 gefuellt), `Insights` heisst `Intelligence`. **Gebaut ist `Catalog`:** 44 Eintraege aus `supplement_catalog`, Stufenfilter mit gezaehlter Verteilung (**S 4 · A 13 · B 16 · C 7 · D 4**), Serving, Timing, Preis, „In stack". Marken **22 → 20**, im Browser gezaehlt (A-24) und am Quelltext gegengeprueft. **Drei Behauptungen der Vorlage weggelassen:** das **Gewicht je Evidenzstufe** (S 1.00 … F 0.00) steht nur in `spec-daten.ts` und speist einen Score, den es nicht gibt; die Spalte **„Mode"** hat kein Feld; die Spalte **„Dose"** zeigt `serving_size`, weil `typical_dose_min/max` auf **allen 44 leer** sind — *eine Dosis zeigen ist etwas anderes, als eine zu raten.* Dazu sagt der Tab, dass **`evidence_summary` 0/44** und `evidence_sources` ueberall leer ist: der Buchstabe liegt vor, seine Begruendung nicht. **Der Hauptbefund steckt in `Intelligence`:** der Mikro-Lesepfad **existiert inzwischen** (`nutrition.micronutrient_snapshot`, 8 echte Zeilen) und **7 der 8 Codes** kommen auch in `nutrients_provided` vor — **die Gap Analysis scheitert an einer Einheit.** Vitamin D steht im Supplement in **IU**, im Mikro-Pfad in **µg**; naiv addiert ergaebe das **33.430 %** statt rund 930 %, ein **Faktor 40**. **Ein Umrechnungsfaktor liegt nirgends im Repo** (auch nicht im Vorgaengerrepo) — betroffen ist genau **eine** von 11 Zeilen. Dazu unveraendert: `nutrients_provided` **11 von 44**. **`Stacks` bleibt Attrappe:** 1 von 4 Kacheln haette Daten (`user_stacks`, fuer `dev` **1 Zeile**), `stack_templates` existiert nicht, `cycling` ist auf allen 8 Positionen leer — **und die Kachel behauptet einen „DB EXCLUDE constraint", den `pg_constraint` nicht kennt**, waehrend zwei Zeilen auf `is_active` stehen. **`Extended` bleibt Attrappe:** `enhanced_substances` **existiert nicht**, und **`experience_level` ist auf allen 5 Profilen NULL** — der Tab zeigt trotzdem etwas, weil sein Gate ein blosses `useState` ist und **nichts schuetzt**. `pnpm gate` 8/8, **418 Tests gruen** (vorher 411), Zeilenschutz: `test-user` sieht **0 Stacks, 0 Positionen, 0 Einnahmen — aber 44 Katalogzeilen**, weil der Katalog kuratierte Referenz ist. **Eigener Fehler gemeldet:** zweimal `next build` ohne `LUMEOS_DIST_DIR` (also in `.next`) neben dem laufenden Dev-Server plus dreimal `.next` geloescht — genau der Schaden, vor dem B-18 warnt; die Trennung ist nachgeprueft intakt, der Gate-Lauf ist mit `pnpm gate` gemacht. **Nebenbefund:** `tools/encoding-pruefen.mjs` prueft 3.846 Dateien und **nimmt `.next*` nicht aus** — fuenf `U+FFFD` in erzeugten Chunks unter `apps/web/.next-g90/` (fremdes Build-Verzeichnis) faerben das Gate rot, obwohl keine Quelldatei betroffen ist; seit B-18 jeder Agent in ein eigenes `.next-<auftrag>` baut, trifft das kuenftig jeden. Bildschirmfotos fehlen (geteilte Browsersitzung dauerhaft blockiert). |
| `143-rezepte-und-plaene.md` | **C-150, Codex:** Rezepte, Wochenplaene und Portionen — Struktur aus dem Vorgaengerrepo uebernommen, **aber nicht die zweite Naehrwert-Wahrheit** des alten Planmodells (`meal_items` friert erst beim Erfassen ein). Zeile vom Orchestrator nachgetragen, Inhalt nicht gegengeprueft. |
| `144-erfassungsdialog.md` | **Die Vorlieben gelten jetzt beim Erfassen** (G-13). **Der Auftrag nannte die falsche Datei:** `erfassen.tsx` (477 Zeilen) hat **null Importeure** — der laufende Dialog ist `HinzufuegenModal` in `mahlzeiten.tsx:410`. **`p_user_id` fehlte an drei Stellen, nicht an einer** (Argumenttyp, Lesefunktion, Route); **die Kennung kommt aus der Sitzung, nie aus der Anfrage** — ein durchgereichter Parameter haette aus dem Unterschied der Trefferzahlen fremde Allergien verraten. **Gemessen fuer `dev@lumeos.app`:** „mandel" **64 → 0**, „erdnuss" 10 → 0, „nuss" 125 → 42; Ursache ist `hard_exclude` auf `contains_nuts`, und **63 der 64 Mandel-Treffer tragen den Tag** — die Null ist richtig, nicht kaputt. **Weil eine leere Liste sonst wie ein Datenbankfehler aussieht**, meldet die Route `preferences_applied` und `preferences_hidden`; der Dialog schreibt *„Kein Treffer — 64 Eintraege sind durch deine Vorlieben ausgeblendet."* Der Zaehler kostet einen zweiten Aufruf und laeuft **nur bis 5 Treffer** (`total` ist limitunabhaengig, deshalb `p_limit: 1`). **Vier Filter, 4/4 gegen SQL:** vegetarian 1.751, vegan 1.377, high_protein 1.400, whole_food 2.884; zwei Sortierungen (Relevanz vs. Protein: 0,2 g gegen **35,6 g** bei „kaese"). **Bewusst weggelassen:** 13 Kategoriepillen, fuenf Tag-Filter, `kcal_asc`/`name_asc` und der Allergen-Ausschluss aus G-73 — **letzterer waere hier der schlechtere von zwei Wegen**, weil C-94 dasselbe ueber alle 7.140 leistet statt nur auf der geladenen Seite. **Der Katalog bleibt ungefiltert:** Vorgabe AUS, `prefs=1` schaltet ein — drei der vier Aufrufer sind Kataloge, und bei der **leeren** Anfrage der Startliste schloesse `food_search` zusaetzlich `strong_avoid` aus (075:455). Zeilenschutz: `test-user` sieht **64/125**, wo `dev` 0/42 sieht. **Gate 5/7, die zwei roten Tests gehoeren nicht hierher** — sie lesen nur `supplements/tabs.tsx`, dessen uneingecheckter Arbeitsstand **0 statt 16** `RUECKFALL`-Marken hat. **Nachweis ohne Fenster** (Tom, 2026-08-20): gstack gestoppt, `tools/lauf.py` um `npx`/`pnpm`/`node`/`hole` und `tools/schuss.mjs` um `--klick`/`--tippe` erweitert statt danebengeschrieben; die HTTP-Anmeldung braucht `sb-127-auth-token` als **JSON, nicht `base64-`** (@supabase/ssr ist 0.1.0). **Neuer Befund:** der Anzeigename fehlt in der laufenden Fassung an **drei** Stellen (750/766/535) — **5.014 von 7.140** Eintraegen zeigen damit den amtlichen statt des lesbaren Namens; erledigt war das nur in der toten Datei. |
| `145-schema-freigabe-pruefung.md` | **A-26, Codex:** `coach` fehlte in `supabase/config.toml` — die Tabellen lagen da, PostgREST meldete trotzdem `PGRST106`. **Ein Container-Restart genuegt nicht**, PostgREST liest die Schemaliste beim Start. Neu: `tools/schemafreigabe-pruefen.mjs` im Gate. Zeile vom Orchestrator nachgetragen, Inhalt nicht gegengeprueft. |
| `146-coach-portal.md` | **Das Coach-Portal laeuft** (F-07). **Schema 6 → 12 Tabellen** (151-154: Beziehung `invited → active → ended` mit Ende und Trigger-Historie von Anfang an, Check-ins mit Statusmaschine als CHECK, Nachrichten mit `sender` per CHECK ans Paar gebunden, Alerts **bewusst ohne Schweregrad**). **Der Lesepfad setzt die Freigabe in der Datenbank durch:** eine Sichtregel (`coach.hat_sicht`), 22 `coach_read`-Policies auf den Nutzerdaten der sechs Module, 6 summary-DEFINER-Funktionen (Medical: nur Existenz und Datum, nie Werte) — `coach-lesepfad-pruefen.sql` **besteht mit 9 Punkten auf Wegwerf-DB UND live**: full liest (725 Mahlzeiten, 101 Saetze ueber Doppel-Join), summary liefert 0 Zeilen + Aggregat, none nichts, Dritter nichts, Eigentuemer unversehrt. **`apps/coach` auf Port 3220**, Admin-Vorbild, eigene Sitzung `sb-127-coach-auth-token` (gemessen); Anmeldung `coach@lumeos.app`; Nicht-Coach bekommt Absage. **8 von 16 Tabs datengetragen** (Overview-Arbeitsliste, Athleten, Alerts, Autonomy-Schreibformular, Consent mit Historien, Check-in-Queue mit Ein-Bildschirm-Review und deterministischen Befunden ohne Urteil, Onboarding, Messages) plus Klientenakte je Sichtstufe; **8 Leerzustaende mit Ursache** (T7/E8/E4/T8 ...). **Wirkung gemessen:** Autonomy-Formular → `autonomy_change_log` 3 → 4 (der ssot/139-Nachweis), Review → `reviewed` 1 → 2. **68 Bilder, 0 Attrappen, 0 Konsolenfehler.** Kette End-zu-End Exit 0 (80 Schritte), Schema vollstaendig, Testdaten + Pruefung gruen. **Zwei @supabase/ssr-0.1.0-Befunde** (Browser-Client crasht bei `cookieOptions` ohne `cookies`; `storageKey` nur im ServerClient — **admin waere zu pruefen**), Umgehung lokal in `browser-client.ts`, `packages/shared` unveraendert. **Gate: alle Pakete gruen ausser `@lumeos/web`** — dessen Typfehler stammt aus dem uneingecheckten G-13-Stand (`TrainingKachel.prs`), nicht aus diesem Auftrag. **Beinahe-Unfall gemeldet:** `testdaten-einspielen.ts` ignoriert unbekannte Flags, `--database` lief gegen live — folgenlos (eine Transaktion, Rollback gemessen), Folgepunkt angelegt. Kein Ausfuehrer (bewusst, eigener Auftrag); T1-T9 weiter offen, konservative Varianten markiert. |
| `147-meal-plans.md` | **Der Attrappen-Test, der Planner und die acht Spalten** (G-97). **Teil A — der Test war richtig, die Datei war falsch:** `supplements/tabs.tsx` lag als **ganzer Stand VOR G-91** im Arbeitsverzeichnis — nicht nur 16 Marken zurueckgedreht (`ATTRAPPE` statt `RUECKFALL`), sondern auch der Import von `CostErgaenzung` geloescht und **`Serving` wieder zu `Typical dose`**, also genau die Spalte, die G-91 entfernt hat, **weil sie auf allen 44 Eintraegen leer ist**. Stand **gestasht statt verworfen**; danach 1 + 16, Marke je Fassung richtig, `SuppInteractions` behaelt `ATTRAPPE`, **76 Tests gruen**, gerendert **1 statt 17**. C-150 hatte denselben Befund und richtig zugeordnet. **Teil B — der Planner liest echt:** Plan „Aufbau-Wochenplan" mit **2.500 kcal / 170 g P**, drei Wochen (gefuellt 28, leer 0, kopiert 28), angezeigt **18.6.–24.6. mit 28 kcal-Zellen**, Marken im Tab **0**. **Die kcal sind gerechnet, nicht gewuerfelt** (`recipe_nutrition` und `food_nutrient_snapshot` aus C-150) — sichtbar an der Skalierung: 493 kcal bei 1×, **617 bei 1,25×**. Hervorgehoben ist **heute** statt des fest verdrahteten Samstags; leere Zellen bleiben leer. **Kein Generator** — `Copy week` traegt die Entwicklungsmarke, weil die Funktion in der Datenbank steht, der Schreibpfad im Browser aber fehlt. **Die acht G-72-Spalten: zwei wirken** (`meals_per_day`, `snacks_per_day` bestimmen die Rasterzeilen), **drei sind sichtbar vergleichbar** (`cooking_skill`, Zeit, Kueche stehen an jedem Rezept), **drei bleiben wirkungslos** — `recipes` fuehrt **kein Preis- und kein Vorkochfeld**. **Eigener Fehler beim Messen gefunden:** die erste Fassung zog Snacks von `meals_per_day` ab und schrieb *„4 Mahlzeiten, davon 1 Snack"* ueber vier Reihen; die DB-Vorgaben (`meals 3` UND `snacks 1`) sagen, dass **getrennt gezaehlt wird** — 6 Tests halten es fest. **Der zweite Tab: `MealPlansView` IST definiert** (`module-nutrition-spec.jsx:334`) — Auftrag und C-150 sagten beide das Gegenteil, **beide suchten in der falschen Datei**. Ihm fehlt dennoch das Wesentliche: **`meal_plan_entries` hat keinen Zustand** (confirmed/deviated/skipped) und **`meals`/`meal_items` keine Herkunft zum Planeintrag** — damit ist Compliance **nicht ableitbar, auch nicht naeherungsweise**. Einzig die Einkaufsliste waere ohne Schemaaenderung baubar (**72 von 112** Eintraegen haengen an Rezepten mit Mengen). **HTTP 500 bei gruenem Typecheck erneut ausgeloest** — Wert-Import aus einer `'use client'`-Datei zog `next/headers` ins Browserbuendel, wie in G-74 und G-79; behoben durch `plan-model.ts` ohne Serverbezug. Gate fuer `@lumeos/web` **3/3 gruen**; `pnpm gate` als Ganzes rot an **`apps/coach`** (Fables neues Paket, untracked, kein `node_modules`, nicht im Lockfile). Zeilenschutz ueber **vier Ebenen**: `test-user` sieht „Noch kein Wochenplan". |
| `148-regelwerk.md` | **C-133, Codex:** Warn-, Gap- und Medikamentenregeln in der Datenbank. Zeile vom Orchestrator nachgetragen, Inhalt nicht gegengeprueft. |
| `149-dashboard.md` | **Das Dashboard liest jetzt aus allen sieben Modulen** (G-100) — es war das einzige nie angebundene Modul und die erste Seite, die ein Nutzer sieht. **Attrappen gerendert 11 → 6.** Angebunden mit gemessenen Werten fuer den 2026-08-20: Recovery **83,9** (Schnitt der sieben Tage davor **80,7**), Schlaf **7,8 h**, Kalorien **2.727/2.500**, Training **15 absolviert · 14 geplant**, **3 von 41 PRs**, Medical **2 ausserhalb Labor · 5 nur ausserhalb Optimal**, Supplements **93,5 %**. **Nichts neu gerechnet** — Medical laeuft ueber dieselbe Kette wie sein Modul (G-84), die Tagesziele ueber `getZielwerteAm`. **Der Stichtag ist entscheidend, nicht die juengste Zeile:** die Seeds laufen bis 2026-11-06, `ladeScores()` haette einen Novemberwert als „heute" gezeigt. **Die erfundenen Zahlen sind aus dem HTML verschwunden** („2,142", „1,847", „Streak 23d" — 0 Treffer). **Zwei Urteile gemeldet statt gebaut:** der Readiness-Komposit (fuenf Anteile des Entwurfs gegen **sieben** in `recovery.scores`, Gewichtung nirgends entschieden — zwei Gesamtwerte waeren schlimmer als einer) und *„Push hard / green light"*. **Body battery gibt es im Schema nicht** — keine Spalte `batter*` in irgendeinem Schema. **Zwei eigene Fehlannahmen beim Messen korrigiert:** „Tonight" ist zum grossen Teil baubar (`started_time`, `duration_minutes`, `location` sind auf allen 30 Sitzungen gesetzt — nur Block/Woche/Coach fehlen, und `total_sets` ist bei Plaenen 0), und beim Tagesverlauf fehlen nicht die Uhrzeiten (**725/725, 360/360, 30/30**), sondern die **Dauer je Ereignis**. **Zwei Zahlen des Auftrags stimmen nicht mehr:** Medical ist **2+5**, nicht 2+4 (das Modul selbst schreibt „2 + 5 = 7"), und Recovery hat **4 Konsolenfehler**, nicht 5. **Die zwei SVG-Fehler sind kein Encoding-Problem** (0 U+FFFD in der Datei), sondern **abgeschnittene Pfaddaten**: ein `C` mit vier statt sechs Zahlen, ein zweites mit zwei — unveraendert seit `3dae1a2`, nicht behoben weil `packages/ui` gesperrt (G-102). Zeilenschutz: `test-user` sieht ueberall `—` und 0; **die Makroziele sieht er trotzdem, aber seine eigenen** (2.978 aus seinem Profil gegen 2.500 bei `dev`) — kein Leck, eine Formel. **424 Tests gruen**, Build erfolgreich (Route 2 kB → 4,47 kB); Gate als Ganzes rot an `apps/coach` (Fable). **Der Dev-Server hing** und wurde nach Freigabe in der Reihenfolge aus CLAUDE.md neu gestartet — **`.next` nicht geloescht**, Ursache vorher geprueft (kein fremdes Build-Verzeichnis). |
| `151-recovery-und-seeds.md` | **C-153, Codex:** Recovery und Seeds. Zeile vom Orchestrator nachgetragen, Inhalt nicht gegengeprueft. **`150` ist nicht vergeben** — die Nummer wurde uebersprungen. |
| `154-preferences.md` | **Preferences speichern, bedienen, wirken** (G-104). **Es speicherte zum Messzeitpunkt bereits** — Toms Befund traf den Stand vor C-156; gemessen haelt jede der sechs Kacheln setzen → neu laden. Zwei echte Bugs gefunden und zu: **Klick-Kreuzung** (schnelle Klicks sendeten den Stand VOR dem vorigen und loeschten ihn — jetzt synchroner Ref, serialisierte Laeufe, nur die juengste Antwort gilt) und **Daumen-Zeilen waren ueber die Kachel unloeschbar** (C-156 loescht nur `settings`; die Zeile kam nach dem Reload wieder — jetzt Herkunftsweiche auf den Daumen-Weg, nachgemessen). **Ein Klickmuster statt drei:** tippen schaltet weiter, der Wert steht AM Element (Zyklusknopf `—/+50/−50` statt vier unbeschrifteter Knoepfe; Tags mit ±30 im Knopf, ⊘ jetzt gesperrt — vorher machte ein Klick aus dem Allergie-Ausschluss ein „mag ich"). **Individual foods nimmt selbst auf:** Suchfeld in der Kachel (bewusst ohne Vorlieben-Filter), Treffer = +100, Zyklus, Papierkorb, Daumen-Pill je Herkunft. **Daumen ueber die RPC? Nein, begruendet:** Satz-Vertrag; ein Einzelzeilen-Aufruf loeschte alle settings-Zeilen — zwei Schreibwege unter einer Bedienung, per Test festgehalten. **Wirkung gemessen:** `mandel` 64 → 0 (verborgen 64); Kategorie −50: „Honig" (SUESSES) faellt von Rang 1 aus den Top-8, „Honigmelone" (OBST) uebernimmt. **`schokolade` 163 → 1 ist KEIN Fehler:** der Seed-Ausschluss `ultra_processed` filtert ueber die Tag-Schiene hart (075:382-390) — korrekt, aber an der Pille nicht ablesbar (Folgepunkt). **Veralteter Hinweis ersetzt** („wirkt noch nicht in der Suche" — falsch seit C-94); der Waechter-Test, der ihn festschrieb, prueft jetzt die neue Aussage. Planner/Rezepte: kein Ansatzpunkt (keine Lebensmittelsuche darin); MealCam: kein Modell; Suchseite/Kataloge bewusst ungefiltert (Bewertungsort). **`source` ueberlebt drei Kachel-Laeufe** (search_thumb-Zeile unveraendert). Zeilenschutz: test-user sieht nichts von dev, `mandel` dort 64. Tests **441/441**, Tab rendert **0** Attrappen (die eine gezaehlte Marke ist der Buddy-Orb der Schale). |
| `152-nutrition-feinschliff-2.md` | **Filter, Naehrstoffordnung, Diary und Insights** (G-101). **Der Food-DB-Filter griff die ganze Zeit** — gemessen in drei Schichten (Funktion, Route, Browser: je 7.140 → 1.400), und Avocado verschwindet aus der Liste. **Was fehlte, war die Anzeige:** bei geschlossenem Filterband kam das Wort „Proteinreich" auf der ganzen Seite **null Mal** vor, sichtbar war nur eine kleine `1` am Knopf. Jetzt steht „Gefiltert nach · Proteinreich ✕" ueber der Liste, je Filter abwaehlbar. **Die Naehrstoffordnung (C-54) liest jetzt `display_tier`:** 138 Naehrstoffe in 12 Gruppen statt der **79 erfundenen Eintraege in 8 Gruppen** des Entwurfs; alle elf Kohlenhydrate mit Einrueckung, `Zucker gesamt` auf Stufe 1. **Die Falle steckte in den Daten:** `SUGAR` hat `sort_index` **73**, seine Kinder beginnen bei 65 — ein einzelner Durchlauf haette die ersten sechs verloren, **fuenf Tests halten es fest**. Ehrlich dabei: **33 von 138** tragen fuer den Tag einen Wert. **Diary:** `micronutrient_snapshot` und `micronutrient_below_threshold` angebunden — der Auftrag nannte den Namen falsch, aber anders als vermutet (`micronutrient_overview_items` ist die **Konfiguration**, 8 Zeilen; die Funktionen heissen wie gesucht). **Vitamin C zeigt bewusst keinen Prozentwert** (`reference_status = incomplete`), und die 80 % heissen an der Kachel **Anzeigegrenze, keine medizinische** — der Entwurf faerbte alles `warn`, das waere ein Urteil. **Hydration: 100/250/500 ml plus freie Eingabe**, gegen die Route geprueft (4× HTTP 201, Testdaten wieder entfernt); `WATER_QUICK_AMOUNTS_ML` bleibt unangetastet, weil es aus SPEC_04 stammt. **Insights:** Kalorienbilanz **−47,4 kcal/Tag** (2.487,4 gegen 2.534,8, Konfidenz hoch, `alpha = 1,0` angezeigt) und Makroschnitt **27,7/47,6/24,7 %** — **ohne die „Target ratio 28/47/25" des Entwurfs**, die nirgends entschieden ist. **Micronutrient trend nicht gebaut:** die Werte liegen je Tag vor, aber `daily_reference_assessment` rechnet **einen Tag auf einmal** — 30 Aufrufe je Seitenaufruf waeren eine Entscheidung (G-107). **Zwei eigene Aussagen berichtigt:** `display_tier` wird sehr wohl gelesen, nur in der aelteren Flaeche `/nutrition/local-schema` (9 Dateien), und `nutrient-baum.ts` hat zwei Aufrufer, ist also kein toter Code. Attrappen gerendert **Diary 7 → 5**, Nutrients 1, Insights 4, Food DB 1. **434 Tests gruen** (vorher 424). `tools/schuss.mjs` um `--zaehle`/`--zaehleSel` erweitert — die feste Wortliste aus G-13 stand jedem anderen Auftrag im Weg. **Der Dev-Server hing zum zweiten Mal an einem Tag** (G-109). |

| `153-vorlieben-schreibweg.md` | **C-156, Codex:** Vorlieben-Schreibweg und Datenluecken. Zeile vom Orchestrator nachgetragen, Inhalt nicht gegengeprueft. |
| `155-substanzkatalog.md` | **C-134, Codex:** Konsolidierter Substanzkatalog. Zeile vom Orchestrator nachgetragen, Inhalt nicht gegengeprueft. |
| `156-supplements-interactions.md` | **Interactions liest das Regelwerk, und das Gate schuetzt wirklich** (G-110). **G-91 hatte `Interactions` als blockiert gemeldet — C-133 hat es aufgehoben:** 64 Regeln, `rule_assessment` mit drei Zustaenden. Gemessen fuer `dev`: **1 zutreffend, 50 nicht, 13 ohne Daten**. `wr_anticoag_stack` zeigt seinen Treffer im Klartext (`anticoagulant_vka`, `CYP2C9_substrate`), `wr_lab_biotin` nennt beide fehlenden Pfade. **Die Grenze steht im Text der Kachel:** *„Hinweis, keine Bewertung — LumeOS gibt keine Dosierung vor, sperrt nichts und rechnet daraus keine Note.“* Kein Score, keine Blockade, keine Zeitplan-Urteile; `physician_referral` (33 Regeln) erscheint als Wort, nicht als Handlung. **Alle 13 `missing_input` stehen einzeln da** — der Kern von C-132: *„weder zutreffend noch ausgeschlossen“*. **Sieben der zwoelf fehlenden Pfade haengen an Quellen, die es nicht gibt** (kein Schema `sleep`, kein `location`, keine Symptomtabelle, kein `training.high_impact`) — die Landkarte dessen, was fehlt. **Das Gate von Extended war ein `useState`** (G-92: *„Wer klickt, sieht die Protokolle“*); es faellt jetzt **serverseitig** gegen `profiles.experience_level` (C-140, auf allen 7 Profilen NULL). **Stufe fuer Stufe gemessen:** NULL und `beginner` → Gate, ab `advanced` → Protokolle; der Grad wurde danach wieder auf NULL gesetzt. **Das innere Gate ist entfernt**, die Settings-Kachel entsperrt (sie stand seit G-80 mit *„gibt es kein Feld dafuer“* — das stimmte bis C-140). **Ein `dynamic({ssr:false})` ist zurueckgenommen:** es hielt den Code aus dem Buendel, machte den Tab aber leer (G-117). **Drei Zahlen des Auftrags berichtigt:** die sieben Arten sind `rule_kind`, nicht `rule_type` (der hat drei); und der Attrappen-Test steht bei **16/1**, nicht 18/2 — die 18 sind `tab-spec.tsx`. **441 Tests gruen** (vorher 434), Attrappen gerendert **1**, Zeilenschutz: `test-user` sieht den Katalog, aber **keine zutreffende Regel und keine Merkmale**. |
| `157-tagesbilanz.md` | **C-157, Codex:** Die Tagesbilanz in langer Form — `daily_nutrient_summary_long` (je Nutzer/Tag/Naehrstoff, mit `value_count`/`missing_count`/`value_complete`) und `nutrient_summary_window(user, stichtag, tage)` mit Summe und drei Schnitten. Loest das 37-von-138-Problem der breiten `daily_summary` (74 Spalten, je Code eine Migration); dazu leere `assessment_horizon_*`-Spalten auf `nutrient_defs` (C-160, bewusst ungefuellt). |
| `158-tab-zustand.md` | **Tab in der Adresse, Wasser-Historie, Naehrstoff-Klappen** (G-117). **Der Taeter am „Tagwechsel springt auf Diary" war die Datumsnavigation** — `router.push('?datum=…')` warf alle uebrigen Parameter weg; `?tab=insights` selbst lieferte zum Messzeitpunkt laengst Insights (G-111-Messung nicht mehr reproduzierbar, plausibel der haengende Server aus G-109). **Eine Stelle fuer alle Module:** `lib/tab-url.ts` als Drop-in fuer `useState` — sechs Module umgestellt (Training/Recovery/Goals/Medical/Coach/Coach-AI), Nutrition hatte das Muster, Dashboard hat keine Tabs, **`supplements` haelt den Tab weiter in `useState` — fremder Bereich, gemeldet** (dieselbe Zwei-Zeilen-Aenderung). Gemessen: `?tab=history` direkt ✓, Klick schreibt `?tab=library`, Neuladen haelt; Tagwechsel → `?tab=insights&datum=…`. **Wasser-Historie:** der Servercode lag KOMPLETT vor (Codex: `listOwnWaterLogs`, `removeWaterLog` mit G-79-Nullzeilen-Pruefung, sogar `updateWaterLogAmount`) — es fehlten HTTP-Verb und Oberflaeche. Jetzt: `GET ?liste=1`, `DELETE ?id&datum` mit Tag+Restliste als Antwort; Kachel-Liste (Zeit · Menge · Quelle) mit **benennender Abfrage** („750 ml von 20:00 loeschen?"). Kreis gemessen: 2 → 3 → 2 → Neuladen 2; test-user: eigene Liste 0, fremdes DELETE **404 NOT_FOUND**, devs Zeilen unveraendert. **Naehrstoffgruppen:** Klappmechanik gab es seit G-101, der Standard war „erste offen" — jetzt **12/12 zu**, Klick oeffnet die 36 Fettsaeuren-Zeilen; **Zeitfilter-Platz markiert, Filter bewusst nicht gebaut** (C-157). Ungenutzt gemeldet: `updateWaterLogAmount` (Menge korrigieren statt loeschen+neu). Werkzeugfund: `schuss.mjs`-Pfade ohne `?` fallen der MSYS-Pfadumwandlung zum Opfer — `MSYS_NO_PATHCONV=1`. Tests **441/441**. |
| `159-naehrstoffcodes.md` | **C-158, Codex:** Welchen Naehrstoff liefert ein Supplement — 17 belegte Zuordnungen in `supplement_nutrient_mappings` (von 111 Kandidaten aus 567 Substanzen; der Rest offen, weil Menge, Form oder Aufteilung fehlt), Tageslesepfad `supplement_nutrient_intake_for_day` summiert nur Genommenes (dev am 19.8.: FAPUN3 2 g, MG 400 mg, VITD 125 µg). |
| `161-naehrstoffanzeige.md` | **138 statt 37, mit Zeitfenstern** (G-121). Der Nutrients-Tab liest die lange Form: **STARCH 188,1 g aus 11 von 14 Positionen** statt Strich, ALC und alle 6 Ballaststoff-Arten da, Vollstaendigkeit je Zeile. **Fenster 1/7/14/30/45/60/90 in der Adresse** (`?fenster=`), ein Pfad ueber `nutrient_summary_window` (30 T = 35,4 ms, 90 T = 91,8 ms); Tag zeigt die Summe, Fenster den **Schnitt je protokolliertem Tag** — angeschrieben, Fenstersumme als Hinweis. Mockup-Spalten (Ziel · Fortschritt · % · Status als Aussage ueber die Zahl; Referenz aus `daily_reference_assessment`, 31 von 138) und Scope-Filter mit der Vorlagen-Regel **Eltern bleiben sichtbar, wenn ein Kind auffaellig ist** (gemessen: FASAT bleibt wegen Linolsaeure 94 %). Detail-Modal NICHT gebaut (wartet auf C-161/`parent_code`), Andockpunkt `waehlen` liegt bereit; der 14-Tage-Trend der Vorlage ist dort erfunden und dockt spaeter echt mit an. Zeilenschutz doppelt belegt (UI + Funktion: fremde UUID → 0 mit Wert). Tests **447/447**. |
| `162-recovery-rest.md` | **Die Konsolenfehler sind vollstaendig zugeordnet, und 17 Sperrbegruendungen zeigten in die falsche Richtung** (G-123). **Recovery hat vier Fehler, nicht fuenf** — je Tab gemessen (seit G-117 moeglich): **alle neun Tabs haben 2 Hydrationswarnungen**, die drei mit Muskelkarte (`today`, `checkin`, `muscles`) dazu **die 2 bekannten SVG-Fehler** (G-105). **Die Spalte „sonst“ ist ueberall null — es gibt nichts Drittes.** `[read]` Ein eigener Pfadpruefer meldete **291** Fehler und war falsch: SVG erlaubt wiederholte Parametersaetze ohne Befehlsbuchstaben. **Fuer SVG ist der Browser die Messung.** **`next_day_score_delta` (C-153) war gefuellt und wurde nicht gezeigt** — die Kachel las `next_day_effect`, die Selbsteinschaetzung. Jetzt steht der gemessene Unterschied je Art: **Sauna +2,76**, Dehnen +0,13, Massage +0,05, **Eisbad −0,07** — ohne Urteil, mit dem Rechenweg darunter. **`supplements` war das letzte `useState`-Modul** (G-119); `?tab=` steuert jetzt alle elf Tabs, fuenf davon gegengeprueft. **Die zwei Sperrbegruendungen des Auftrags waren siebzehn:** **neun nennen eine Tabelle, die es gibt** (`user_medications` 2 Zeilen, `goal_phases` 5, `user_goals` 11 **mit Schreibweg**, `body_measurements` 362, `lab_result_values` 280, `modality_log` 178, `checkins` 340, `exercises` 1.416) — zwei behaupteten sogar, **das Schema `recovery` gebe es nicht**, auf einer Seite, die 170 Tage anzeigt. **Acht weitere stimmen halb:** die Tabelle fehlt wirklich, aber der Zusatz „das Schema gibt es noch nicht“ ist falsch (`medical` 11 Tabellen, `training` 8, `recovery` 3) — **und zweimal hiess es, `/v2/medical` gebe es nicht.** `[read]` **Die Sperren bleiben alle richtig** — gemessen fehlt jedes Mal der SCHREIBWEG, nicht das Schema; der Unterschied schickt den naechsten Agenten zu Codex statt in `apps/web`. **447 Tests gruen**, Attrappen Recovery **4**, Supplements **1**. Zeilenschutz: `test-user` sieht kein +2,76 und **6 statt 4 Attrappen** — ohne Modalitaeten faellt die Kachel auf den Entwurf zurueck. **G-120 nur gemeldet** (`nutrition` gehoert Fable). |
| `163-shared-einstieg.md` | **Der Build war heil, und die vorgeschlagene Marke haette nichts gemessen** (A-30). Der Auftrag sagt, `next/headers` gerate ueber `packages/shared` ins Browserbuendel und der Build breche — `[cmd]` **er bricht nicht**, 0 Treffer in 69 Logzeilen. **Der Einstieg ist schon geteilt:** `.` erreicht 4 Dateien und **kein** `next/headers`; nur `./session` (2) und `./auth` (4) ziehen es, beides Servereinstiege. **Die Ursache lag jedes Mal in `apps/web`** — ein WERT-Import aus einer Datei mit `@lumeos/shared/session` in eine `'use client'`-Datei; `import type` ist folgenlos, `import` ohne `type` schleppt das Modul mit, **und der Typecheck sieht beides als gueltig**. Nachgestellt in `rechte-echt.tsx`: derselbe Bruch, dieselbe Kette ueber `rechte-read.ts`. **Die Trennungen (`rechte-modell.ts` neben `rechte-read.ts`, `plan-model.ts`, `reihe.ts`) sind keine Umgehungen, sondern die Loesung** — faellt eine weg, kommt der Fehler zurueck. **Auf `next/headers` zu pruefen misst nichts:** die Zeichenkette steht in **0 von 104 Serverdateien und 0 von 49 Client-Chunks**, Next loest sie beim Buendeln auf. Geprueft wird auf `createServerClient` (3/0) und `cookies()` (4/0); `createBrowserClient` (1/1) scheidet aus. **`tools/serverimport-pruefen.mjs` haengt nach dem Build im Gate**, beide Richtungen gemessen (sauber gruen, Fehler eingebaut Exitcode 1) — und **eine dritte gegen Stumpfwerden**: faellt eine Marke serverseitig auf 0, bricht sie ab. `[read]` Ein gebrochener Build raeumt `.next-gate` weg, die Pruefung meldet dann „kein Build" statt den Fund — ebenfalls Exitcode 1, die Meldung nennt den Fall. **Gate 11/11, 447/447 Tests**, Anmeldung auf beiden Seiten ohne HTTP 500. **Nichts in `packages/shared` und `apps/web` geaendert.** Offen: der Anlass zu A-30 bleibt unerklaert — gemeldet wurde ein Bruch, gemessen ein heiler Build. |
| `164-naehrstoffbaum.md` | **C-161, Codex:** Der echte Naehrstoffbaum — `nutrient_defs.parent_code` (40 Wurzeln, 98 Kinder; CLD/S/CU/MN als Wurzeln korrigiert, RETOL/CARTB unter VITA mit Formelgewicht 1/6), Prueffunktion `nutrient_tree_value_anomalies` (0 Treffer), **110 Erklaerungen** aus dem Vorgaengerrepo in `nutrition.nutrient_details` (dreisprachig; `rda_*` bleibt Text — weicht von EFSA ab), `daily_reference_assessment` liest jetzt die lange Form (**138 Codes, 45 mit Prozent**; FAT/CHO bewusst ohne — GO-00), und **`public.user_display_preferences`** (user_id + key + jsonb, RLS 4 Policies) fuer gespeicherte Ansichten. Offener Datenbefund: `NIA` Stufe 1 unter `NIAEQ` Stufe 2. |

| `165-svg-und-wasser.md` | **Die Rueckenfigur hatte seit vier Monaten gar keinen Umriss — und der Stift lag fertig da** (G-124). `[cmd]` **Nicht zwei abgeschnittene Kurven, sondern 42 % fehlende Pfaddaten:** `UMRISS_VORNE` zeichnete **939,8 von 6.275,8** Laenge, `UMRISS_HINTEN` **55,1 von 6.272,7** — der Browser bricht beim ersten Fehler ab, danach kam nichts mehr. **Unbemerkt blieb es, weil die 158 Muskelflaechen die Figur tragen** und der Umriss eine 1,5-px-Linie dahinter ist. **Das Muster ist kein Abschneiden:** 11 ganze `Q`-Bloecke weg, `C`-Bloecke ohne das MITTLERE Kontrollpunktpaar, **0 Bloecke als verkuerztes Praefix** — ein misslungener Vereinfachungslauf, keine kaputte Datei. **Die 291 aus G-123 exakt reproduziert und eingeordnet:** falsch aus zwei Gruenden gleichzeitig (wiederholte Parametersaetze **und** Folgeschaden nach dem Abbruch); ein zweiter eigener Pruefer meldete zusaetzlich 116 Muskelpfade — auch falsch, kompakte Schreibweise. **Belastbar ist nur, jeden Pfad einzeln dem Browser vorzulegen: 160 geprueft, 2 kaputt, jetzt 0.** **Kein Punkt geraten** — beide heilen Umrisse liegen in drei uebereinstimmenden Kopien, gefunden ueber `react-muscle-highlighter` **im Vorgaengerrepo**; ohne diesen Griff waere HINTEN als verloren gemeldet worden (im Hauptrepo ist er in *jeder* Fassung kaputt). `[read]` **Der Umriss aendert sich sichtbar** (2,47 % / 2,66 % der Bildflaeche, Linienpixel 1.147→7.615 und 75→7.043) — die Regel „darf sich nicht veraendern" setzt einen heilen Zustand voraus, den es hier nicht gab; **Vergleichsbilder liegen bereit, Rueckbau ist ein `git checkout`.** **Teil B:** `updateWaterLogAmount` und `waterLogUpdateSchema` lagen seit G-117 fertig — es fehlten **ein Verb und ein Knopf**. `PATCH` antwortet mit neu gerechnetem Tag plus Restliste; der alte Wert steht durchgestrichen daneben (G-67-Muster). **Kreis gemessen: 500 → 250 ml nach Neuladen, `logged_ml` 2250→2000→1750, fremde Id 404 NOT_FOUND.** `[cmd]` **Eine Messung fand einen echten Fehler:** das Feld rendert **42,1 statt 78 px**, weil `.v2-feld` `flex: 1; min-width: 0` traegt — `width` verliert gegen Flex-Schrumpfen, `flex: '0 0 82px'` loest es. **Konsolenfehler 4→2 auf allen drei Kartentabs, 0 SVG in acht Bildern, 449/449 Tests.** Offen: die Bildentscheidung, `MuscleBodyMap.js` traegt denselben Schaden weiter, und eine Gate-Pruefung fehlt. |
| `166-naehrstoffbaum-anzeige.md` | **Jede Ebene klappbar, gespeicherte Ansicht, Detailmodal** (G-122). Der Baum haengt an `parent_code`; **6 Karten statt 12** (28 Kinder queren Gruppengrenzen — die Karten folgen der Wurzel, wie im Mockup); jede Ebene klappt (gemessen 0→8→12→14→17 Zeilen, GLUS auf Ebene 4), Start alles zu, „+N“ an zugeklappten Knoten. **Die letzte Sicht liegt in der DB** (`user_display_preferences`, `{offen, fenster, scope}`, 600-ms-Sammelschreiben; frischer Browserkontext zeigt sie ohne Klick; Adresse > gespeichert > Tag). **Detailmodal:** beide Zielwerte nebeneinander (persoenlich + EFSA/WHO/NAM mit `source_url`/Version), NO_(STANDALONE_)REFERENCE sichtbar mit Hinweistext, Erklaerungen als Information („Bei Mangel: Skorbut“), Kinderliste, **echter 14-Tage-Trend** (Luecken bleiben Luecken; die Fake-Kurve der Vorlage nicht nachgebaut); GLUS zeigt „unter Monosaccharide“, CLD keinen Elternteil. Altbestand nachgemessen: 114 Schluessel, **`CHOL` → `CHORL` fehlt** (Folgepunkt Codex), `SE` ohne Zielcode, 28 Codes ohne Text. Deckungsgrenze: Vorschlag „unter 50 % dimmen“ (heute 9 Codes), Entscheidung bei Tom. Tests **449/449**. |
| `167-lab-marker.md` | **C-162, Codex:** Welcher Blutwert sich verschiebt — 66 Kimi-Marker gegen den 11.676er-LOINC-Katalog: 46 Kandidaten alle bestaetigt, 18 von 20 ohne Kandidat lokal aufgeloest (`medical.lab_marker_catalog`, `supplements.substance_lab_effects`). |
| `168-naehrstoff-suche.md` | **Acht Karten, Volltextsuche, Ursachen im Filter** (G-129: GO-22 + G-127 + G-128). Die drei Makro-Aeste tragen eigene Karten — acht in fester Reihenfolge, Summen 23+37+21+17+12+16+2+10 = 138, Aeste ganz; Auslegung dokumentiert: `FIBT` unter Kohlenhydrate, Wasser/Alkohol/OA/Rohasche unter Sonstige. **Suche ohne Schema:** je Knoten zwei normalisierte Felder (Name+Code / Erklaertexte, zusammen 33.697 Zeichen); Naehe-Regel 2 = Code, 3 = Name, ab 4 = Text — gemessen, weil „EPA“ sonst ueber „R-epa-ratur“ Valin fing. „Omega 3“/„EPA“/„Skorbut“/`FE` belegt, Treffer stehen mit ihrem Ast da, Suche ist fluechtig (nicht gespeichert). **Ursachen-Regel** (`zeigeKind`): unter einem selbst auffaelligen Knoten erscheinen die Kinder MIT Wert — Vitamin A 3.582,8 ueber UL 3.000 zeigt CARTB 17.413,7 als Taeter. Randbefund: Probelaeufe ueberschreiben jetzt die gespeicherte Nutzeransicht (live stand „Auffaellig/30 Tage“ — vermutlich Tom); Gruppen-Schluessel verfallen einmalig durch die Umbenennung. Tests **453/453**. |
| `169-food-search-filter.md` | **Mehrere Tags, mehrere Ausschluesse, eine Signatur** (C-164, Codex). `nutrition.food_search` behaelt die 15 bisherigen Argumente und bekommt `p_filters jsonb` ans Ende — **innerhalb einer Gruppe ODER, zwischen Gruppen UND**. Bestehende Aufrufer laufen unveraendert weiter. |
| `170-medical-lab-marker.md` | **Medical: Medikamente und Lab-Marker** (Codex). `user_medications` mit 21 Spalten vorhanden; dev: 1 Medikation, 1 Condition; test-user leer. |
| `172-naehrstoff-aliase.md` | **Wonach ein Mensch sucht** (C-165). `nutrition.nutrient_aliases` gebaut und live: **98 Zeilen, 74 Begriffe, 50 Codes** (umgangssprache 24, uebersetzung 21, abkuerzung 16, schreibvariante 8, gruppe 29; th bewusst 0 — keine Erfindungen). **Gruppenbegriff = ein Alias, mehrere Zeilen** (PK auf dem Paar; BCAA → LEU/ILE/VAL, Elektrolyte → 5, Spurenelemente → 8, B-Vitamine → 8) — die Suche zeigt Mitglieder, keinen Kunstknoten. Testfaelle simuliert mit der exakten apps/web-Regel: Fiber/Carbs/BCAA treffen per Alias, Omega 3 traf schon per Name; **88 der 138 brauchen keinen Alias** (Name ist der Suchbegriff oder es gibt keinen Zweitnamen). Handelsbegriffe (Whey, Casein, Kreatin) bleiben im Substanzkatalog. **Anschluss fuer apps/web liegt bereit** (`nutrient_search_aliases`, gleiche Faltung) — plus gemessener Befund: die heutige Kurz-Token-Regel faende „Vitamin B5" nie; Regelerweiterung beschrieben, G-Auftrag noetig. Kette 126,3 s Exit 0, Sollliste ergaenzt, live eingespielt, testdaten-pruefen gruen. |
| `173-gesamtabgleich.md` | **Gesamtabgleich Mockup/Spec/Repo gegen Ist** (Toms Auftrag 2026-08-20): 49 Mockups (1.725 KB), 120 Specs gegen Live gemessen — **7 von 15 Entwurfs-Modulen haben keine Seite** (Admin, Buddy, Marketplace, Settings, Cross-Module, Onboarding, Stubs); Problemliste zum Durcharbeiten. |
| `174-makroziele-und-suche.md` | **Makroziele, Alias-Suche, offene Einzelwurzel** (G-147: G-143 + G-142/G-144 + G-145). Die Tabelle liest jetzt BEIDE Zielquellen: das persoenliche Ziel aus `goals.nutrition_targets` gewinnt Balken/%/Status (Protein 164,5 g gegen **170 g Ziel = 97 % „unter Ziel"** statt 234 % gegen die 70,6-g-Referenz; Fett 75 g = 83 %, KH 313 g = 54 %), die Referenz steht als „Ref."-Zeile daneben; test-user sieht seine eigenen Formelwerte (156,8 g). **C-165-Aliase angeschlossen** (drittes Suchfeld + Kurz-Token-Regel „exakter Alias trifft"): alle sieben Belege, **BCAA zeigt drei** — der Zwei-von-drei-Befund war Text-Zufall („BCAA" stand woertlich in den function_de von ILE und VAL, nicht bei LEU), nicht Leucins Zielwert. **Einzelwurzel-Karten starten offen** (Protein 10 Zeilen, zweite Ebene zu; Schliessen als `zu:`-Marker). **Zonenbalken der Vorlage gebaut** (`spektrumLage`): Vitamin A 478 % mit Markierung jenseits der UL-Zone statt eines vollen 100-%-Balkens. A-34: Nachweise auf test-user, der eine dev-Lauf mit Sichern/Zurueckschreiben. Randbefund: zwei Probeskripte verschwanden waehrend des Auftrags aus `backup/`. Tests **469/469**. |

| `171-health-score.md` | **Die zwei „Unbekannten" aus G-84 standen die ganze Zeit im Repo — und eine unmarkierte Kachel sah aus wie ein Zeilenschutzleck** (G-135). `[cmd]` **Gruppierung in `biomarker_spec_enrichment.system_groups`** (27 der 49 Zeilen: cardiovascular 7, liver 6, hormonal 6, metabolic 4, kidney 4), **Gewichtung in `SPEC_09_SCORING.md`**. **Zugeordnet wird ueber LOINC, nicht ueber den Namen:** die Spec vergleicht drei Namensfelder und verliert dabei `Total Testosterone` (Bestand `Testosterone, Total`, **gleicher Code 2986-8**). **23 der 26 Spec-Marker treffen; von den drei Fehlenden ist genau EINER eine echte Luecke** (Prolactin, 4 Werte, keine Zeile) — `HOMA-IR` ist ein Rechenwert, `Total Testosterone` eine Schreibweise. **18 der 37 Marker fallen in ein System.** Gemessen: **Gesamt 86** (nachgerechnet 85,55), Herz-Kreislauf 90 (5/6), Stoffwechsel 72 (3/3), Hormone 100 (6/6), Leber 92 (3/6), Niere 75 (1/4), **5 von 5 Systemen, 100 % des Gewichts**. `[read]` **Kein Statuswort** — die Spec kennt `warn`/`critical`, das sind Urteile (G-60); gezeigt werden Zahl, `n von m Markern` und Rechenweg. **Fuenf Spec-Fehler:** Namensvergleich traegt nicht, `?? 50` erfindet eine Zahl, `score: 0` bei leerer Datenlage (hier `null`), `HOMA-IR` ist kein Marker, **fuenf `spec_name` doppelt** (LDL als 2089-1 UND 13457-7 — naiv gezaehlt „7 von 7" statt 6 Substanzen). `[cmd]` **Zwei eigene Fehlmessungen, beide zugunsten der Anzeige:** SQL nach `created_at` statt Befunddatum (sagte 83, richtig sind 72) und SQL mit `lab_reference_*`-Pflicht (fand 13 statt 18 Marker — die App kennt den Katalogrueckfall). `[cmd]` **Ein Fehlalarm beim Zeilenschutz:** ein Zwischenstand zeigte beiden Konten dieselben 81, auch dem mit 0 Biomarkern — **kein Leck, sondern eine Ableitung aus `calcOverallHealthScore()`**. `[read]` **Die Karte trug NIE eine Attrappenmarke**, obwohl sie mit Entwurfszahlen rechnete — der Attrappenzaehler haette sie nie gemeldet. **Zeilenschutz jetzt belegt:** `test-user` sieht keine Karte, „0 von 5 Systemen · 0 %". **467/467 Tests** (14 neue), 0 SVG-Fehler. Offen: Prolactin und ApoB (zwei LOINC-Codes: Bestand 1884-6, enrichment 1869-7) sind Codex-Auftraege. |

| `175-supplements-schreibwege.md` | **Die elf Modale standen alle schon da — mit einer Begruendung, die seit Monaten falsch war** (G-148). `[cmd]` **`modale.tsx` fuehrt seit G-45 alle zwoelf Faelle**; gefehlt hat der Schreibweg, nicht das Fenster. **Jedes trug den Satz *„Es gibt kein `supplements`-Schema"* — das Schema hat 14 Tabellen und `intake_logs` 720 Zeilen** (656 taken, 64 skipped). **Vier Fenster schreiben jetzt** (`logDose`, `skip`, `add`, `reorder`), **sieben bleiben Attrappe** — je mit eigenem Grund statt des pauschalen. **Der Kreis gemessen: 360 → 361 Einnahmen, Notiz nach Neuladen im DOM, Compliance 93,1 % → 93,2 %**, danach zurueck auf 360/93,1 %. **Stack: 4 → 5 Positionen** mit Katalogsuche („Magnesium", 1 Treffer mit Evidenzgrad), zurueck auf 4. **Zeilenschutz auf allen drei Wegen je 404 NOT_FOUND** (G-79-Muster, `.select()` + Nullzeilenpruefung). `[cmd]` **Die Momentaufnahme wird aus `stack_items` gelesen, nicht vom Browser uebernommen** — sonst truege eine Zeile einen Namen, der nie im Stack stand; **derselbe Lesezugriff ist die Rechtepruefung.** `[cmd]` **Zeit auf `HH:MM:00` normalisiert** (`intake_logs_intake_time_check` verlangt Sekunden = 0). **Keine Dosisempfehlung** — Vorbelegung aus `stack_items.dose`, `typical_dose_min/max` sind auf allen 44 leer (G-91). `[read]` **Der G-135-Befund hat hier zugeschlagen, in die andere Richtung:** Nach dem Anbinden meldete das Add-Fenster *„keine Daten gelesen"*, obwohl die Seite 360 Einnahmen zeigte — **`SupplementsModale` stand ausserhalb von `SuppCtx.Provider`**, `useSupp()` lieferte den Vorgabewert. **Typecheck gruen, Tests gruen, Fenster log** — gefunden nur, weil es im Browser geoeffnet wurde. **`Rahmen` nimmt jetzt `echt`:** ein schreibendes Fenster traegt keine Marke mehr (belegt: Pille 1 → 0). **Es gibt kein `user_inventory`** — Bestand steht in `stack_items`; das Reorder-Fenster traegt nach, **bestellt aber nichts** (dafuer fehlt `shopping_lists`, C-175). **Der Skip-Grund landet in `notes`**, eine eigene Spalte gibt es nicht. **469/469 Tests, Gate 11/11, Attrappen 1, 0 SVG-Fehler**, Datenbestand danach exakt wie vorher. Offen: der Haken bucht auf den Stichtag statt auf heute, `LogDose` ist nur ueber Extended erreichbar, Nebenwirkungen und Zyklen brauchen Tabellen (C-186). |

| `176-regelwerk-laufzeit.md` | **Neun Sekunden hingen an einer einzigen Abfrage** (C-189). `[cmd]` `supplements.rule_assessment` rief `platform_input_status` **einmal je Regel auf, 64 Mal** — die uebrigen Datenquellen lagen bei rund 200 ms und trugen vor allem `docker exec`-Aufwand. Der Status wird jetzt **einmal vor der Schleife** in `v_input_status JSONB` gelegt und per `jsonb_to_recordset` gelesen; **derselbe Commit fasst zwei `daily_reference_assessment`-Aufrufe (VITD, MG) zu einem zusammen.** `[read]` Der Bericht rechnet offen vor, dass der Rest **in `platform_input_status` selbst** steckt, nicht in der Schleifenkorrektur. |
| `177-alias-bruecke-nachgezogen.md` | **Ein Waechter auf exakt 291 gegen einen Bestand von 290** (C-188/C-185). `crawl_027` fuehrt 290 Substanzen — Melanotan I war ein Duplikat und ist gemergt. **Drei Waechter von Gleichheit auf Untergrenze umgestellt** (`132_substance_alias_bridge.ts`, `134_substance_catalog.ts`, `147_substance_lab_markers.ts`), in TypeScript und in der erzeugten SQL. **Katalog 567 → 566, Quellenzeilen 667 → 668, Alias-Bruecke 1.132 → 1.541 Zeilen.** **Vier Identitaetsfehler aus `crawl_027` nachgezogen — darunter ein fabriziertes UNII bei BPC-157** (`[cmd]` im Katalog jetzt 0 Treffer auf `unii`). Kettenlauf **87 Schritte, Exit 0**; `kette.json` fuehrt genau diese 87. |
| `178-datenluecken-c187.md` | **C-187, Codex:** Shopping Lists im Recipes/Meal-Plans-Block ergaenzt, `AAE9` auf exakt neun unentbehrliche Aminosaeuren korrigiert, zehn Medikations-Ueberwachungsspalten an `medical.user_medications`, G-122-Reste eingeordnet (`CHOL -> CHORL` vorhanden, Selen fehlt, 28 Texte bleiben leer), und C-178 auf reale Objekte gemappt (`medical.lab_marker_catalog`, `biomarker_spec_enrichment.system_groups`). Kettenlauf **87 Schritte, Exit 0**. |
| `179-evidenz-konstanten.md` | **Vier beantwortete Fragen umgesetzt: C-105, C-124, GO-21, G-89** (crawl_025-Registry lokalisiert: `backup/kimi-research/.../data/evidence/`, 181+32+13 Records). **C-124: 17 numerische Bonuswerte → 0** (11 Katalog + Deckel 5,0 + 5 scoreDelta) — an ihrer Stelle Richtung + Endpunkt + Quelle je Modalitaet („Sauna: verbessert die Ausdauerleistung in Hitze, Grad C, PMID 16877041"); fuenf registerlose Modalitaeten behaupten nichts; DB war schon sauber (178+340 Zeilen alle 0,00/pending_c124_e5 — die not_evidence-Seeds waren nie live). **C-105: MAV 10 Werte + 5 Verwendungen → 0** (DO_NOT_IMPLEMENT, kein Ersatz), MEV = Richtungshinweis, MRV/RP-Rahmenwerk als Heuristik beschriftet, drei Zonen statt vier, Waechtertest umgedreht (schlaegt an, wenn Boni/MAV zurueckkommen). **GO-21: WHR/WHtR/Bauchumfang/BMI** mit WHO-Quelle+Jahr, geschlechtsabhaengig, ohne Geschlecht keine Schwelle, BMI mit Muskelmasse-Vorbehalt. **Gemeldeter Widerspruch: Auftrag sagte Frauen 0,80, Registry/WHO 2008 sagen 0,85** — gebaut ist die Registry-Zahl, nicht selbst aufgeloest. **G-89: vier Traditionen als beschriftete Heuristik** (Grad E), ohne Richtungsfarbe, Reeves/McCallum nur als Formeltext (Handgelenk/Knoechel werden nicht gemessen); FFMI-25 bleibt draussen (CONFLICTING_EVIDENCE). Tests gruen. **Dazu der Dauerfix fuer den haengenden Dev-Server:** fuenf parallele next-dev-Instanzen (Port-Ausweichen bei belegtem 3200) waren die Wurzel — jetzt `tools/server.py` (status/aufraeumen/start/neustart, weicht nie aus, schont Admin/Coach), Regel in CLAUDE.md: nie `pnpm dev` von Hand. |

| `180-c191-offene-reste.md` | **C-191, Codex:** C-178 auf echte Medical-Objekte korrigiert: `biomarker_spec_enrichment` 49 -> 51, `lab_marker_catalog` bleibt 66; ApoB auf `1884-6`, Prolactin auf `2842-3`, `system_groups` ist `text[]`. Selen fehlt im importierten BLS-Katalog; `S` ist Schwefel, `SER` Serin, `SE` existiert nicht. Die 28 Texte sind keine `nutrient_defs`-Beschreibungsspalte und keine Thai-Luecke. |

| `181-c289-nachtlaeufe.md` | **C-289/C-290 (Codex) und G-200/G-201 (Claude Code), geprueft 2026-08-27:** Der Kettenvergleich steht wieder — eigener Frischlauf 119 Schritte, `SCHEMA VOLLSTAENDIG`, `im_katalog` 412 auf beiden Seiten, 0 sichtbare Unterformen. Ursache war nicht die Datenmenge (333 MB, 816 GB frei), sondern aktive Sessions beim Klon und eine `net`-Funktion mit `SET log_min_messages`; der Ausfuehrer klont ohnehin nicht. Kettenschritt `286` strukturell von `286a` getrennt. Abschlusspruefer isoliert gemessen: **18,7 s bei 51 SQL-Aufrufen** (Codex: 24,1 s / 51; Ausgangslage 215 s / 653) — **mein erster Einwand „Faktor 1,3“ war falsch, ich hatte Kettengesamtzeit gegen Pruefzeit gestellt.** Kettengesamtzeit 167,8 s bei mir gegen 134,6 s im Bericht, beide Zahlen stehen nebeneinander. C-290: Migrationsgrenze in `supabase/README.md` festgehalten, **aber der Waechter findet nur einen von fuenf Faellen** (`DO`-Block, `DELETE`, `TRUNCATE`, `MERGE` bleiben gruen) **und laeuft in keinem Gate** — C-291. G-200: das Suchmuster kannte nur Kleinbuchstaben, `community_anzeigeX` wurde nie gesehen; eigene Sabotage plus byteidentischer Rueckbau belegen die Reparatur. G-201: genau eine betroffene Stelle, Wortgrenze statt `includes`, kein Waechter. **Nebenbefunde:** elf Kettenschritte lesen aus `backup/kimi-research/`, sieben aus `docs/kimi_research/` (C-295); der Dublettenpruefer schreibt bei jedem Gate-Lauf (G-202). |

| `182-c291-datenlogik-waechter.md` | **C-291 (Codex), geprueft 2026-08-27:** Der Waechter erkennt jetzt `INSERT`, `UPDATE`, `COPY`, `DELETE`, `TRUNCATE`, `MERGE` — auf oberster Ebene und in Dollar-Bloecken — und haengt im Gate an Position 5 von 13. **Vom Orchestrator mit dreizehn echten Probemigrationen nachgemessen:** zehn urteilen richtig, darunter zwei nicht beauftragte Formen (`WITH … INSERT` und ein Backfill im Funktionskoerper). **Die benannte Ausnahme `public.handle_new_user()` ist in beide Richtungen geprueft** — ein zusaetzliches `DELETE` im selben Koerper und ein anderes Zielobjekt werden rot, Rueckbau je sha256-identisch; sie ist an Datei, Funktion, Befehlszahl und SQL-Inhalt gebunden, nicht am Namen. **Drei Restluecken** (geschachteltes Dollar-Quoting, `CREATE TABLE AS`, `SELECT INTO`) als C-297, **zwei Fehlalarme** (`FOR UPDATE` und `ON DELETE CASCADE` in `DO`-Bloecken, weil dort frei im Text statt am Statement-Anfang gesucht wird) als C-298. |

| `183-g203-rls-aufschlag.md` | **G-203 (Claude Code), geprueft 2026-08-27:** Die 700 ms zwischen Datenbank und Anwendung sind **keine der vier Vermutungen, sondern RLS.** Gegenprobe im selben Serverprozess: dieselbe RPC ueber den Supabase-Client (Nutzer-Token) 838-929 ms, als nacktes `fetch` (Dienstschluessel) 152-186 ms; auf `test-user` 17-26 gegen 14-18. **Vom Orchestrator per Rollentausch nachgemessen:** dev `service_role` 158,7/174,5/166,9 ms bei 25.022 Pufferzugriffen, dev `authenticated` 553,3/551,5/553,3 ms bei 128.655 — **+387 ms und Faktor 5,1 in den Puffern fuer dieselben 64 Ergebniszeilen**; test-user +2 ms, Puffer unveraendert. `rule_assessment` ist SECURITY INVOKER (`prosecdef = f`). **Damit ist C-278 (Ueberbau) ausgeschlossen** — ein Aufschlag von 3 ms hier und 700 ms dort bei gleicher Zeilenzahl ist keiner. **Zwei Praezisierungen:** die Funktion liest **14** Tabellen, nicht fuenf, alle mit RLS ausser `nutrition.daily_summary`; und die Zuordnung zu C-279 ist zu grob — das Kreuzprodukt (temp read 9.457) tritt **ohne** RLS auf, der RLS-Aufschlag ist ein zweiter Posten (C-299). **270-360 ms bleiben unerklaert** (C-300), fuer test-user geht die Rechnung glatt auf. Dazu C-301 (zwei SELECT-Policies auf `intake_logs`), G-206 (Kompression aus), G-205 (der Dev-Server beendet sich selbst). |

| `184-c292-kimi-welle1.md` | **C-292 (Codex), geprueft 2026-08-27:** Kimis Welle 1 additiv importiert. Live nachgemessen: 498 Wirkstoffe, **CAS 489, ATC 497, MoA 494, Precautions 393, Reproduktion 498**. **Die Abweichung von meinen Auftragszahlen ist die richtige** — meine waren Bruttozeilen, seine die Werte mit Inhalt: CAS 498−9 leere, MoA 498−4, Prec 397−4, ATC 498−1 (Sacubitril trug bereits C09CA und wurde nicht ueberschrieben, Chlorine bleibt leer). **Provenienz haelt:** 442 Wirkstoffe mit `c292_identifiers` à 465 Rohzeilen, 302 MoA- und 385 Precaution-Records; alle 81 neuen Reproduktionszeilen mit leerem `sex_specific` **und** gefuelltem `missing_fertility_sex` — der Fallstrick aus dem Auftrag ist umschifft. Neuer Schritt liest nur aus `docs/kimi_research/`, die elf alten Pfade unangetastet (C-295 offen). Abschlusspruefer gegen Live 18,4 s, SCHEMA VOLLSTAENDIG mit den neuen Mindestwerten. **Zwei Berichtigungen an meinem Auftrag:** 14 statt 15 vorbelegte ATC, und die falschen `drug_class`-Tags sind **zehn Tags in 17 Records**, darunter neun Antidepressiva als `maoi` — C-296 traegt jetzt die Liste. |

| `185-c293-medikamententexte.md` | **C-293 (Codex), geprueft 2026-08-27:** Kimis Block D importiert. Live nachgemessen: `medication_user_texts` **498 Zeilen auf 498 Wirkstoffe**, `medication_faq` **2.313 auf 498**. Variety gemessen statt geglaubt: `kurz_was_de` 498 verschiedene von 498, `antwort_de` 2.313 von 2.313. Zwoelf Textfelder je `_de`/`_en`/`_th`, `en` und `th` durchgaengig leer; Datenlogik-Waechter (C-291) gruen, die Migration traegt nur Struktur. **Der wichtigere Befund: meine Auftragsvermutung ist gefallen.** Ich hatte `[annahme]` geschrieben, Bedarfsmedikamente haetten kein „zu wenig“ — Codex hat geprueft und es haelt nicht durchgaengig. 40 ohne `zu_wenig_de`, 115 ohne `mythen_de`, 11 ohne beides, alle mit `null_context: reason_status "not_supplied"` statt einer erfundenen klinischen Aussage. Nachgefordert als C-294. |

| `186-g207-symptome.md` | **G-207 (Claude Code), geprueft 2026-08-27:** Der Zuschnitt hielt, die Ausgangslage nicht — `medical.symptoms` (34) und `symptom_biomarker_map` (102) existierten bereits, die C-159-Meldung „keine Symptomtabelle“ gilt nicht mehr. Nachgemessen: HIGH 10 / MODERATE 48 / LOW 44, `reason_de` bei allen 102, `is_diagnosis_claim` nirgends gesetzt. **Die entscheidende Frage lautet nein:** `symptom_not_in_catalog` 49 von 102, `marker_not_in_explanations` 2 (`lab_bnp` bei `sym_edema` HIGH, `lab_crp` bei `sym_abdominal_pain` MODERATE) — C-304. **Und es waere nie aufgefallen:** `tab-tracking.tsx:114` warf Unbekanntes mit `.filter(Boolean)` weg, Zeile 218 schrieb `b?.abbr ?? m`, ein unbekannter Marker sah aus wie ein bekannter. **Zwei Berichtigungen an meinem Auftrag:** die 27 Attrappen waren falsch gemessen (`git grep -c` zaehlt Zeilen je Datei, nicht Vorkommen — richtig sind 18, nachher 17), und ich hatte Katalog und Tagebuchzeile verwechselt — erfasste Symptome brauchen eine eigene Tabelle, Entwurf als C-303. |

| `187-c299-c300-rls-rest.md` | **C-299 und C-300 (Codex), geprueft 2026-08-27:** Fuenf Policies auf `user_id IN (SELECT …)` umgestellt, Sicherheitsnachweis fuer alle vier Faelle gruen, `intake_logs` von 6,155 auf 0,240 ms (Faktor 25,6). **Aber `rule_assessment` sank nur 549 → 537 ms und die Pufferzahl bewegte sich um elf Bloecke von 128.000** — meine Deutung, die 103.633 Bloecke seien 33.000 `hat_sicht`-Aufrufe, schien damit widerlegt. **C-300 hat sie rehabilitiert:** `nutrition.meals` und `meal_items` fuehren **62.461 Aufrufe** aus, weil `nutrition.daily_summary` eine VIEW mit `{security_invoker=true}` ist — die Policies der Basistabellen greifen beim Aufrufer. `meals` 2.895 Zeilen davon 2.170 fremd, `meal_items` 9.051. **Der Mechanismus stimmte, meine Tabellenliste nicht:** ich hatte die gelesenen Tabellen per Regex ueber `prosrc` erhoben, und ein Regex loest keine View auf. **Lehre: wer die Kosten einer Funktion abschaetzt, muss die Views aufloesen** — und eine Hypothese nicht verwerfen, solange sie nur am falschen Ort gesucht hat. Rest als C-305. |

| `188-g208-wirkstoffkatalog.md` | **G-208 (Claude Code), geprueft 2026-08-27:** 498 Wirkstoffe und 2.313 FAQ sichtbar, alle fuenf Auftragszahlen getroffen. **Die Praezisierung zur Zaehlweise ist der wichtigste Satz:** `precautions is not null` haette 0 Luecken gemeldet — die Spalte ist bei allen 498 nicht null, **105 tragen `[]`**. Die Unterscheidung *begruendet leer* gegen *nicht bearbeitet* ist in drei Formen sichtbar, gelesen aus `evidence_provenance.*.missing_reason` und `null_context.*.reason_status`. **`drug_class` weggelassen, und C-296 unterschaetzte es:** jeder Tag doppelt (`maoi` 15 / `MAOI` 15, `ssri` 10 / `SSRI` 10), acht Wirkstoffe tragen `{SSRI,MAOI,maoi,ssri}` gleichzeitig — unbrauchbar, nicht fehlerbehaftet. **Negativprobe:** CAS entzogen → Kachel wurde `nicht_bearbeitet`, die Ansicht weigerte sich eine Begruendung zu erfinden; der zweite Teil ging nicht, weil die deutschen Textspalten `NOT NULL` sind. Drei Fallen: `atc_code` als text mit JSON (C-306), `mythen_de` in drei Gestalten (C-307), A-30 Wert-Import aus dem Leseweg — **die Warnung stand im Kopf der Datei, die Falle wurde trotzdem gebaut; der Build hat sie gefunden, nicht der Kommentar.** |

| `189-c310-wirkstoff-dubletten.md` | **C-310 (Codex), geprueft 2026-08-27:** Drei CAS-Dublettgruppen bestaetigt — Acetaminophen/Paracetamol (103-90-2), Ciclosporin/Cyclosporine (59865-13-3), zwei Schreibweisen von Sulfamethoxazol/Trimethoprim. **Keine Regel nennt einen der Namen** (Textsuche in `conditions`: 0 Treffer) — die Regeln greifen ueber Eigenschaften: 10 ueber `drug_class`, 4 ueber CYP3A4, 1 ueber `cyp_profile`. **Also entscheidet der gewaehlte Eintrag, welche Bedingungen zutreffen, und der Unterschied ist unsichtbar.** **Codex hat meine Zahl berichtigt:** 20 `medication`-Regeln, nicht 31 — ich hatte einen Textsuchtreffer (31) fuer eine Kategorie gehalten, `modules_involved` ergibt wieder eine dritte Zahl (48). **Entscheidung Tom: die INN-Form fuehrt.** Gemessen stuetzt das: Paracetamol 2 Produkte gegen 1, Ciclosporin traegt als einziger das CYP3A4-Profil, an dem vier Regeln haengen — und der Acetaminophen-Eintrag traegt `opioid`, einen der falschen Tags aus C-296. **Zwei Vorgaben:** die US-Form wird Synonym statt geloescht, und die Zusammenfuehrung nimmt die reichere Eigenschaftsmenge, nicht die des fuehrenden Eintrags — *„der fuehrende gewinnt“* haette Ciclosporins CYP3A4-Profil vernichtet. |

**Was als Nächstes zu tun ist:** `docs/todo/TODO.md`
**Getroffene Entscheidungen:** `docs/spezifikation/90-entscheidungen/`
(D-06, 2026-08-06: `docs/decisions/` war nie befüllt — `[cmd]` einzige
getrackte Datei dort ist `.gitkeep`. Der Ort ist aufgelöst.)
**Sitzungsübergaben:** `docs/sessions/`

`[cmd]` **Zuletzt: `2026-08-26-katalog.md`** — zwei Tage, 31 Aufträge,
drei Agenten parallel. Der Substanzkatalog von 290 Fachnotizen auf
**412 sichtbare Substanzen** mit 446 Nutzertexten und 1.970 FAQ-Zeilen;
Schema `wissen` angelegt; der Medikamentenkatalog begonnen. **Und die
drei Fehler des Orchestrators, die den Weg bestimmt haben** — Kimis
Bestand dreimal zitiert und nie gelesen, achtmal am falschen
Ausschnitt gemessen, die Reihenfolge verkehrt. **Aus allen dreien sind
Gate-Prüfungen geworden.**

---

## Was hier NICHT steht

- **Produktspezifikation** → `docs/specs/` (13 Module, ~1,5 MB, Zielbild)
- **Design-System** → `docs/design-system/`
- **Historische Brainstorms** → `docs/BrainstormDocs/` (archiviert, keine Referenz)
- **Altlast Governance-Dokumentation** → `docs/_archive/`

---

## Pflege

Diese Dateien werden bei jeder Sitzung aktualisiert, in der sich etwas ändert.
Wer eine Aussage ändert, ändert auch ihren Marker und das Datum im Kopf der Datei.
Eine SSOT ohne Datum ist wertlos.
