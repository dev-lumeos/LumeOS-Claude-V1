# P1-005 Additive Migration Candidate Plan

> **Status**: Candidate Plan — *Not Approved for Execution*
> **Purpose**: Identify and sequence additive schema preparation steps for P1-005
> **Authorization**: *No DB work, Supabase commands, migration execution, or product implementation is authorized under this plan*

---

## 1. Scope & Intent

This document outlines **candidate additive schema preparation steps** inferred from:
- Current P1-005 specifications (if available in `docs/`)
- Existing local database inventory (schema, tables, relationships)

It is **not** an approved migration file. It is a planning artifact only.

---

## 2. Candidate Additive Steps (Inferred)

The following steps are *likely* additive (non-breaking) changes, based on typical P1-005 patterns. Each candidate must be validated against the final spec before any migration file is authored.

| # | Candidate Step | Type | Rationale |
|---|----------------|------|----------|
| 1 | Add `is_deleted` column to `meals` table | Column Add | Soft-delete pattern common in nutrition logs |
| 2 | Add `created_at` index on `meal_logs` | Index Add | Query performance for time-range filters |
| 3 | Add `metadata` JSONB column to `recipes` | Column Add | Flexible storage for dietary tags, allergens |
| 4 | Add `user_preferences` table | Table Add | Store per-user nutrition goals and constraints |
| 5 | Add `sync_status` enum column to `sync_queue` | Column Add | Track local ↔ cloud sync state |

> **Note**: These candidates are *inferred* and may change or be removed pending spec finalization.

---

## 3. Sequencing & Validation Gates

### 3.1 Sequencing Principles

- **Additive-first**: All candidate steps must be additive (no column drops, no type changes, no constraint removals)
- **Dependency-aware**: Indexes after tables/columns exist; foreign keys after referenced tables exist
- **Reversible**: Each step must be reversible without data loss

### 3.2 Validation Gates (Pre-Migration)

Before any candidate step is converted to an approved migration file, the following gates must pass:

| Gate | Description |
|------|-------------|
| `spec-finalization-gate` | P1-005 spec must be finalized and match candidate steps |
| `schema-inventory-gate` | Local inventory must be confirmed (via `supabase db dump` or equivalent) |
| `additive-verification-gate` | Each step must be verified as additive by a senior engineer |
| `rollback-plan-gate` | Reversal SQL must be documented and validated |

---

## 4. Plan Candidates vs. Approved Migration Files

| Category | Description | Example |
|----------|-------------|--------|
| **Plan Candidates** | This document, inferred steps, no SQL files | `docs/project/p1-005/P1-005-additive-migration-candidate-plan.md` |
| **Approved Migration Files** | Executable SQL files in `db/migrations/`, signed off, versioned | `db/migrations/20250405120000_add_is_deleted_to_meals.sql` |

**Key distinction**: Plan candidates are *drafts*. Approved migration files are *production artifacts*.

---

## 5. Explicit Non-Authorizations

Under this P1-005 additive migration candidate plan:

- ❌ No database changes (local or remote)
- ❌ No Supabase CLI commands executed
- ❌ No migration files created or applied
- ❌ No product code changes
- ❌ No data migration or transformation

This plan is **read-only planning**. Execution requires a separate, approved workorder with explicit migration authorization.

---

## 6. Next Steps (Post-Approval)

If P1-005 is approved for execution:

1. Convert candidate steps to approved migration files in `db/migrations/`
2. Run `supabase db diff` to validate against local inventory
3. Execute `supabase db push` in staging only
4. Document rollback procedure in `docs/project/p1-005/P1-005-rollback-plan.md`

---

*Generated by docs-agent. No production action authorized.*
